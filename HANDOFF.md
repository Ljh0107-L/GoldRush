# HANDOFF — 工具与事实交接

**这份文档故意\*不\*包含我们的策略结论。**

我们前几天的判断、试过的方向、判负的候选,全部在 `src/CHANGELOG.md` 里。
**建议你先形成自己的判断,再去读它** —— 那些结论有它们各自的成立条件,而先读会锚定你。

本文只提供四样东西:**规则事实 · 工具怎么用 · 数据在哪 · 对手是谁及怎么逆向。**

---

## 0. 五分钟上手

```bash
cd ~/GoldRush

# 1. 看现役构型能不能跑,并确认模拟器保真
python3 sim/validate.py                       # 保真度套件,应 hard_pass
python3 sim/cli.py --p1 stay --p2 scripted --map map1 --games 1

# 2. 看平台状态
python3 tools/arena.py rank -n 130            # 排行榜(队名/胜率/P90)
python3 tools/arena.py quota                  # 今日配额(已用/上限/剩余,只读)
python3 tools/arena.py opponents              # 可挑战的公开位
python3 tools/arena.py games                  # 我方对局记录

# 3. 看规则
less docs/赛制介绍.md                          # 官方赛制
less docs/PRELIM_RULES.md                     # 初赛条款审计(带行号回源)
less docs/QUOTA.md                             # 每日配额探测(只读接口)
```

**平台凭据**:官网 `http://47.103.127.219/`,ID `220`,KEY 在 `tools/.arena_token`(已 gitignore)。
**我方队名** `0x8F`,`user_id = 220`,公开位 `model_id = 278135`。

---

## 1. 规则事实(全部可回源,附出处)

| 事实 | 出处 |
|---|---|
| 17×17 网格,500 轮,**得分 = 毛金币 − 视野费** | `docs/赛制介绍.md` |
| 每轮两玩家各 6 步(两单位分配),**决策耗时短者先动** | 同上 |
| NPC 7 个,行动夹在两玩家之间 | 引擎实测 |
| **每轮 300ms 上限 + 60 秒总思考池**,穿池判负 | `赛制介绍.md` |
| 初赛 = **引擎自动汇总代码 + 全场循环赛 + 按胜率排名 + 前 16 晋级** | `赛制介绍.md:33-39` |
| **极速优化奖** = 初赛前 16 中 P90 延迟最低,**限 C++,前三名除外** | `赛制介绍.md:211` |
| 提交物:C++ 交 `.so`(≤16MB) | `赛制介绍.md:103-105` |
| **公开位(`stage=4`)是当前唯一可提交的代码位**;`add_model_4` 是 **upsert**,每队仅一个 | `PRELIM_RULES.md:201-203`,实测 105 模型/105 distinct user_id |
| 时间线:报名 8.14 · 模拟赛 8.15-16 · **初赛 8.17-21** · 决赛 9.6 | `赛制介绍.md:13` |
| 每日配额 **500 局**(仅我方\*发起\*的局计入),北京午夜 = **UTC 16:00** 重置；探测见 `docs/QUOTA.md` | `FAQ:366` + `get_user_info` |

**两个仍未知的**(`PRELIM_RULES.md:236-290` 记为待确认):初赛用防守位还是另有专用提交入口;初赛期间(8.17-21)能否继续更新提交。
⇒ 在弄清之前,占优做法是**任何时刻让公开位保持你的最佳交付物**。

---

## 2. 仓库地图

```
src/player.cpp        现役策略实现(C++,单文件,唯一构型)
src/INFRA.md          成本模型:指令→ns→金币的换算与测量协议
src/CHANGELOG.md      版本谱系 + 全部判负记录 + 军规(想读结论时看这里)
AGENT.md              作战手册:平台操作、配额规程、当前状态
HANDOFF.md            本文件

sim/                  本地模拟器核心(纯 Python,标准库)
sim/probe/            观测探针(买满视野跟随对手)
tests/                验收工具:pair_diff / verify_construct.sh / 延迟台架
tools/arena.py        平台客户端(含 quota 只读探测)
docs/                 官方赛制、FAQ、初赛审计、配额探测(QUOTA.md)
logs/                 对局日志归档(gitignore)
```

---

## 3. `sim/` — 本地模拟器

**它是什么**:官方 ABI 兼容的确定性重放引擎,纯 Python 标准库,不联网,可直接加载策略 `.so`。

### 3.1 怎么跑

```bash
# 单局
python3 sim/cli.py --p1 a.so --p2 b.so --map map1 --seed 1000 --games 1

# ⭐ 同种子配对 A/B + 换座(这是唯一推荐的比较方式)
python3 sim/cli.py --p1 a.so --p2 b.so --map map1 --seed 1000 --games 30 --paired

# 固定行动顺序(用于隔离顺序效应)
python3 sim/cli.py --p1 a.so --p2 b.so --map map1 --dispatch fixed --fixed-costs 200,201
#   fixed-costs 200,201 → p1 先动;201,200 → p2 先动

# 并行
python3 sim/cli.py ... --jobs 8
```

`--map` 接受 `map1/map2/map3`、地图 JSON、日志第二行 JSON,或**一份官方日志**(用于机械回放)。

### 3.2 保真度(实测,`python3 sim/validate.py` 可复跑)

| 项 | 结果 |
|---|---|
| 机械回放 end-grid 逐格 | **433,500 / 433,500** |
| 快照 | **8,910 / 8,910** |
| 官方迷雾过滤 | **8,000 / 8,000** |
| 同种子 SHA | 逐字节可复现 |
| 单局耗时 | 约 4 秒 |

### 3.3 ⚠️ 已知局限(用之前必须知道)

1. ⭐ **完全不建模延迟。** 模拟器只算金币。任何延迟结论都不能从这里出。
2. **NPC 模型是拟合的**,动作命中率约 **38.7%**;且它偏贪、偏中央 ⇒ 会系统性压低中央剩余金币。
   ⇒ **绝对收入不可与平台比较;只能做同种子配对 A/B。**
3. **map3 的 token-2 元数据不完整**(外圈热点语义未经全信息日志交叉验证)。
4. 生成/NPC 参数是**公测期标定**的,赛事方明确不保证正赛沿用当前地图与 NPC 配置。

### 3.4 ⭐ 一个现成的资产:真实棋盘重构器

引擎时序是 `快方 → NPC×7 → 慢方`。⇒ **当我方是慢方时,那九个 actor 的动作已全部发生,且不可能依赖我方本轮动作** ⇒ **我方将面对的棋盘在决策前是\*可精确计算\*的。**

`sim/analyze_order_sensitivity.py` 里建了这个重构器,并验证过:
**预测的轮末纯净地面 vs 官方日志逐格一致 = 9,000 / 9,000 轮。**

它重放对手的 `PlayerInput`(用 `render_filtered_ground` + `visible_cells`)、查询对手 `.so` 的本轮决策、按 `_stable_seed("npc-policy", digest, round, npc_id)` 复演 7 个 NPC、再从 `execute_round` 自身重导移动/拾取/炸弹消耗/踩踏。

⇒ **如果你的策略要用"我看到的是真实棋盘"这个性质,这套东西已经写好并验证过了。**

### 3.5 其它可能有用的模块

| 文件 | 作用 |
|---|---|
| `sim/engine.py` | 引擎本体 |
| `sim/abi.py` | `GameInput` 字节布局断言 |
| `sim/maps.json` | 三张公测地图 |
| `sim/maps_unknown.json` | 陌生图注册表（初赛换图） |
| `sim/maps_final_photos.json` | 历史照片重建的终局图（**仅墙体几何,无 token-2 热点元数据** ⇒ 只能做几何比较）|
| `sim/GENERATION.md` | 金币生成机制实测(公测期) |
| `sim/OPPONENTS.md` | 对手画像与逆向方法 |
| `sim/make_unknown_maps.py` · `audit_unknown_maps.py` | 生成陌生图并审计构型在其上的行为 |
| `sim/analyze_order_sensitivity.py` | 慢方真实棋盘重构器（已验证） |
| `sim/field_sample.py` | 平台分层抽样 |
| `sim/ladder_ledger.py` · `passive_monitor.py` | 公开位被动局记账 |
| `sim/probe/` | 买满视野的观测探针 + 日志归档器 |

---

## 4. 开发机 / 赛事机

```bash
ssh Ubiquant220@8.153.76.120     # 免密已配,密码同平台 KEY
```

**规格**:AMD EPYC Zen4(AVX-512 全套)· GCC 14.3.1 · **与评测机同构**。

### 4.1 构建

```bash
rsync -a src/ Ubiquant220@8.153.76.120:~/goldrush/src/
ssh Ubiquant220@8.153.76.120 'cd ~/goldrush/src && \
  g++ -std=c++17 -O3 -march=native -fPIC -Wall -Wextra -shared -o player.so player.cpp'
```

### 4.2 ⛔ 不要在本地 macOS 构建

历史事故:在一台 Intel Granite Rapids 机器上用 `-march=native` 编译,生成了评测机不支持的 AVX512-FP16 指令(`vmovw`),**平台 SIGILL 判负**。
⇒ **产物必须在赛事机构建**,并检查 FP16 指令计数为 0(`verify_construct.sh` 会查)。

### 4.3 ⭐ 跨机交付用 `.text` sha256,不要用源码 sha256

注释差异会让源码 sha 不同而编译产物 `.text` **逐字节相同**;反之表宽/立即数变化会让 `.text` 尺寸相同而内容不同。
⇒ **对齐"我测的二进制"与"我落库的源码"唯一可靠的方式是比较 `.text` 的 sha256。**

另:GCC 默认 `-frandom-seed` 取自**源文件名** ⇒ 把 `player.cpp` 改名重建会得到不同 SHA(即使代码相同)。

### 4.4 全量日志

```bash
ssh Ubiquant220@8.153.76.120 'ls -la /share/data.tar.gz'   # 13.4MB
# 解压后 data/full/ 是上帝视角日志:无迷雾、含全部 7 个 NPC 的动作与拾取
```

⚠️ 官方那 201 份日志**全部来自 map1**,对手是弱演示 bot(12.6μs)。但 `data/full/` 是模拟器保真验证的核心资产。

---

## 5. `src/INFRA.md` — 成本模型与延迟测量

### 5.1 换算(注意每一项都有前提)

| 量 | 值 | 前提 |
|---|---|---|
| 平台单轮 cost 经验式 | `≈ 40ns 壳 + 2ns/条载荷 + 指令 × 0.1454ns` | 该输入集 |
| **均价** | 约 **0.1454 ns/指令** | 整函数平均 |
| ⚠️ **边际价** | 实测低至 **0.025 ns/指令** | 依赖/访存受限区,删指令≠变快 |
| 交叉带汇率 | 双方 P50 相距 ≤20ns 时,**1ns ≈ 11 金/局** | **远离交叉点显著下降** |

⇒ ⭐ **指令数只是\*平均\*成本的代理。** 判例两则,方向都不可预测:
- 一次改动 **−42 指令**,实测平台 **P90 +113ns**(它把一张每轮读的查表加宽,`.rodata` 1360→1808)
- 同一个改动的两个代理估计相差 **70 倍**

⇒ **热\*总\*足迹(`.text` + 热 `.rodata`)的尺寸增长本身是一等成本信号**,而且必须合看 —— 分项看会被"换容器"绕过。

### 5.2 延迟测量:按可信度排序

| 优先 | 手段 | 说明 |
|---|---|---|
| **1(唯一权威)** | ⭐ **平台内窗交错对照** | base/候选**交替提交**、同对手同图、**逐局串行**。三图各 2 局共 12 局即可分辨 10ns 级 |
| **2** | 已校准逐出台架 | `tests/tail_icache_thrash.cpp`,**38,400 B 代码-only 逐出**;⚠️ 加数据 thrash 会摧毁信号 |
| **3(仅温态)** | `--mode hot` 回放 | 刻意**不**做逐出 ⇒ 测的是温态;**禁用于足迹/冷路径判断** |

⛔ **两条硬纪律**:
1. **并发提交会把我方自己的读数放大 4.3–4.7 倍**(实测 P50 190→820、P90 290→1320)⇒ **延迟测量必须逐局串行**。
2. **跨窗绝对值不可比**;但**小 Δ 精确可迁移**(`docs/LATENCY_VALIDATION.md` 标定:27 TSC ticks = 10.00ns,与平台 −10ns 对上)⇒ **"Δ 加到已记录的绝对值"合法,"两个窗口的绝对值直接相减"不合法**。
3. 本机 TSC 只按 **27 ticks 步进(= 10.00ns)** ⇒ **亚量子结果不得报成正**。
4. 平台所有 `cost` 都是 **10ns 整数倍** ⇒ 亚 10ns 的增量在不跨桶时翻转为零、跨桶时翻转一整档。

### 5.3 相关工具

```bash
tests/latency_bench.cpp        # TSC 台架(hot/cold/cold2 三档; ⚠ cold 对「冷读 vs 冷代码」无分辨力)
tests/tsc_calibrate.cpp        # TSC 频率标定(实测 2.699993 GHz)
tests/icount.cpp               # 同流动态指令数与 cycles(perf_event_open,需赛事机; 验收量是 cycles)
# 战役期一次性台架(tail_path_bench/analyze_latency 等)已移出工作树, 取用: git log --diff-filter=D
```

---

## 6. 验收工具

### 6.1 `tests/pair_diff.py` — 逐位行为对账

```bash
python3 tests/pair_diff.py a.so b.so <日志...>
```

对同一批输入,逐轮比较两个 `.so` 发出的动作。等价重构应得 `0/500`。

⚠️ **它的真实地位**:`pair_diff 0/500` 是**「在\*已记录的\*输入分布上无分歧」的证据,不是等价性的\*证明\***。
判例:一处"看起来逐位等价"的复用实际不等价(LUT 索引被钳位到 ±3,而另一处收到未钳位值,行/列优先判据因此可翻转)—— 只有某份日志**恰好包含那种几何**时才会显影。
⇒ **凡声称等价重构,请\*额外\*给定义域论证**(索引范围、钳位、饱和),`pair_diff` 作补充证据。

⚠️ 另:`tests/dump_inputs.py` 生成的输入里,`enter`/`leave`/`gold_generated`/`gold_collected` 四个流量字段**故意留空并声明失明**(引擎窗口错位,填错会产生伪分歧)。覆盖面表会在 `verify_construct.sh` 通过时整张打印。

### 6.2 `tests/verify_construct.sh` — 构型护栏

```bash
ssh Ubiquant220@8.153.76.120 'cd ~/goldrush && ./tests/verify_construct.sh --baseline-so /tmp/base.so'
```

五项:`pair_diff` 三图 · **入口 `mod64`** · FP16 计数 = 0 · 产物 SHA256 对账 · 同流指令数。
⚠️ **它在 macOS arm64 无法构建**(`_MM_HINT_T0` 等),只能在赛事机跑,或逐项手工执行其判据。

### 6.3 ⚠️ 一个会咬人的布局细节:入口对齐

`moveDecision` 的入口地址 **mod 64** 是一个**离散悬崖**:实测 `0x10` 是唯一未退化档,`0x20`/`0x30` 各 **+11.67ns**。

- 现役用一段死垫 `asm(".space N, 0x90")` 把入口归一到 `0x10`
- **垫长与代码体积耦合** ⇒ 任何改动都要重查 `nm` + `% 64` 并重调垫
- 公式:`pad ≡ (32 − X) mod 64`,其中 X 是入口前代码的字节增量;**一次到位,不要逐步试减**(中间存在坏桶)
- ⛔ **不要从源码位置推断"什么排在入口之前"** —— GCC 不按源码顺序发射函数,**只有 `nm` 说的算**
- ⛔ `.rodata` 排在 `.text` 之后(`.text @ 0x1040`,`.rodata @ 0x3000`,页对齐)⇒ **增大 `.rodata` 不会移动入口**(单变量干预已验证)

---

## 7. 平台操作 `tools/arena.py`

```bash
python3 tools/arena.py rank -n 130      # 排行榜
python3 tools/arena.py maps             # 地图列表
python3 tools/arena.py opponents        # 可挑战的公开位(含 updated_at / lang)
python3 tools/arena.py submit ...       # 发起对局
python3 tools/arena.py info <id>        # 单局状态
python3 tools/arena.py log --id <id>    # 下载日志(⚠️ 参数名是 id,不是 game_id)
python3 tools/arena.py watch ...        # 轮询直到可回放并保存
python3 tools/arena.py publish ...      # 上传公开位(add_model_4,upsert)
```

### 7.1 配额

规程全文：`docs/QUOTA.md`。一句话：

```bash
python3 tools/arena.py quota          # 只读。剩余 = daily_initiate_limit - today_initiated
```

- **探测自由，动用须所有者批准。** 不是「剩余 20 局冻结」（那是 `run_rikka_batch.py` 的本地 `RESERVE`）。
- ⛔ **必须读 `today_initiated`，不要数 `get_game_list_1` 的行数** —— 列表含别人挑战我方的局，那些**不占配额**（曾因此差 28 局）。

### 7.2 `publish` 后必做的后验

1. `model_id` 仍是 **278135**(upsert,不应新建)—— 查 `get_model_list_4` 里 `user_id=220` 的条目**只有一条**
2. `updated_at` 已更新到 publish 时刻
3. 记录 `user_cost1` 与读取时刻(**publish 前也要记一次**,否则没有对照)
4. 记录**被发布产物的 sha256** 与**它对应的源码 commit** —— 这是本地台账,不是平台对账,见下

> 🔴 **原第 3 项「平台侧 SHA256 与落库产物一致」已删除:它\*不可执行\***(2026-08-13 实测)。
> `tools/arena.py` **没有取回已发布产物的接口**,平台的 `get_model_list_4` 只返回
> `id / stage / user_id / lang / name / created_at / updated_at / user_name_cn`,**不含任何摘要字段**。
> ⇒ **`publish` 是单向通道:发上去之后无法读回比对。**
> ⭐ 按本仓军规「凡『知道规则就能避免』的防线一律视为无效防线」,一条**无法执行**的检查比没有更坏 ——
> 它会让人以为对过账了。**唯一可得的替代是本地台账**:把「发布时刻 · 产物 sha256 · 源码 commit」
> 三者同址记进 CHANGELOG,使「平台上跑的是哪一版」可由时间线反推。

### 7.3 ⚠️ `user_cost1`(榜显那个延迟数)的用法禁令

它的聚合方式**未知**,两个锚点互相矛盾:T-1 的 `200` 精确等于它实测的 **P50**(其 P90 是 280);Tundra 的 `330` 等于它**旧时代**的 **P90**。

- ✅ **可用于跨队比较**(对所有队是同一个平台计算量)
- ⛔ **禁止拿本地实测 P90 去对它做验收** —— 我方本地逐局 P90 在 240-370 波动、并发时到 1320,而榜显 295
- **它是"开测以来累计"量** ⇒ 单批污染阈值是「新增样本 / 累计样本 > 10%」;我方 886 局约 443k 样本 ⇒ 任何 ≤88 局的批次对它无害
- ⇒ 验证只能靠 **publish 后重读 `user_cost1`**,且需数百局才显影

### 7.4 免费通道:被动局

**别人挑战我方的局不占配额**,而它们跑的是我方**公开位**。
⇒ 这是唯一免费、且**最接近初赛条件**(我方公开位 vs 对手当期代码)的样本源。
`sim/passive_monitor.py` 每日零配额聚合(cron `f5f68b3c`),按对手汇总并给 bootstrap 区间。

---

## 8. 数据资产

| 资产 | 内容 |
|---|---|
| `logs/opponents/manifest.json` + 各队目录 | 归档对局(软链接),取路径:`python3 sim/probe/archive_logs.py paths --team '<队名>'` |
| `/share/data.tar.gz`(开发机) | `data/full/` 上帝视角日志,无迷雾,含 7 NPC 全部动作与拾取 |
| `sim/GENERATION.md` | 金币生成机制实测(公测期,含换赛段重标定方法) |

### 8.1 ⭐ 日志里哪些通道可信

| 通道 | 完整度 |
|---|---|
| **per-unit `gold`(持金)** | ⭐ **100%** —— 逐轮差分无偏,可复现终局净分差 |
| **双方逐轮 `cost`** | ⭐ **100%** —— 可零配额重建任何对手的延迟分布 |
| `position` / `actions` / `pickup` | ⚠️ 仅 **33-39%**(被迷雾截断) |

⇒ **想做无偏的对手分析,优先用 `gold` 与 `cost` 两个通道。**

### 8.2 ⚠️ `cost` 字段有陈旧副本

`start[r].cost == end[r-1].cost`(499/499)⇒ **当轮真实延迟只能读 `end.players[i].cost`**。
且 **r0-r3 是预热**(2160 → 490ns),统计须剔除。

### 8.3 ⚠️ 视角偏倚

默认 5×5 视野下,**56% 的轮次两个对手单位都不可见**。买满 `vp=2`(9×9)后双单位并集上限约 **162/289 ≈ 56%**。
⇒ **历史对局日志对"对手在哪"这类问题有选择偏倚**;探针局(见 §9.3)才是无偏空间样本,两者必须分层。

⭐ **一个可用的授权判据**:`bias_ratio = 可见子集均值 / 完整通道均值`。用它之前先算 —— 若显著偏离 1,该统计量不可用于定价。

---

## 9. 主要对手类型与逆向方法

### 9.1 已识别的四种类型(这是分类,不是排名)

| 类型 | 代表 | 特征 |
|---|---|---|
| **A. 快 + 固定策略** | **Tiuntled-1**(model 87478 / `player163`) | P50 **200ns** / P90 **280ns**;固定 3+3 步分配(0/72006 例外);**不买视野**;榜显长期 #1 约 95% |
| | Tundra-wawa(model 43116 / `player57`) | sane 形态 P50 **210** / P90 **270**;不买视野 |
| **B. 双构型(公开位快 + 定制局慢)** | **rikka**(`user_id 47`) | 公开位 `player47` **350-400ns**、固定 3+3;而 `g47v220m1/2/3` 是**专打我方的搜索,263μs–11ms** |
| **C. 慢速大模型/搜索** | 群里提到的 neck 队等 | 榜显 P90 在 μs–ms 级 |
| **D. 构型故障(必须剔除)** | `QuantLK`、部分 `Tundra` 局 | **终局净分为负**,把金全花在视野上(如 `gold 4 / vision 1500 / net −1496`) |

⚠️ **D 类不是罕见事件**:我方观测到 **7 局 / 4 支对手 ≈ 7% 的对局**。
⇒ **任何胜率或收入统计都必须剔除"对手终局净分为负"的局**,否则会虚增。`sim/field_sample.py` 里的 `is_contest()` 是常设过滤器。
⇒ 顺带一个方向:**故障会让对手\*看起来更慢\***(坏构型 P50 4430-5550ns),不会让它看起来更快 ⇒ "某队很快"的读数不会被故障局向下污染。

### 9.2 榜显数字的陷阱

- **胜率是 24 小时滚动窗口;P90 是开测以来累计。** 两者不同步。
- **前 16 门槛不是常数** —— 它是 rank-16 的滚动值,实测 40 分钟内动了三次(67.56 / 67.31 / 67.80)。任何与门槛的比较**必须同址记录门槛值与读取时刻**。
- **榜显 P90 对\*所有队\*都滞后**。判例:Tundra 榜显 330 只匹配它的旧时代;它近期 55 局是 **270**。
- ⭐ **`get_model_list_4` 暴露每队公开位的 `updated_at` 与 `lang`** ⇒ 可据此判断某队公开位的新鲜度(实测 **47.3% 超过 3 天,最旧 21 天**)。

### 9.3 逆向方法(可复用)

**方法一:零配额,从归档重建**(脚本已随战役移出工作树,方法与产物保留)
```bash
git show 0fe9264^:sim/analyze_opponents.py > /tmp/ao.py    # 生成 sim/OPPONENTS.md 的原脚本
git show 0fe9264^:sim/analyze_gold_delta.py > /tmp/ag.py   # gold 通道归因
```
用 per-unit `gold` 逐轮差分 + 逐轮 `cost`,可以在**不花一局**的情况下重建对手的收入率、进账轮频率、延迟分布。

**方法二:观测探针(花配额,但样本无偏)**
```bash
ls sim/probe/            # player.py 是探针策略:固定 vp=2、保持距离跟随对手
python3 sim/probe/archive_logs.py --help
```
探针买满视野并跟随对手 ⇒ **可见率从 44% 提到 85-95%,双敌同时可见从 4.2% 提到 31-60%**。
它已在 map1/map2 跑过 1000 轮零非法。**用于采集空间行为样本。**

**方法三:直接查询对手 `.so`(仅限我方可下载的公开位)**
`sim/analyze_order_sensitivity.py` 里的做法:从冻结的 `RoundStart` 重建对手的 `PlayerInput`,查询它**私有副本**的本轮决策。⇒ **可以逐轮问对手"你会怎么走"。**

**方法四:随机化对照(用于把"叙事"变成"因果")**
不要只测"顺向臂 vs 关闭"。判例:某信号"顺向 − 关闭"看起来 +32,但**反向臂**却 +92 ⇒ 收益来自"行为改变本身"而非"信息"。
⇒ **凡「读某信号 ⇒ 改行为」类候选,必须同时跑\*按信号反向行动\*的臂**;只有 `顺向 − 反向` 才是信息的识别值。

---

## 10. 测量有效性检查表

这些不是策略结论,是**避免把噪声当结果**的机制。按被咬过的次数排序。

| # | 规则 |
|---|---|
| 1 | ⭐ **凡有前提条件的量,前提必须与量\*同址\*返回** —— 构型 hash · n · 误差棒 · 语料 · 加权单位 · 读取时刻。分开写就会被当常数引用 |
| 2 | ⭐ **相减前确认五项相同**:同构型 / 同对手 / 同图 / 同行动顺序 / 同窗口。**算术自洽只证明内部一致,不证明条件可比** |
| 3 | ⭐ **循环赛按\*对手\*加权,不是按\*局\*加权** —— 同一批数据两种加权差 13.7pp(某对手挑战 6 次会被计 6 份权重) |
| 4 | ⭐ **任何裁决器/护栏先用"已知零信号"的合成输入跑一遍,确认它报"无"**,并把干跑记录附在交付里。干跑必须覆盖**选样**这一步,不只是**裁决**这一步 |
| 5 | ⭐ **"越线"声明必须给\*越线余量\*的误差棒**,不只是 margin 的。`+111.3 ± 19.6` 越过 `+108` 是 `+3.3 ± 19.6 = 0.17σ` |
| 6 | **教科书二项方差在 `p = 1` 处给零方差** ⇒ 用 Jeffreys 或 bootstrap |
| 7 | ⭐ **开环上界会被"存量 vs 流量"灌水** —— 金币是**存量**不是流量(跳过的格保留 35% 并继续追加),判例中 85-92% 是时序重复计数 |
| 8 | ⭐ **看 `margin`(双方净分之差)而不是 `net`** —— 共享棋盘上我方净分涨可能只是对手涨得更多。三分类:**联合移动**(我涨对手跌)/ **把饼做小**(双跌)/ **让地**(双涨但对手更多) |
| 9 | **"余量/上界/最大值"若\*恰好等于\*测量窗口的边界,先假定它是删失产物**,去界重测。判例:某 max 恰为 24(= 窗口),去界后 p95 = 339 |
| 10 | ⭐ **假否证与假确认同等危险,解药都是"先核对控制在比什么"**。判例:一次 `pair_diff` 读到 41/36/36 看似否证等价性,实为控制里缺了两个 hunk;补齐后 0/500 —— 若当时接受,一个正确方案会被判死 |
| 11 | **门只应检验\*主测量未捕获\*的代价**。检验一个已包含在主测量里的量会\*错误否决\* |
| 12 | ⭐ **凡「知道规则就能避免」的防线一律视为无效防线。** 有效形式只有三种:**必印列**(判据与数同址返回)· **单变量干预**(而非论证)· **随机化/反向对照**(而非一致观察) |

---

## 11. 现役构型的事实(不含评价)

`src/player.cpp`,**构型 R14(commit `862ee58`,2026-08-13;单构型)**。
算法流程 = 其头注释;各器官的实测价值与交付台账 = `src/CHANGELOG.md` 对应节。

| 行为参数 | 值 | 回源 |
|---|---|---|
| 地图知识 | **无指纹、无烘焙墙表** —— 所有图按陌生图处理,在线学墙,`round>=8` 且两锚点确认后**永久退场** | 头注释 + `grep -c BAKED` = 0 |
| 视野 | 仅第 0 轮买一次 9×9,其余 `vp=0` | `learnVisibleWalls` 尾部 |
| 选路 | 每单位每轮 48 条候选(L32/S12/O4),面值分档三轮 cmov 收敛,平手先「不背离锚点」 | `PathT` + `decide` |
| 阻挡 | 墙/弹/雾/越界 + 单玩家槽(敌优先) + **已聚 3 个 NPC 的格**(踩踏罚金) | `bd` 构建段 |
| NPC 判定 | `vpconflictd` 同格计数(每轮一次,阈值 3),玩家阻挡者注入 permute 填充槽 lane 7 —— **零新增分支** | AVX-512 块注释 |
| 步数/order | 固定 3+3;order = 持金多者先动 | `decide` 尾部 |
| 入口对齐 | 死垫锁 `mod64 = 0x10`(垫长与体积耦合,改后必查 `nm`) | 文件尾 `asm(".space …")` |

它读 `GameInput` 十字段中的**七个**:`round`·`grid`·`my_units`·`my_units_gold`·`visible_enemies`·`num_visible_npcs`·`visible_npcs`。
`gold_opp`·`snapshot_valid`·`snapshot` 未读取。

**当期平台读数(2026-08-13 多窗交错,gid 见 CHANGELOG 各节)**:P50 **130-140** · 对 T-1 配对 ΔP50 **−10~−30** ·
先手 54-71% · 踩踏 −43%(6.3→3.4-4.2/局)。⚠️ 绝对值不可跨窗引用;T-1 已于 08-13T08:47Z 换代(变慢),对它的定价按被动局通道持续更新。

## 12. 交接中的制品

> ⛔ **本节三份制品均已\*不再适用于现役构型\***(2026-08-12 更正)。
> 它们全部是 `fd47ea6` 时代 **LUT 三步导向**底盘上的补丁,而该底盘已在 `sweep-planner` 换代
> (48 条候选路径枚举)中整块退场 —— HEAD 里 `grep -c SLut src/player.cpp` = **0**。
> ⇒ **不得直接施用**;若要复活其中的*机制*,须在新底盘上重新实现并重新过门。

`sim/reports/` **已不在工作树**(`.gitignore` + `0fe9264` 精简),取用走 git 历史:

```bash
git show 0fe9264^:sim/reports/narrow_table_equivalent.diff
```

| 文件 | 当时状态(条件域 = `fd47ea6` 底盘) |
|---|---|
| `narrow_table_equivalent.diff` | 已过三门(`pair_diff` 0/500×三图 + 49/49 LUT 格 · `.rodata` 不变 · 指令低于基线);**符号取决于优化哪个统计量**,详见文件头 |
| `tail_width_wave.diff` | 已验证、未落库;`pair_diff` 0/500,ΔP50 = 0 |
| `r1_table_reentry.diff` | 已判负,含一个真实缺陷的说明(尾段检查读了错单位的位图) |

**当前状态**(2026-08-13):`src/player.cpp` = **R14**(`862ee58`),公开位与之同源
(publish `2026-08-13T07:56:21Z`,`.text` sha 对账见 CHANGELOG 交付台账)。
读「平台上跑的是哪版」永远以**交付台账时间线**为准 —— publish 是单向通道,发上去读不回来(§7.2)。

---

## 13. 如果你只想记住三件事

1. ⭐ **模拟器测收入,平台内窗交错测延迟,两者不可互相替代。** 模拟器不建模延迟;平台跨窗不可比。
2. ⭐ **`gold` 与 `cost` 两个通道 100% 完整** ⇒ 很多对手分析可以零配额做完,不必花局。
3. ⭐ **每个数字带着它的前提一起走。** 这个项目里绝大多数返工不是因为测错了,是因为**把一个有条件的数当成了常数**。

祝顺利。有任何工具用不起来,`AGENT.md` 里有更细的操作记录,`src/CHANGELOG.md` 里有全部历史(包含我们的结论,建议你形成自己的判断之后再读)。
