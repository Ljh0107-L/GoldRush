// idle.cpp — 公开位诱饵：全程原地不动。
//
// 目的（所有者 2026-08-13 指示）：初赛前把公开位换成诱饵，防止被动局被用来
// 观察/蒸馏现役策略（R14）。被动局跑的是公开位，任何人都能免配额约打我方。
//
// 合法性：每轮输出六个 STAY、k=3、order=0、vp=0 —— 全部在合法域内；不读输入,
// 无分支无循环，不存在超时与异常路径。net 恒为 0（STAY 不结算金币、不买视野）。
//
// 初赛不从公开位收代码（所有者 2026-08-13 确认）⇒ 公开位与初赛脱钩,
// 本诱饵可挂到底,无需换回。真武器 = src/player.so（R14, sha 3e9d3b06…）,
// 走初赛专用提交入口,冻结前由所有者提交。
#include "game_api.h"

extern "C" GameOutput moveDecision(const GameInput* input) {
    (void)input;
    return {{4, 4, 4, 4, 4, 4}, 3, 0, 0};
}
