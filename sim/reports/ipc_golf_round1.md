# IPC 高尔夫第一刀：−84 指令换回 −5.6 cycles，并推翻「指令头寸=延迟头寸」

> 2026-08-10。零平台配额。落库构型通过全部六门。
> 交付产物 SHA256 `c7a1fd5ff45ec0f13cc904319ffd24440cc705518b2ae5508d9b4235e5bfd4cc`
> （`quant-compiler` / EPYC 9T25 / GCC 14.3.1 / znver5 / `-march=native`）。

## 0. 结论先行

**这一刀成了，但它同时证明整条高尔夫路线被高估了约 5 倍。**

| 项 | 值 |
|---|---|
| 指令数 | map1/2/3 **883.4→798.0 / 976.5→892.5 / 1008.3→924.1**，即 **−84.0 ~ −85.4/调用** |
| cycles | **−5.0 ~ −6.2/调用**（n=8 配对交错，5.7-9.0σ） |
| 折算 | **−1.9 ~ −2.3ns ≈ +20 ~ +25 金/局** |
| 行为 | 三图 `pair_diff` **0/500** |
| 布局 | `moveDecision` 入口 `0x1990`、mod64 **0x10**（与基线逐位相同档） |
| 体积 | `size` text 8014→7750（−264B）；`.text` 5363→5091（−272B） |

**但**：84 条指令只换回 5.6 cycles，即 **0.025ns/条**，比 `INFRA §1` 的 **0.1454ns/条低 6 倍**。
按 0.1454 线性外推本该拿到 −12.2ns ≈ +134 金，实际只有 +23 金。

## 1. 最重要的发现：0.1454ns/条是均价，不是边际价

`INFRA §1` 的 `0.1454ns/指令` 是**整函数平均价**（用总指令数除总耗时反推）。它对
「新增一整个器官」这类改动大致可用，但**不能用来给「删掉若干条现存指令」定价**。

实测证据：

| 构型 | Δ指令 | Δcycles | 判决 |
|---|---:|---:|---|
| golf1 弹片写融进载入循环 | **−42** | **+15 ~ +18** | **判负**（−60 ~ −74 金） |
| golf2 只拆 pack/unpack、保留两趟结构 | **−84** | **−5.0 ~ −6.2** | **采用**（+20 ~ +25 金） |
| golf3 只把位移改无分支 | −9 ~ −11 | −0.4 ~ −3.7 | 已被 golf2 包含 |

`golf1` 是决定性反例：它**通过了「指令数下降」这道门**（−42 条），却**慢了 15-18 cycles**。
如果只按 Master 预注册的门 2（指令数下降）验收，它会被判为成功并落库，实际净损约 −67 金/局。

根因：本函数 **IPC ≈ 4.14**（883 指令 / 213 cycles），瓶颈是依赖链与访存并行度，不是指令吞吐。
`golf1` 把 `bombbit` 的读-改-写塞进 AVX 载入循环，破坏了「5 次向量载入连续发射」的
访存并行度：原版两趟结构让 5 个 `vmovdqu` 背靠背发射、之后再做 5 次标量 RMW；融合后变成
载入→掩码→RMW→载入的串行链。**省下的指令本来就在载入的阴影里免费执行。**

这正是 `INFRA §2.4` 已经记录但未被贯彻的信号：scan 占 43.6% 指令却占 49.4% cycles。
当时的结论是「略偏 cycle 重但不是 DRAM 受限 → 方向是省指令」，**这个推论是错的**：
偏 cycle 重恰恰说明存在空闲发射槽，删掉占用空闲槽的指令不会变快。

## 2. 高尔夫路线必须重新定价

`INFRA §2.4/§2.6` 的头寸「90-160 条 = 13.1-23.3ns = 120-200 金」是**按均价线性外推**得出的，
现已被实测否证。按本刀实测的边际价（0.025ns/条）重估：

```
90-160 条 × 0.025ns/条 ≈ 2.3-4.0ns ≈ 25-44 金/局
```

即 **高尔夫的真实头寸约 25-45 金，不是 120-200 金**，被高估约 4-5 倍。

必须强调这是**上界性质的重估而非定论**：边际价随所删指令是否在关键路径上而变。若后续能
找到真正位于依赖链上的指令（而非填充空闲槽的），边际价会更接近甚至超过均价。但**默认假设
必须反过来**：先证明目标指令在关键路径上，再谈收益。

对战场的影响（对手缺口见 `CHANGELOG` 现役档案）：

| 战场 | 缺口 | +23 金覆盖 | 旧口径 +120-200 金曾承诺 |
|---|---:|---:|---|
| T-1 map3 | −102.9 ± 63.5 (n=12) | 22% | 「够翻」 |
| T-1 map2 | −164.6 ± 61.6 (n=12) | 14% | 「边缘」 |
| T-1 map1 | −274.3 (n=6) | 8% | 翻不了 |
| Tundra map1 | −219.2 (n=6) | 10% | 翻不了 |

**结论：高尔夫不再是「唯一剩余正期望路线」级别的头寸，它是一条 25-45 金的小额稳赚路线。**

## 3. 这一刀改了什么

原扫描把 5 行的炸弹位片打包进单个 15 位标量 `bombm`（`<<(i*5)` + `|=`），第二趟循环再拆开
（`>>(i*5)` + `&31`）并用 `shl>=0 ? bsl<<shl : bsl>>-shl` 的 cmov 做位移。本刀：

1. **取消 pack/unpack**：第一趟直接把每行的 5 位片存进 `bslv[5]`（展开后驻寄存器），
   第二趟按行取用。**保留两趟结构**，载入并行度不变。
2. **位移改无分支**：`(uint64_t)bsl << sc >> 1`。等价性：`sc∈[0,16]` ⇒ `shl=sc-1∈[-1,15]`，
   `sc>=1` 时得 `bsl<<(sc-1)` ✓，`sc==0` 时得 `bsl>>1` ✓；`bsl<=31`、`sc<=16` 故无截断。
3. **布局归一化死垫**：改写使 `decide` 缩小，入口 mod64 从已证最优档 `0x10` 掉到 `0x20`
   （四档扫描已证 +11.67ns）。补 48B 永不执行的 `nop` 把入口移回 `0x1990`/`0x10`。
   已验证：pad 区 `[0x1960,0x1990)` **无任何分支目标**（全 .so 反汇编穷举），provably unreachable。

> ⚠ **维护负担**：死垫长度与 `decide` 的体积耦合。**任何后续改动 `decide` 的刀都必须重新核对
> `moveDecision` 的 mod64 并调整垫长**，否则会白付 11.67ns 布局税。

## 4. 六门验收

| 门 | 要求 | 实测 | 结果 |
|---|---|---|---|
| 1 | 三图 `pair_diff` 0/500 | map1/2/3 均 0/500 | ✅ |
| 2 | 动态指令数下降 | −84.0 / −84.0 / −85.2（500k calls × 3 reps，6 位小数复现） | ✅ |
| 3 | mod64 保持 `0x10` | `0x1990` → `0x10`（与基线同址） | ✅ 已用死垫归一并显式报告 |
| 4 | FP16 越界指令 = 0 | 赛事机 `-march=native` 构建，`objdump` 计数 0 | ✅ |
| 5 | 本地 hot Δ | **cycles −5.0~−6.2（5.7-9.0σ）**；墙钟 10ns 台阶分辨不出，故以 perf cycles 为准 | ✅ |
| 6 | `.text` 变化 | text 8014→7750（−264B）；`.text` 5363→5091（−272B） | ✅ 已报告 |

## 5. 方法学：门 2 是错的门，应换成 cycles

Master 预注册的门 2 是「动态指令数下降」。`golf1` 证明该门**会放过净负构型**。建议改为：

> **门 2′：perf `cycles/调用` 在同窗配对交错下下降，且达到 ≥2σ。指令数只作辅助记录。**

配套工具已入库 `tests/icount.cpp`（`instructions|cycles` 双计数器）+ `tests/trivial_player.cpp`
（harness 开销标定物）。用法与口径写在文件头。要点：

- harness 开销由 trivial 标定：`raw(trivial) − 静态体指令数(objdump)` = **10.016 条/调用**，
  净值 = `raw − 10.016`。逐 rep 复现到小数第 6 位。
- **必须同窗配对交错**：开发机 115 用户、load 5-10，跨窗绝对值可漂 50%（同一构型同一输入
  实测出现过 213 cy 与 326 cy 两个窗）。本报告全部 Δ 均为交错测得。
- 一次 n=6 的 map2 复测得 −20.4±14.2（1.4σ），明显被负载污染；以 n=8 的 −4.98±0.56（9.0σ）为准。
  **报告两者，不取其一。**

## 6. 顺带修正的计量事实

**`894.216 条/调用` 这个锚点是输入集特定的，不是构型常数。** 同一 `f18064c`、同一编译产物，
换输入集实测 raw 在 **881-1110 条**之间；本刀采用的三图标准输入集净值为
**883.4 / 976.5 / 1008.3**（map1/2/3），跨图相差 14%。`game_140521.bin` 得 raw 902.75，
与档案记录的 903.23 相差 0.5，故旧锚可复现——但它约等于「某个 map1 输入集」的值。
**结论：指令数必须连输入集一起报，跨输入集比较无意义；A/B 必须同输入集。**

另：`.text 8014B` 与 `.text 5363B` 两个档案数字都对，只是口径不同——`size` 的 `text` 列
（含 `.rodata` 等只读段，8014）vs `.text` 节本身（5363）。以后写明用哪个。

## 7. 复现

```sh
# 开发机
rsync -a src/ Ubiquant220@8.153.76.120:~/goldrush/src/
rsync -a tests/ Ubiquant220@8.153.76.120:~/goldrush/tests/
ssh Ubiquant220@8.153.76.120 'cd ~/goldrush && \
  g++ -std=c++17 -O3 -march=native -fPIC -Wall -Wextra -shared -o /tmp/cand.so src/player.cpp && \
  g++ -std=c++17 -O2 -o /tmp/icount tests/icount.cpp -ldl && \
  python3 tests/pair_diff.py /tmp/base.so /tmp/cand.so logs/game_179643.log logs/game_179631.log logs/game_179637.log && \
  taskset -c 6 /tmp/icount /tmp/cand.so logs/game_179643.bin 300000 1 cycles'
```
标准输入集：`game_179643`(map1, 40 墙) / `game_179631`(map2, 24 墙) / `game_179637`(map3, 78 墙)，
均为 `frTu*` 族 500 轮局、我方为 player1，`tests/dump_inputs.py` 生成 `.bin`。

## 8. 待办与建议

1. **平台验收**：+23 金远低于单局噪声（SE ±60-110），**平台局无法测出该收益**。建议只花
   **6 局**做「布局税回归检查」——看 P50 是否意外 +10/+20ns（`noseparate` 案的 +11.67ns 就是
   这样出现的）。不要试图用平台局证明 +23 金。
2. **后续高尔夫必须先定位关键路径**。建议用 `perf record -e cycles:pp` 找真正的停顿点，
   而不是继续按指令占比选目标。scan 剩余的边界钳位/`bestrow` 查表链大概率也在空闲槽里。
3. 若要真正拿下 map1（−219/−274），**延迟路线的头寸不够**，需要回到命中率/站位（见
   `path_harvest_verdict.md` §7 留档的无偏结构事实）。

---

# 附录 A：平台验收（8 局，成对换座）与一次自造的假警报

## A.1 结果

map1 自博弈，基线 `f18064c`(gpA*) 与候选(gpB*) 同局对打。**前 4 局候选固定坐 p2，后 4 局换座坐 p1。**

| game | 候选槽位 | base P50 | cand P50 | base P90 | cand P90 |
|---|---|---:|---:|---:|---:|
| 183549 | p2 | 210 | 210 | 290 | 320 |
| 183552 | p2 | 200 | 220 | 300 | 330 |
| 183555 | p2 | 210 | 210 | 330 | 330 |
| 183558 | p2 | 210 | 200 | 300 | 320 |
| 183795 | p1 | 200 | 190 | 290 | 280 |
| 183802 | p1 | 200 | 190 | 310 | 290 |
| 183803 | p1 | 210 | 230 | 350 | 330 |
| 183804 | p1 | 210 | 200 | 320 | 300 |

| 口径 | ΔP50 | ΔP90 |
|---|---|---|
| 候选坐 p2（n=4） | +2.5 | **+20.0** |
| 候选坐 p1（n=4） | −2.5 | **−17.5** |
| **合并 n=8** | **+0.00 ± 4.63 ns（0.00σ）** | **+1.25 ± 7.89 ns（0.16σ）** |

**判决：无布局税回归，48B 死垫有效。** 若死垫失效，应见约 +11.67ns 的系统性偏移；实测 ΔP50 为
零。本地 −2.1ns 的改善在平台**不可见**（10ns 量化 + ±4.6ns SE），这是投局前就预测到的结果。

## A.2 我自己造的假警报（方法学教训，已入军规 32）

第一批 4 局我把候选固定放在 p2，得到 **ΔP90 +20ns、2.83σ** —— 按常规判据这足以判「候选有 P90
回归」并触发回滚。但该设计把**构型与 player slot 完全混淆**。换座后 4 局给出 **−17.5ns，符号
几乎对称翻转**，证明那 +20ns 是**槽位效应，不是构型效应**。

- 槽位效应量级：P90 上约 **±20ns**，P50 上约 ±2.5ns。
- 档案里的「同种子换座」原本只用于**收入类** A/B；本案证明**延迟读数同样必须换座**。
- 若我按原计划只花 6 局单臂（甚至只花这 4 局），就会上报一个不存在的回归。

## A.3 配额

授权 8 局，实用 8 局（4 单座 + 4 换座）。**未超授权。** 首批 4 局本想只用 4 局结案，
发现混淆后把剩余 4 局全部用于换座对照——这是本轮最有价值的 4 局。

---

# 附录 B：高尔夫第二轮 —— 先量后建，结论是收线

按「不达 ≥5 cycles 门槛即不实现」执行。`perf record -e cycles:u`（IBS 与 `cycles:pp` 在本机
不可用：`ibs_op` 需提权、`cycles:pp` 返回 EINVAL，AMD 无 PEBS；故存在 1-3 条指令的 skid，
只作区域级判读）。3289 样本，94.98% 落在 `moveDecision`，函数内 600 条指令占比合计 99.8%。

## B.1 剖面：瓶颈是单一序列化点，不是访存

| 类别 | 占 `moveDecision` cycles |
|---|---:|
| mov/lea | 25.60% |
| **cmov** | **18.71%** |
| 标量 bitops | 16.76% |
| AVX cmp/mask | 13.56% |
| branch/cmp | 6.05% |
| LUT/byte load | 5.74% |
| **AVX 载入** | **1.52%** |

最热单条指令：**一条 `cmovbe` 占 15.75%**（约 32 cycles）。它是「5 行 rowsel → 3 层 min 树」的
**归约尾**，其下游是 `blind` 判定、选靶、整条路由。**AVX 载入只占 1.52% ⇒ 访存不是瓶颈**，
61% 的 cycles 花在标量 mov/lea/cmov/bitops 上，且高度集中在这一个串行点。

（注：不要按未对齐地址反汇编去读这条指令的上下文——x86 变长指令，`--start-address` 落在指令
中间会产出完全虚假的反汇编。本报告只采用函数整体反汇编的结果。）

## B.2 候选清单与预期节省

| 候选 | 预期 cycles | 是否在空闲槽 | 判定 |
|---|---:|---|---|
| 双单位扫描/路由交错（最大鱼） | 15-25 | 否，真在关键路径 | **等价重构下非法**：u0 的 `g_s.bombbit` 写被 u1 的路由读取，重排即改行为，`pair_diff` 必破 |
| min 树降深度 | ~0 | — | 5 元素深度下限已是 3，现役即最优 |
| `blind` 判定折进 min 树 | ~1 | 否 | 低于门槛 |
| `-mprefer-vector-width=256`（消 11 条 zmm） | 实测 | — | **判负**：map1 `−5.77±1.68` vs 落库 `−4.91±0.83` 无差；map3 `−1.58±6.32` 反而更差。且 zmm 本就存在于基线，非本刀引入 |
| `rclv` 行钳位 LUT → cmov min/max | ≤1，可能为负 | 是 | 低于门槛 |
| `bestrow` 改 vpgather | 可能为负 | — | Zen gather 慢；且重演 golf1「拿 ILP 换指令数」的失败模式 |

## B.3 收线判决

**无任何候选的预期节省 ≥5 cycles，故高尔夫收线，不再实现。**

根因是结构性的：那条 15.75% 的串行链是「扫 5 行 → 归约出唯一目标 → 按目标算路由」这个**算法
本身**，不是实现冗余。要缩短它必须改算法（例如让路由不依赖单一归约结果），那是重写而非高尔夫，
且会立刻破 `pair_diff`。

配合边际价重估（0.025ns/条），整条延迟路线剩余空间是个位数金量级，**在 −104 ~ −274 的缺口面前
期望值不足**。建议把工程量投向命中率/站位（已由并行 Worker 承接）。
