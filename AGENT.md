# AGENT.md — GoldRush 2.0 作战手册（2026-08-09 扁平化版）

> 给接手本赛题的 AI Agent。人类已把赛题全权委托给 Agent。
> **每日 500 局配额：探测自由，动用须所有者批准。** 规程 `docs/QUOTA.md`。
> 三份核心文档：本手册（怎么干活）· `src/INFRA.md`（平台成本模型，设计算法前必读）·
> `src/CHANGELOG.md`（版本史与负结果军规）。历史逐日战报在 git 历史。

## ⏸️ 待所有者裁决:**一个已过三门的制品,两个口径下符号相反**(2026-08-10 夜)

> ⛔ **它被拒的理由*不是*"某个阈值超了" —— 是**失败于支配性**:
> 它的符号*恰好*取决于我们在优化哪个目标。技术判据已经用尽,剩下的是*目标选择*,
> 而目标选择属于所有者。⇒ 执行者的正确动作是:**保全制品、量化两侧、把选择摆出来**,不是替所有者选一个。**
> ⚠️ 写"阈值超了"会让人以为**调宽一点就能过**;写"失败于支配性"才准确说明它需要的是**一个决定**。

**制品**:`sim/reports/narrow_table_equivalent.diff` · `.text` sha256 `da9d960053d8a1fa2330fa85eeb6b503…`
(施用于 `fd47ea6` 行为 + 注释更正的提交状态;垫 80 ⇒ 入口 `0x1a10`、`mod64 = 0x10`;FP16 0)

### ✅ 已过的三道零配额门
| 门 | 结果 |
|---|---|
| **等价性** | `pair_diff` vs 已测构型 **0/500 × 三图** + **49/49 LUT 格穷举逐格相同**(定义域论证) |
| **足迹** | **`.rodata` = 1360,与 `fd47ea6` 逐字节相同** ⇒ 导致上一次回滚的那个已实测机制被消除 |
| **指令** | **799.802 < 同流基线 802.804** |

**收入 `+111.3 ± 16.0`**(门 32.1、OOS +79.8、joint move)—— **由*逐位相同*继承,不引入新测量误差。**

### ⚖️ 而平台内窗交错对照(12 局、逐局串行、每臂 992 干净轮)给出**相反的两侧**
| 侧 | 量 | 后果 |
|---|---|---|
| ⛔ **胜率** | **ΔP50 = +16.7ns**(逐图 +20 / +0 / +30) | 对 **T-1**:λ 15.9-22.6% ⇒ **净 −33 ~ −94**;**对全场 ≈ 0**(λ≈0、f 中位 0.994、0/39 对手更快) |
| ✅ **极速奖** | **ΔP90 = −36.7ns**;**尾宽 143 → 90 = −53ns** | **我方 290 → 约 253**,而对手当前 **270** ⇒ **由落后 20ns 变为领先约 17ns** |

⭐ **尾部线的目标是 −20ns,它做到 −36.7ns(1.8 倍),且*不是搬家*:P90 绝对值真降了。**
💡 **那个投射是被许可的操作**:`docs/LATENCY_VALIDATION` 已标定**小 Δ 精确可迁移**(27 ticks = 10.00ns ↔ 平台 −10ns)
而**绝对值不可迁移** ⇒ 故「把 Δ 加到已记录的绝对值上」合法,而「直接比较两窗的绝对值」不合法
(本对照 base 的 P90 均值是 **340**,与记录的 290 不同,这是**预期的**、不是矛盾)。

### ⇒ 两个选项,各一行
- **选 A(保胜率,= 现状)**:**不发**。现役 `fd47ea6` 不动。放弃 ΔP90 −36.7ns,保住对 T-1 的 P50。
- **选 B(换极速奖)**:**发它**。接受对 T-1 净 −33~−94,换 **P90 由落后 20ns 变领先 17ns**,而**对全场胜率影响 ≈ 0**。

⚠️ **选 B 的前置**:极速奖 = 前 16 内 P90 最低(排除前三);我方晋级概率已测 **99.6%**,而对全场 λ≈0
⇒ **选 B 的胜率代价几乎只落在 T-1 一支身上,而 T-1 是 rank 1、赢它不改变我方是否进前 16。**


## 🔴🔴 延迟门(2026-08-10 夜新增。**这是当晚唯一抓到一次将要发生的自伤的门,读靶心之前先读**)

> ⭐⭐ **凡改动触及*热数据结构尺寸*(表宽 / `State` 布局 / 数组维度 / 函数体积),
> 指令数与 cycles 均*不足以*验收 —— 必须做*平台内窗交错*延迟对照
> (base 与候选*交替提交*、同对手同图、三图),ΔP50 与 ΔP90 都要报。**

**判例「−42 指令 / +113ns」**:一个改动**减少 42.07 条指令**、过了全部七道门
(`pair_diff` 0/500 × 三图 · `mod64==0x10` · FP16=0 · 同流指令数低于基线 ·
模拟器 n=150 双场 +OOS 收入 +107.6 · 零信号对照 300/300 逐位相同 · 11 局平台安全检查零非法零判负),
**而平台内窗对照实测 `P50 +33ns / P90 +113ns`(三图同号)**。
**机制**:`SLW` 3→6 使 `fact/pdr/pdc` 由 441B 涨到 882B(`.rodata` 1360→**1808**),
**该 LUT 每轮都读**,而平台上我方两次决策之间有对手 + 7 个 NPC 执行 ⇒ **我方每轮都是冷的**
⇒ **热数据足迹翻倍 = 每轮多付一次冷缺失。**
**代价**:对 T-1(λ(33ns)≈22.6%)净 **−86 金**,并把 P90 从 290 推到约 400 ⇒ **摧毁极速奖**。

### 为什么每一道门都漏了(照抄,勿重犯)
| 门 | 为何漏 |
|---|---|
| 模拟器配对 A/B | **完全不建模延迟** ⇒ 只测收入 |
| **同流指令数** | **降了 42** ⇒ ⭐ **代理量*指向反方向*** |
| `pair_diff` / `mod64` / FP16 | 都不测延迟 |
| 本地 `latency_bench` | 该机散布 ±60ns > 效应 ⇒ 已判不可分辨 |
| 已校准逐出台架 | 当时测的是**基线**,不是这个产物 |

### ⚠️ 而分项足迹门**可被搬运绕过**
续攻候选 R1 把表宽改回 3(`.rodata` **1360 不变**,门"通过")—— 但 `.text` 由 5267 涨到 **5731(+464)**,
**与 `nc` 的 `.rodata` +448 同一量级**,本地逐出实测 **ΔP50 = +27 ticks 三图一致**。
> ⭐ **⇒ 门必须是「热**总**足迹(`.text` + 热 `.rodata`)不得显著增长」;
> 分开看任一项都会被同一成本换个容器绕过。**

> ⭐⭐ **两条判例的共同形状:*我们设的门总是测上一次事故的那个量*。**
> 「−42 指令/+113ns」抓的是"指令数不是成本";R1 抓的是"分项足迹门可被搬运绕过"。

### ⚠️ 仪器**指向了错误的条件**:`--mode hot` 不能给冷路径回归定界

当晚另一条线的 `latency_bench --mode hot` 把 base / 候选 / 叠加**全部**读成 P50 110–120ns、P90 散布 ±60ns
⇒ 它如实报告「不可分辨」。**该读数*本身正确*,却完全无法保护我们。**

> ⭐⭐ **`--mode hot` 刻意*不*做逐出 ⇒ 它测的是*温*态;
> 而平台真实条件是「我方两次决策之间有对手 + 7 个 NPC 执行」⇒ **我方每轮都是冷的**。
> ⇒ **一个"热"的本地读数*不能*给冷路径回归定界。**

⇒ **推论(可操作的那一半):`.rodata`(以及 `.text`)的*尺寸增长本身*是一等成本信号,
不是指令数的脚注。** 判例:`.rodata` 1360→1808 是**唯一**在事前就可见的信号,
而当时它被当作无关紧要的旁注 —— 而指令数(−42)正指向反方向。

⇒ **本地延迟测量的选择顺序:① 平台内窗交错(唯一权威)· ② 已校准*逐出*台架(可预筛,零配额)·
③ `--mode hot`(只能用于*温态*效应,不得用于足迹/冷路径)。**


### 配套三条(同晚落库,均在 `src/CHANGELOG.md` 有判例)
1. **`pair_diff 0/500` 不是等价性的*证明***,只是「在*已记录输入分布*上无分歧」
   ⇒ 凡称"等价重构"必须*额外*给出**定义域论证**(索引范围/钳位/饱和)。
   判例:`blk[]` 复用可证**正因为**查了 `r∈[−1,17]→blk[0..18]` 而 `blk` 有 19 项;
   `ok0` 复用**不可证**正因为没查钳位(LUT 索引被 `clamp(±3)`,而 `steerStep` 收未钳位值)。
2. **「越线」声明必须对*越线余量本身*给误差棒**,不是只给 margin 的。
   判例:`+111.3 ± 19.6` 对门 `+108` ⇒ 越线余量 `+3.3 ± 19.6` = **0.17σ ⇒ 不是越线**。
3. **热态 vs 逐出态必须声明**:同一路径跨度热态 27 ticks、逐出 135–162(**三倍**),
   且**热态可能完全不显示效应** ⇒ 任何跨路径延迟比较不注明条件即无效。


## ⛔ 交付门（读靶心之前先读这条：今天十次事故的统一诊断）

> **今天推翻的每一个结论，失败模式都不是「算错」，而是「这个数回答的不是我们以为的那个问题」。**
>
> `A>0` 答的是**先手权的价值**，不是采集力 · `36.0%` 答的是**另一个语料** ·
> `2.38×` 答的是**局面难度**，不是顺序脆弱性 · 样本内 oracle 答的是**筛选量**，不是定价量 ·
> `ORT_A` 答的是**开局端点**，不是驻守位 · `55.6% 覆盖` 答的是**静止锚点窗口的并集**，
> 不是整局实际覆盖（实测 **80.2%**）。
> **六次都是「量对、问题错」。**

**唯一能在交付前捕获这类错误的动作是强制书写**：为每一项写一句
**「这一项是在什么条件下测的、与什么比较」**。

> ⛔ **交付门（不是建议）：任何含分解、恒等式或跨组比较的报告，交付前必须为每一项写出该条件说明；
> 缺此项退回。** 与军规 33 的「零信号干跑记录在案」并列 —— 两者都把"应该做"变成**可查的交付物**。

**判例的强度恰在于**：立这道门的人，**在落库后第一个小时内就自己犯了第六次**（覆盖率那次）。
若他对自己执行了这道门，写出来就是「静止锚点窗口的并集」—— **一写就露**。
⇒ 与军规 33 的自证同形：**这类缺陷不以异常形式出现，故无人可豁免，包括立规者。**

### ⛔⛔ 交付偏倚：错不在**计算**，在**交付时选哪个口径**（2026-08-12 新错型）

上面那六例是「量对、问题错」—— 算的是错的东西。**本条是不同的错型：每一项都算对了，选的口径都合法，但\*选择\*系统性偏向让结论更强。**

**同一份报告里的三个面（全部出自我自己交给上游的那一封）**
| 面 | 我交付的 | 最不利口径下的真值 |
|---|---|---|
| ① **跨窗取最有利的窗口** | 用 **08-11** 的 Tundra `P90 190` 去驳 **08-10** 那批的「0/39」 | 必须同窗比：08-10 窗内它是 **220** |
| ② **区间取最有利的端点** | 「需要 **−50ns**」「差 **约 20 倍**」 | 区间是 **−20 ~ −50ns**、**7 ~ 20 倍** |
| ③ **把校正值当普查值** | 「**X = 2.6%**」 | X 是**错分校正值**，不是 39 支的延迟普查 |

> ⭐ **诊断特征（这是本条能被抓住的唯一办法）：三处偏差的方向全部一致，都指向「结论更强」。**
> **而计算错误的偏差方向应当是随机的。** ⇒ ⛔ **若一份报告里多处小偏差方向一致，那不是计算问题，是交付问题。**

> ⛔ **可执行动作（交付前跑一遍）：把结论依赖的每一个数换成\*最不利\*的那个口径重算一次。**
> **若结论翻转 ⇒ 原报告不成立。若不翻转 ⇒ 报最不利那一端。**

**⚠️ 判例的代价不是「赔可信度」，是它会造成一次\*错误的关门\***（这才是本条值钱的地方）：
按最有利端交付的「需要 50ns」被写成**复活条件「出现 50ns 级机制才重开延迟轴」** ⇒ **那会把一个 25ns 级机制错误地挡在门外**，而 **25ns 足以打下 08-10 同窗的 Tundra（只需 −20ns）**。
⇒ ⇒ **「取最有利那端」在这里不是修辞问题，它直接错误地关闭了一条本该开着的门。** 复活阈值已按保守端改为 **≥20ns**。

⭐ **与「对自己有利的数字要主动给出口径禁令」是同一件事的两个环节**：那条管**使用**，本条管**选择**。
⇒ **选择环节更隐蔽，因为它发生在数字被写下\*之前\*** —— 没有任何痕迹可供事后 grep。

### 🧪 军规 33 升级：干跑必须覆盖**选样**，不只是**裁决**

> **零信号干跑必须同时喂给「语料过滤器」和「统计量」。** 只测裁决器、不测选样，等于验了秤没验样品。

**判例（`sim/analyze_margin_sensitivity.py`，2026-08-10）**：过滤器写成
`bool(n) and OPP.match(n) and n != OUR_ACCOUNT`。正则不匹配时返回的是 **`None` 而不是 `False`**，
而 **Python 里 `None == False` 为 `False`** ⇒ 「两侧都是我方」这道自博弈守卫**从未触发**，
我方构型（`terra143`/`taskmpc1`/`unified`…）被整批当成对手：**报出 106 支对手，真实只有 12 支。**

**判例的价值在于污染的方向**：它让样本**变多**（106 ≫ 12），而**「样本比预期大」在直觉上像好消息**
⇒ 这类 bug **不会以异常形式出现**，与本仓库其它装置缺陷同形（探针读未执行镜像 / 单座 A/B /
132 全 404 / 轮内标签置换 null 在零信号下报显著）。同一次干跑随后又抓到第二个：
畸形表头 `None` vs `player47` 会通过 `is_opp(p1) != is_opp(p2)` 这道门。

⇒ **交付要求**：过滤器干跑至少覆盖 **自博弈 / 正常 / 换座 / 双对手 / 双我方 / 测试账号 / 畸形输入** 七向。

### 📐 准入门是可重标定量，不是常数（`+150` 已废）

> **胜率对边际金币的敏感度实测在 40–200 金区间近乎恒定 ≈ 0.055 pp/金**
> （逐局 pooled，n=333；`pp/gold` = 0.0526 / 0.0541 / 0.0621 / 0.0616）。
> ⇒ **没有凸性支持任何阈值门。** `+150` 等价于把 0–150 金那段的价值定为 0，而实测那段与 150 以上**同斜率**。

**近平手带非空（存在性已证）**：逐局 `|margin| ≤ 200` 占 **20.4%**（Wilson95 [16.4, 25.1]）、
`≤ 400` 占 **43.2%**（[38.0, 48.6]）⇒ 小额候选有真实胜率价值，**不得再用固定金额门一律枪毙**。

⚠️ 三条随数走的前提：① 语料是**混合构型**（约 300 个我方构型），只能证「存在」，**不是现役构型的分布**；
② 对手是我们主动挑的、263/333 局打两强，**不是全场**；③ **`margin` 必须是同局双方之差，永不用我方 `net`**
（判例：B2 净 +111 而 margin 仅 +14.5，机制是让地）。
⚠️ **「带内队数」是废弃指标**：队内单局 σ ≈ 250（低构型数对手实测 189–213；archive 汇总 766 是被
**构型异质性**灌水，而那个灌水本身就是「混构型会毁掉该测量」的量化证据）⇒ ±200 带仅 0.8σ 宽，
1 局/队时真实 margin=0 的队只有 **57.6%** 概率被观测进带内 ⇒ 该估计量被噪声严重衰减。
**决定胜负的是逐局 margin，不是队均值**，故反事实计数免疫此问题。
出处 `sim/reports/margin_sensitivity.json`，裁决器 `sim/analyze_margin_sensitivity.py`。

#### 两条门的分工（正交，不竞争）
| | 是什么 | 角色 |
|---|---|---|
| `max(2SE, \|标定场−均匀场\|)` | **显著性/可测性**：这个效应是真的吗 | ⭐ **唯一准入判据** |
| **pp/金 = 0.0511** | **价值**：它值多少胜率 | **战略记账，不参与准入** |

**因为价值线性过原点，价值尺度不产生任何阈值。** 一个 +5 金候选也值 +0.26pp；它不能收下**不是因为价值太小，是因为测不出来**。⇒ 准入完全由测量精度决定；pp/金 只回答另一个问题：「累计够不够补 17.6pp」。⇒ 不会出现「过了 2SE 但价值不明」（过了就自动有价值），也不会出现反向（价值尺度不设门）。

#### flip 计数 vs 密度法：谁是被估量
**对一个恰好值 +δ margin 的候选，`flips(δ)/n` 是*精确*的胜率增益，不是近似。** 密度法是同一量的**低方差替代品**（带一点带宽偏倚）。
⇒ 正确措辞：**以密度法作低方差估计，但候选的真实兑现值仍是 flip 计数；两者之差即带宽偏倚，实测 6%。**
**保守性证明**：flip/密度 = **1.06 > 1** ⇒ 刚输的局比刚赢的局更密（±200 内 **23 vs 11**）⇒ **我们有一队「差一点就赢」在排队等着被兑换**，而密度法因此**低估**正 margin 候选约 6%，**不会高估**。

#### ⭐ 斜率对「每对打几局」免疫（一个不存在的依赖，别去问赛事方）
胜率**不是**「N 局平均 margin 的符号」，而是**「每局符号的平均」**：`E[胜率] = P(margin>0) = Φ(μ/σ_single)`，**`N` 不出现在期望里**。因为 `sign()` 是**逐局**施加的非线性算子，而 `mean(sign(x)) ≠ sign(mean(x))`。⇒ `dP/dμ = φ(μ/σ_single)/σ_single` **对 N 完全不变**；N 只买**估计精度**，不改变胜率本身。

**模拟实证**（30 队、σ=766、+100 金冲击）：
| N | (a) 按**局**计胜率 → dP | (b) 按**对**计（N 局多数） → dP |
|---|---|---|
| 1 | 3.53pp | 3.01pp |
| 3 | 3.34pp | 4.46pp |
| 9 | 3.74pp | 4.81pp |
| 25 | 4.00pp | **6.89pp** |

⇒ 规则 (a) 无趋势；规则 (b) 单调涨 2.3×。**平台报的是 wins/games**（我们用局数复现过榜显值 0.2320 vs 0.2314）⇒ **规则 (a) 成立，`1/√N` 折扣不存在**，30 队 × 1 局/队 的设计是**无条件**正确的，而非"在 N=1 假设下正确"。
⚠️ **真正的推论**：`σ_single` 是唯一进入斜率的赛制相关量，它由**局内随机性**（金币生成 / NPC / 先后手）决定。⇒ 正赛若换图或改 NPC 数使 `σ_single` 变化，斜率按 `1/σ` 变 —— **那才是要重标定的东西，不是每对局数。**

⛔ **一条我自己撤回的「交叉验证」（判例）**：我曾报 `σ_eff = 0.3989/f(0) = 781` 与独立测得的队内 SD `766` 相差 1.9%，称为两条独立路径互证。**作废。** ① 该公式要求 `f(0)` 是**峰值**密度（中心在 0），而实测分布重度负偏 ⇒ `f(0)` 是**肩部**密度；实算 `μ=−600, σ=766` 时该式给 1041，**高估 σ 达 36%**。② `766` 正是我自己判定为被**构型异质性灌水**的那个数（低构型数对手只有 189–213）。⇒ **两个受同源偏倚污染的量数值接近，不构成验证。** 归入「闭合得很漂亮的分解恰是最容易过审的错误形态」。

⚠️ **`σ` 收窄不是单向利好**：`φ(μ/σ)/σ` 在 **`σ = |μ|`** 处取极大 ⇒ **σ 收窄只对 `|μ_i| < 约 300` 的队抬高斜率，对远离 0 的队压低它**（实算 `|μ|=400` 时 σ 从 766 降到 200 使斜率 ×0.59；`|μ|=600` 时 ×0.06）。⇒ 斜率与 margin 分布**必须联合解读**，不得只取有利的一半。

📌 **新通则：纠正一个错误结论时，保留它成立的条件域，而不是删掉它。** 判例：我那条 `1/√N` 折扣在「每对按 N 局多数决胜」规则下**是对的**，`sim/verify_winrate_invariance.py` 因此同时模拟两种规则并把错的那版留在模块里 —— 只留正确结论，防不住下一个人在别的赛制下错误外推免疫性。

### ⛔ 延迟：`user_cost1` 的用法禁令 + 极速奖的真实约束

**`user_cost1`（榜显"P90"）的聚合方式未知。** 两个锚点互相矛盾：**T-1 的 200 精确等于它实测 P50**（其 P90 是 280）；**Tundra 的 330 等于它旧时代的逐局 P90 中位**。⇒ 无法命名它是哪个分位数。

| 用途 | 允许？ | 理由 |
|---|---|---|
| **跨队比较**（谁更低） | ✅ | 对所有队是同一个平台计算量 |
| **拿本地实测 P90 对它做验收** | ⛔ **禁止** | 我方本地逐局 P90 在 **240–370** 波动、并发时到 **1320**，而榜显 295 —— 任何「本地 P90 vs 榜显值」的门都会给错答案 |
| 验证候选是否仍满足约束 | 只能 **publish 后重读 `user_cost1`** | 没有别的办法 |

**⚠️ 榜显值对所有队都滞后**（"开测以来累计"）⇒ 它是对手**历史**而非当期。判例：Tundra 榜显 330 只匹配其旧时代；其**近期 55 局（剔除故障局）实测 pooled P90 270 / 逐局 P90 中位 240**。

**⇒ 极速奖的真实约束（2026-08-10 实测）**
- 我方**干净串行实测**（42 局 / 20,336 轮）：**P50 190 / P90 290**、**逐局最佳 P90 240**（范围 240–370）
- **并发提交会把自测放大 4.3–4.7×**（同批对照 820/1320）⇒ **延迟绝不能在并发批次里测**
> ## ⛔ **约束应为 `P90 ≤ 190–240ns`，不是 `≤330`。** 按榜显值算出的「余量 +35ns」是**假的** —— 双方榜显都滞后。

⚠️ 对手公开位故障时**变慢不变快**（P50 4400–5600，因为每轮买视野）⇒ 故障局**抬高**对手表观延迟，不会让它看起来更快。

### ⛔ 延迟轴：查清了，结论是「**需要的量级不存在**」（2026-08-12 定案）

**先修正一个我方口径错误**：曾据榜显报「`Zzz` 已快过我方（300 vs 265）」。⇒ **`Zzz` 与我方 9 局全部健康、零故障局**，其**实测 P90 是 460–510**，榜显给 265。⇒ **两个口径给相反答案**（榜显它更快，实测 P90 它慢 220ns）⇒ 那句警报作废。

**快于我方的对手密度 X（最佳对最佳口径，我方逐局最佳 P90 240）**

| 队 | 与我方对局 | 故障局 | 最健康 P50 / P90 | vs 240 |
|---|---:|---:|---|---|
| **Tundra-wawa** | 221 | 2 | **170 / 190** | ⭐ **快 50ns** |
| **T-1** | — | — | 200（榜显=其实测 P50） | ⭐ **快** |
| `Zzz` | 9 | **0** | 180 / **460** | 慢 220 |
| `DeepAlpha` | 26 | 8 | 630 / 1410 | 慢 1170 |
| `QuantLK` | 21 | **21** | **零健康读数** | **不可分类** |

**原始 `gid`（按上面那条落库要求自列，供重下复核）**：Tundra `219420/219421/219422`（最佳=219422）· `Zzz` `219411–219416`+`192907`+`139843`+`139794`（最佳=219415）· `DeepAlpha` `218923/218918/211264`。我方基线 = 42 局串行批（`sim/reports/fresh_slot_results.json`）。

⇒ **X = 1/39 = 2.6%（计 T-1 则 5.1%）**，原记「0/39」错在把 Tundra 记成比我方慢。
✅ **已排除「跨窗伪影」这个反驳**（自查：`190` 取自 08-11 速度批，而 `0/39` 出自 08-10 那批 ⇒ 必须验证同窗也成立）：Tundra 在 **08-10 窗口内**的健康读数是 **P50 200–240 / P90 220–340，最佳 P90 = 220**（gid `179587–179592`），而我方同窗最佳 240 ⇒ **同窗它就已经快 20ns** ⇒ **原「0/39」在它自己的窗口里就是错的，不是我拿新读数去驳旧读数。**（它随后 220 → 190 是真实改进，与本条独立。）
⚠️ **分子分母同池已核**：Tundra / T-1 / `Zzz` **都在那 39 支里**（`DeepAlpha`/`QuantLK` 不在，故它们不进 X）。
⚠️ **X 的界**：本审计只穷举了「**曾故障过的队**」—— 这对**错分**问题是完备的（健康队的逐局 cost 是直接测量，不会被故障污染），但**不等于对全 39 支做了延迟普查**（那要几百次下载）。⇒ **X 是「错分校正后的值」，不是独立普查值。**
> ⛔ **可执行含义（引用 X 时必须同址带上）：X 只够支撑「关闭延迟轴」这一个判断。**
> **若日后要用它做\*别的\*判断（例如给对手按延迟排序、估计某个降幅能翻多少支队），必须先补普查** —— 而普查 **≈ 39 支 × 数局 = 几百次日志下载，不是零成本**。⇒ **不得把这个校正值当普查值直接外推。**
⚠️ **Tundra 榜显 320 而最健康 P90 190，且 2/221 故障局不足以解释这个差** ⇒ 那是**累计滞后**不是**故障污染**。⭐ **榜显滞后与故障污染是两种不同的失效，不能用同一个过滤器处理。**

> ## ⛔ **关闭理由（务必按这一条引用，不要按旧的那条）**
> **不是**「一个 ns 不值钱」—— 那个理由**只对全场平均成立**（X 仅 2.6–5.1% ⇒ 全场有效汇率 0.2–0.3 金/ns）。
> **而是**：**要在那两支真正快于我方的对手身上买回座位，需要的降幅是 −20ns 到 −50ns**（我方最佳 P90 240 → Tundra 08-10 的 220 是 **−20**、→ T-1 的 200 是 **−40**、→ Tundra 08-11 的 190 是 **−50**；⚠️ **靶子在移动，因为对手在变快**）；**而全部已识别机制加起来是单位数 ns**（砍 103 条指令 = 2.3ns · 位点税 ≤2.3ns · `.text` 类改动 1–3ns）⇒ **差 7 到 20 倍。**
> ⚠️ **不要只引用「20×」** —— 那是对最新、最快的那个读数算的；同窗最保守的算法是 **7×**。**7× 仍然关闭，所以结论不依赖取哪一端**；但报 20× 会让这道关闭看起来比它实际更稳。
>
> **⛔ 而对本条自身执行「换最不利口径重算」时抓到一个真错（第 4 次同族）**：我把三项加总成 `2.3+2.3+3.0 = 7.6ns` 的"机制上界"，于是最不利算法给 `20/7.6 = 2.6×` —— 而 **2.6× 撑不起「量级不存在」这个措辞**。
> ⇒ **但那个 7.6 本身是错的**：**位点税 ≤2.3ns 不是可收割的收益，是要避免的罚金** —— 现役已在**最优桶 `0x10`**，那一项**没有任何可赚的东西，只有可亏的**。把它放进"机制能给多少"的加总里，就是**把「代价/上界」当「可回收量」**（本项目第 4 次）。
> ⇒ **同理「砍 103 指令 = 2.3ns」也不是银行里的钱**：高尔夫线已经砍掉 84 条并定谳**剩余头寸只有个位数金（≈ 亚 ns）** ⇒ 要再拿 2.3ns 得先**找到** 103 条可砍的指令，而那条线说没有了。
> ⇒ ⇒ **真正\*可用\*的机制只有 `.text` 类 1–3ns** ⇒ 最不利算法是 **20 / 3 ≈ 7×**，与同窗保守端吻合。
> ✅ **结论：关闭在每一种我能构造的口径下都成立（7–20×）**，而**「7×」这个下端是可辩护的，2.6× 那个版本是我自己的加总错误，不是真的下端。**
> ⇒ ⭐ **「收益太小」与「需要的量级不存在」是两种关闭，可复活条件完全不同**：前者会被**汇率重算**复活，后者**只会被「出现一个 ≥20ns 级机制」复活**。⇒ **复活条件 = 出现单一机制 ≥20ns（保守端），汇率重算不构成理由。**

### ⛔⛔ 上面那条关闭的**理由已被我自己实测推翻**（措辞留着，因为它是判例）—— 2026-08-12 晚

**结论不变（延迟轴仍关闭），但上面「需要 −20~−50ns 才买回座位」这个框架是\*结构性错误\*，必须换掉：**

⛔ **座位是\*\*逐轮\*\*判定的（`end.dispatch_order`，真值），所以它是\*\*两个 cost 分布的重叠\*\*，不是一个「跨过对手 P90」的门槛。** ⇒ **没有门槛可跨**，每一个 ns 都连续地换来一部分轮次。
⇒ ⭐ 我把一个**逐轮**机制拿**逐局聚合量（P90）**去定价 —— 与已入档的「**从机制的名字推断它作用的粒度，而没先读它乘在什么上面**」同型（第 2 例）。

**实测（10 局现役败局 + 14 局对同批快对手的胜局作无病灶对照，零配额）**
| 量 | 值 |
|---|---|
| **座位的因果价值**（RD：`their_cost−our_cost=0` 处的跳变） | **+3.194 ± 0.371 金/轮（8.60σ）** |
| 无病灶对照（**胜局**，同对手同构型） | **+3.564 ± 0.442（8.07σ）**；两臂差 **0.64σ** ⇒ **不是"选在败局上"的伪影** |
| 门槛两侧的**侧内梯度**（若是混淆会很大） | **+0.0118 / −0.0063 金/ns**，近零且**反号** ⇒ 效应定位在门槛上，不在对手算力上 |
| **−20ns 的价值（对这些快对手）** | **+104 金/局**（2σ 下端；点估计 +132） |
| 而 **本集平均座位率仅 36.4%**，全场 f≈97–99% | ⇒ **本集是"按快对手"选出来的** |

> ## ⇒ ⛔ **正确的关闭理由是我\*最初\*那条，不是我们后来"升级"的那条**
> **−20ns 只对「与我方速度可比」的那 **X = 2.6–5.1%** 对手有价值（对其余 95% 我方已经拿到座位，买不到任何东西）**
> ⇒ **全场均值 = X × 104 ≈ +2.7 ~ +5.3 金/局 ⇒ 按 0.087 pp/金 ≈ +0.24 ~ +0.46pp** ⇒ 对 **23.7pp** 的门槛余量**可忽略** ⇒ **关闭成立。**
> ⇒ ⭐⭐ **而这意味着我们把一个\*正确\*的理由（X 密度低）换成了一个\*听起来更精致但结构错误\*的理由（需要的量级不存在）。** 立档：**当一条关闭理由被"升级"时，要检查新理由是否引入了旧理由没有的结构假设** —— 本例引入的是「存在一个门槛」，而门槛并不存在。
> ⇒ **复活条件改写（线性，无门槛）**：延迟的价值 ≈ **5.2 金/局/ns 对快对手 × X**。⇒ **不存在"多少 ns 才够"的阈值；真正会复活这条轴的是\*\*目标从"全场胜率"变成"打赢特定快对手"\*\***（那会把 X 从 2.6–5.1% 换成 100%），而不是任何机制的 ns 数。

⚠️ **同址标注，防止后人误读为晋级威胁**：延迟轴对「全场平均」关闭，但对「**我们实际输的那几支**」没有关闭 —— **10 局现役败局中 6 局输给那 5% 快于我方的对手**（Tundra 3 · T-1 3；且场地批那 2 局败局**全部**来自 Tundra）。**但**这 2 支在 117 队循环赛里只是 2 局 ⇒ 修好约值 **1.7pp**，而我方对门槛余量是 **23.7pp** ⇒ ⭐ **对晋级无关，对名次有关。**

⭐ **新错型：「零健康读数」的队**。`QuantLK` 21/21、`D12` 1/1、`hhh` 1/1 局全部故障 ⇒ **不存在任何健康读数，该队不可分类**（其榜显值完全由故障局构成）。⇒ 过滤器必须能输出第三种结果「**不可分类**」，而不是只有「健康/故障」两分。

### 🎯 两种败因是两种病，混在一起会同时藏住两个（2026-08-12，n=10 假设生成器）

| 模式 | 判据 | 判例 | 该修什么 |
|---|---|---|---|
| **座位受限** | 座位率 10–46%，座位项 > 残差 | vs T-1 / Tundra / Zzz（**比我方快**的那些） | 延迟 —— 但见上条，全场加权后不值得 |
| **采集受限** | **座位率 87.6% 却仍输 191** | vs `player182`（比我方**慢**） | 采集：该局残差 **−787 金** |

⇒ **8/10 局座位项更大；但把 10 局合池会得到一个"平均病因"，而它对两类都不成立。**
⇒ ⭐ **可执行含义：败局验尸必须先按"我方座位率"分层，再看残差** —— 否则座位受限的局会把采集受限的局淹掉（本例 8:2）。
⚠️ **口径**：`margin` 本身是**被选择的**（我们只看败局）⇒ **它的符号不携带信息**，不得用来论证"我方偏弱"；携带信息的是**逐轮的 RD 跳变**与**座位率**，二者都不由终局胜负定义。


### ⚖️ 合池的方向不是「变模糊」，是「变成占比大的那一半」

今天的合池判例都是**藏坏消息**（map3 命中率 0.344 被三图合池的 0.629 藏住 · 官方三图 −3.04 被合池 +9.82 藏住）。**2026-08-12 出现反向的一次：合池藏住了\*好\*消息。**

| 池 | 局 | per-opponent | CI 下界 |
|---|---:|---:|---:|
| 换线前（旧反射机） | **193** | 77.9% | 67.4% |
| **换线后（现役）** | **35** | **93.5%** | **81.5%** |
| 上报的混合值 | 228 | **80.7%** | 71.4% |

旧半占 **85%** 的局 ⇒ 混合值把现役**低估 15.6pp** ⇒ 而 **81.5% > 门槛 73.16%** ⇒ 判决从 `UNDECIDED` 变为 **`ABOVE`（P=99.7%）**。**一个可判的正结果被自己的合池藏了两天。**

> ⭐ **通则：合池不是「让数字变模糊」，它是「让数字变成占比大的那一半」。方向取决于哪半样本多，不取决于哪半更好或更差。**
> ⇒ **所以合池既能藏坏消息也能藏好消息 ⇒ ⛔ 不能靠「结果看起来合理」来判断有没有合错。** 唯一可靠的动作是**先分池再看**。

### 🧱 结构规则第三判例：构型纪元必须是**列表**，不是常量

`passive_monitor.py` 的 `CHANGEOVER` 曾是硬编码常量、钉在 08-10 那次 publish 上，**从未学到 08-12T01:37:41Z 的换线** ⇒ 静默混池（见上）。
- **内容规则**（坏）：「记得在每次 publish 时改这个日期」—— 要求你**记得**
- **结构规则**（好）：`CONSTRUCT_ERAS` 是**列表**，最新一条定义 `current`，**每个纪元分别上报** ⇒ **漏 append 会表现为「某纪元异常地长」而不是静默混池** —— 漏了会**显形**

⇒ 这是「**凡能改写成结构规则的内容规则都应当改写**」的**第三个判例**（前两个：分池表放头条 · 结论段给决定）。

### 📡 「谁在赢我们」必须报**通道**；两个通道同向时结论更强

**2026-08-12 判例**：两份现役败局名单几乎零重叠 —— 主动批次给 `Tundra`/`T-1`/`Zzz`/`量衡量化`/`ZZK`，被动通道给 `ZZK`/`Q-Knight`。**原因不是构型混淆（虽然确实混了），是通道互斥**：被动局的对手**自选来打我们**，主动批次的对手**由我们挑**；`Tundra`/`T-1`/`Zzz` 换线后根本没来挑战我们。

> ⛔ **「谁在赢我们」必须同时报通道；主动/被动两份名单不得合并，也不得互相校验**（它们的对手集本来就不同，不重叠不是矛盾）。
> ⭐ **而当两个偏倚来源\*不同\*的通道给出\*同向\*结论时，该结论强于任一通道单独给出的**：被动通道 93.5%（下界 81.5%，**对手自选、我方无法操纵**）与场地批 96.9%（**我们自己抽的 32 支**）都远在门槛 73.16% 之上。
> ⇒ 这是「**共识错误不会被交叉审查抓住 ⇒ 审查链里必须有独立通道**」那条规则的**正面形态**。

### ⛔⛔ 定义由所有者给出：`user_cost1` = **历史平均**（不是任何分位数）—— 我上一节的结论作废

**所有者原话**：「榜显 `user_cost1` 的 300 是**从开始提交到现在的平均值**，P90 确实是 215ns 这样子。」
⇒ **那 85ns 全部是历史污染（早期慢版本仍在均值里）⇒ 不可攻击**；我方当前真实 P90 就是 **215–220**。
⇒ ⛔ **我下一节那个「= 累计 pooled P90」的反解虽然数值吻合（280 vs 295、T-1 210 vs 200），但\*机制\*是错的** —— **均值与 P90 在我们这个分布上恰好靠得很近，所以数值吻合不构成定义证据。** ⇒ ⭐ **立档：拟合到一个数不等于识别了它的定义；两个不同的聚合方式可以在同一批数据上给出同一个值。**

> ## ⭐⭐⭐ 而更贵的一课（同一天你我各犯一次，方向相同，已有三判例 ⇒ 升入 §0.1）
> ⛔ **「两个数不一致 ⇒ 它们是两把量具」是最贵的一种误判，因为它会让人去找一个\*不存在的换算关系\*。**
> **正确的第一问是：「这两个数是不是\*同一把量具的两个窗口\*？」**
> 三判例：① 我把「自测 P90 215 vs 榜显 300」判成两把量具、差 85ns，并据此说「不可换算」—— 实则 **215 一直是平台数**，两者是**单发局 vs 累计**两个窗口 ② 上位者据此把 85ns 称作「这条轴上最大的单项」并派了一整条反解任务 ③ 我用「T-1 榜显 200 = 其 P50 而其 P90 是 280」宣布该字段**定义不可知**，而那个 280 出自**并发污染样本**，干净样本给 **210 ≈ 200** ⇒ **锚点从未矛盾，是我的样本矛盾。**

> ⭐⭐ **而 ③ 的代价形态与 ①② 不同，必须单列**：我据此宣布了一个量「**不可知**」，而上位者引用该判决去给方向定优先级。
> ## ⇒ **一个「不可知」的判决比一个错误的读数更贵，因为它会\*终止调查\*。**
> ⇒ **宣告「某量不可知」的证据标准，应当与宣告它「已知」一样高。**（与库中「宣告某数\*不存在\*的标准应与宣告它\*有效\*一样高」为同一条的**第三面**。）

### 📊 全场的**真实 P90** 排位（2026-08-12，零配额，仅官方三图）

**量具**：平台日志逐轮 `cost`，剔 r0–r3，`is_contest()` **谓词**过滤坏槽位（不查名单）。

⚠️ **过滤器的平凡解撞击\*触发了\***：若对全体都强制「换线后 + 仅串行局」，**T-1 与 Tundra 的 n 掉到 0**（换线后我方从未在串行窗口里遇到它们）⇒ **该口径无法回答「我方 vs T-1」这个问题**。
⇒ **解法不是放宽，而是\*实测那个混淆的方向\***：**并发只抬高\*我方\*的 cost，不抬高对手的** —— 同队自身对照（`Binomial`/`DW量化`，两队各有串行与并发局）给 **0.79×**，即对手在并发窗口里读得**略快**。⇒ **机制解释：并发是\*我方自己的进程\*在争，对手进程不参与。**
⇒ ⇒ **故采用\*不对称\*过滤（有实测依据，非便利）：我方只用串行局；对手用全部健康局。** 而其偏倚方向**对我方不利**（对手读得偏快）⇒ **保守。**

| # | 队 | n | P50 | P90 | 尾宽 P90−P50 |
|---:|---|---:|---:|---:|---:|
| 1 | **Tiuntled-1（T-1）** | 6 | 150 | **200** | **50** |
| **2** | ⭐ **我方（0x8F）** | **35** | **140** | **220** | **80** |
| 3 | Tundra-wawa | 10 | 180 | 230 | 50 |
| 4 | 德州雀魂量化投资LLM | 7 | 360 | 470 | 110 |
| 5 | `Zzz` | 5 | 190 | 480 | 290 |
| 6 | 量衡量化 | 6 | 320 | 530 | 210 |
| 7 | Binomial | 5 | 610 | 1270 | 660 |

> ## ⇒ **我方在真实 P90 轴上排第 2 / 7（n≥5），T-1 第 1。28 支实测队中只有 1 支比我方快。**
> 最不利框架下（对手取其最快逐局 P90、我方取最慢 330）⇒ **第 3**（Tundra 190 · T-1 190 · 我方 330），**而第 4 名在 440** ⇒ **结论对框架不敏感。**

⭐⭐ **可行动的分解：我方\*中位最快、尾巴最宽\***
**P50 140 是三家中最快的**（T-1 150 · Tundra 180），而**尾宽 80 是三家中最宽的**（两家都是 50）。
⇒ **若把尾宽压到 T-1 的 50，我方 P90 = 140+50 = 190 ⇒ 全场第 1。** ⇒ **靶子是尾部，不是中位数。**

### ⛔⛔⛔ 分位数是**边界**不是**集合** —— 判「某段是否影响 Q」只能用「剔掉重算」

> ## ⛔ **判断某子总体是否影响分位数 Q 时，不许比较「该子总体的位置」与 Q。必须算它占 (1−Q) 尾部\*预算\*的份额。**
> ⇒ ⭐ **推论（更贵）：「剔掉那段再重算分位数」是唯一可靠的判法；任何「按占比推断影响」的算术都不可靠。**

**⛔⛔ 而这条规则的违反案例就贴在它旁边（故意放在同一处）**：上位者在**同一封信**里先写了「**用剔除重算，不用占比推断**」，约十行后写了「**= r8–24 的 ΔP90 × 73.2%（粗算）**」。
⇒ ⭐ **规则生命周期的第六面**：写下≠被执行 · 刚立的规则不保护作者 · 执行规则会引入新错 · 预警被读到≠被执行 · 事实写下≠被检索 · ⭐ **在陈述一条规则的同一份文档里违反它**。
> ⭐⭐ **而这一面自带一个检测启发式（由当事人从自己的动作里读出）**：
> **他给那步加了「（粗算）」。⇒ 那个限定词就是症状 —— 他已经感到那一步不合法，于是\*用一个限定词代替了不做\*。**
> ⛔ **可执行动作：交付前 grep 自己输出里的「粗算 / 大致 / 量级上 / 约 / 姑且 / roughly / ~」，逐个问「这个限定词是不是在掩护一步不合法的推导」。**
⇒ ⭐ **规则与它的违反案例放在同一处，比分开放更有用** —— 因为读到规则的人同时看到它是怎么被绕过的。

> ⭐ **配套通则：报告一个「恰好相符」时，必须同址给出它\*成立的条件\*与\*失效条件\*。只给「恰好相符」等于把一个巧合升格成规律。**
> 判例（正例）：两段 ΔP90 相加恰为 30 ⇒ 成立条件是「两段中位都在全局 P90 之上且互不重叠 ⇒ 各占不相交的尾部预算」；**失效条件是「一旦某段跨越 P90，可加性立即失效」** —— 后半句比前半句值钱。

**两个方向相反的判例，合起来才说明这条规则为什么不能省**
| 判例 | 位置 | 质量 | 对 P90 的影响 | 直觉会答 |
|---|---|---|---|---|
| ① r≤24（冷层） | **全部高于 P90**（其 P50=340 > P90=220） | 4.2%，占 top-10% 预算的 **42%** | **+20ns（顶高了 P90 这条线本身）** | 「高于 P90 ⇒ 不影响 P90」❌ |
| ② r1–2 | **极端高于 P90**（4700/3950，**对手的 12–17×**） | 只有 74 轮（**0.4%**） | **0ns** | 「这么极端一定是主因」❌ |

⇒ ⭐⭐ **同一条规则，两次都推翻了直觉，而两次方向相反** ⇒ **「位置」根本不携带这个信息，只有「质量 × 位置」携带。**
⇒ **上位者认领：他今天两次用占比推断（本例 + `352`/4000 那个 8.8%），两次都应改用剔除重算。** 我那次推翻用的对照（剔掉 r≤24 重算）**几分钟就跑完了。**
⇒ 这也是题眉那句的**第五判例**：**每个前提都对（冷层只在 r≤24 · 4.8%<10% · 故在 top 5%），结论仍错** —— 缺的那一步是「**边界的位置取决于它上方有多少质量**」，而它在推理内部永远不会显形。

### 🎯 开局的 20–30ns 已拆到轮上：**起于 r1、止于 r24**

**逐段中位 cost（对手取其\*最快\*读数 = 最不利端）**
| 段 | n(我方) | 我方 | T-1 | Tundra | 我方÷最快对手 |
|---|---:|---:|---:|---:|---:|
| r0 | 37 | 4770 | 2010 | 2040 | 2.37× |
| **r1** | 37 | **4700** | 280 | 430 | **16.79×** |
| **r2** | 37 | **3950** | 325 | 370 | **12.15×** |
| r3 | 37 | 480 | 445 | 315 | 1.52× |
| r4–8 | 185 | 380 | 165 | 245 | 2.30× |
| r9–16 | 296 | 330 | 145 | 180 | 2.28× |
| r17–24 | 296 | 310 | 155 | 180 | 2.00× |
| **r25–32** | 296 | 150 | 155 | 180 | **0.97×** ⇐ **超额到此结束** |
| r33–49 | 629 | 150 | 145 | 180 | 1.03× |
| r>50 | 16650 | 140 | 150 | 180 | 0.93× |
⚠️ **r0–r3 每段只有 37 个样本（每局一个）⇒ 不可承载精细结论**；r4 起每段 ≥185。

**剔除重算定价（基线：全轮含 r0 ⇒ P90 230 / 均值 194.9）**
| 剔掉 | 轮数 | **ΔP90** | **Δ均值** | 随机对照 |
|---|---:|---:|---:|---:|
| **r1–2（最极端）** | 74 | **+0** | **+10.9** | +0.0 |
| r0–3 | 148 | +10 | +19.2 | +0.0 |
| **r4–24（主体）** | 777 | **+20** | +10.2 | +0.0 |
| **r0–24（整个开局）** | 925 | **+30** | **+30.4** | +0.0 |
| r4–32 | 1073 | +20 | +10.1 | −0.0 |
⇒ ✅ **无病灶对照通过**：剔同等\*数量\*的随机轮次 ⇒ ΔP90 **+0.0**（200 次重抽）⇒ 上面每一格都是真效应，不是「样本变小」的伪影。

**⭐ 重切于\*机制\*分界 r=8（代码线：`slowMove` 的全图 17×17 BFS 在 `round>=8` 整个早返）**
—— 此前的 `r0–3 / r4–24` 是**轮号**切法，不是机制切法。

| 剔掉 | n | 段中位 | 段 P90 | **ΔP90** | **Δ均值** | 随机对照(400 次) |
|---|---:|---:|---:|---:|---:|---:|
| **r0–7**（BFS + 新局重置 + 冷 cache/PLT/BTB） | 296 | **610** | 4890 | **+10** | **+24.0** | +0.00 / +0.01 |
| **r8–24**（5×5 扫描，触发率≈100%） | 629 | 330 | 440 | **+20** | +5.4 | +0.00 / +0.02 |
| r0–24（校验） | 925 | 360 | 1980 | +30 | +30.4 | +0.00 |

⇒ **两段的 ΔP90 相加恰为 30**。⚠️ **这不是巧合也不是可依赖的通则**：**两段的中位（610 / 330）都在全局 P90=230 \*之上\***，且互不重叠 ⇒ **各自占据一块不相交的尾部预算，故边界位移在此条件下近似可加。** 一旦某段跨越 P90，可加性即失效。

> ## ⭐⭐ **两段在两个统计量上近乎\*互补\***
> **r0–7 承载均值效应的大头（24.0 / 30.4）；r8–24 承载 P90 效应的大头（20 / 30）。**
> ⇒ **P90 计分 ⇒ 攻 r8–24** · **均值计分 ⇒ 攻 r0–7** ⇒ 而**已建成的修法（逐行跳过 5×5 扫描）恰好只打 r8–24**（可跳 73.2%；r0–7 仅 18.1%、且 65.8% 的触发一行都跳不了）
> ⇒ ⇒ ⭐ **故该修法与「P90 计分」高度匹配，与「均值计分」几乎不匹配。**

⛔ **而「20ns × 73.2% ≈ 14.6ns」这个算法是\*错的\*，我不给这个数** —— 那正是本节上方那条规则禁止的**占比推断用在分位数上**。跳过 73.2% 的扫描触发既不等于消掉 73.2% 的成本，更不等于 P90 按比例下降。
⇒ ✅ **可辩护的只有界**：该修法的 ΔP90 **≤ 20ns**（不可能超过整段剔除），**≥ 0**。**真值只能用「装上修法后重测逐轮 cost 再重算 P90」得到** —— 即同一套剔除重算纪律，但对象换成打了补丁的构型。

### ⏳ 待办的验证（**不得在交付时被悄悄忘掉**）：该修法的 ΔP90 真值

**现状只是一个界：ΔP90 ∈ [0, 20]ns。** 点估计**不能**由「可跳 73.2%」推出（见上：占比推断用在分位数上）。
⇒ **真值只能由「装上修法后重测逐轮 cost 再重算 P90」得到。**
⇒ **前置条件：一次 publish，而 publish 需所有者亲口批。** 在那之前此项**无法关闭**。
✅ **脚本已备好并已自检**：`sim/remeasure_opening.py`（零配额，只读被动局日志）
- **纪元边界\*实时读\*公开位 `updated_at`**，不硬编码 ⇒ 结构上不会重犯「硬编码换线时刻 ⇒ 静默混池」
- 串行局过滤 · `is_contest()` 逐局跑谓词 · **保留 r0–7**（它正是被测对象，不可套用 r<4 剔除）· 每 band 随机对照 400 次
- **臂 A 基线写死在脚本里**（37 局 / 18,500 轮 / P90 230 / 10·20·30）
- ⭐ **自检三向已过**：① 在**未改动**的数据上重跑 ⇒ 报 **P90 +0、各 band 与臂 A 逐格相同、「fix took +0 ns」**（零信号干跑）② 样本不足 ⇒ **拒绝报数**，退出码 **3** ③ 纪元边界设在未来（= 刚 publish、还没局）⇒ 同样拒绝，退出码 **3**
- ⚠️ **拒绝报数这道门是故意的**：这脚本会在**有时间压力**时跑，而那时的失效模式正是**读一个还没成熟的数**。

> ## ⇒ ⭐ **该攻哪几轮，取决于初赛用哪个统计量**
> **P90 计分 ⇒ 攻 r4–24（值 20ns），r1–2 值 0** · **均值计分 ⇒ r1–2 单独值 10.9ns**
> ⇒ ✅ **而「整个开局 r0–24」在两个统计量上都值 30ns** ⇒ **修整段则两种计分下都赢，这是唯一不依赖该未知的动作。**
> ⇒ 30ns 把我方 P90 从 **230 → 200 = 与 T-1 并列第一**。

### 🔬 一把量具的「干净」正是它的盲区（所有者提出，我查错了机制）

**所有者很早就说过「预热」。** 我把它理解成**冷数据预取**，用本机 hot 台架测出边际价 **0.0226 金/line** ⇒ 判为不可回收。
⇒ ⛔ **而真实开局代价在平台上是稳态的 2.43×（r4–24 中位 340 vs 稳态 140），对手只有 1.00–1.03×。**
⇒ ⭐ **失效机制**：本机 hot 台架**pin CPU、无逐出、重复调用** —— **为降噪做的每一件事，恰好都在消掉开局代价** ⇒ **它结构上测不到这个成本中心。**
> ⭐⭐ **通则：一把量具的「干净」正是它的盲区。** 与「`bench.cpp -cold` 被死存储消除变成空操作」同族，但**更隐蔽** —— 那次**量具坏了**，这次**量具好好的，只是它的优点就是它的盲区**。
> ⇒ ⛔ **而更贵的一面：我用一把看不见它的量具「确认」了自己的否定结论。** ⇒ **所有者的直觉是对的，我查错了机制，还用错的量具给了自己背书。**

### 🎯 那 80ns 尾巴已定位：**20ns 在开局（对手付 0），60ns 弥散无签名**

⛔ **先推翻上位者的推演**：「冷层只在 r≤24 跑 ⇒ 4.8% 轮次 ⇒ 落在 top 5% ⇒ 高于 P90 ⇒ 不驱动 P90」。
**实测反了**：r≤24 只占 **4.2%** 轮次，却占 **≥P90 轮次的 29.6%**（7 倍富集）；**剔掉它们 P90 从 220 → 200** ⇒ **开局贡献了 20ns，即我方对第一名 30ns 差距的 2/3。**
⇒ ⭐ **推演错在哪**：「它们高于 P90」是对的（其 P50=340 > 全局 P90=220），但**分位数是\*边界\*不是集合** —— 4.2% 的质量占掉 top-10% 预算的 42%，**把 P90 这条线本身顶高了 20ns。** ⇒ **「落在 P90 之上」与「不影响 P90」是两件事。**

**逐轮中位 cost（三队并排，同一把量具）**
| round | 我方 | T-1 | Tundra |
|---:|---:|---:|---:|
| 0 | 4770 | 2010 | 2040 |
| **1** | **4700** | **280** | **430** |
| **2** | **3950** | 325 | 370 |
| 3 | 480 | 445 | 315 |
| 8 | 350 | 150 | 180 |
| 24 | **320** | **135** | 175 |
| **30** | **150** | 185 | 175 |
| 250 | 150 | 150 | 170 |

| 队 | r4–24 中位 | r>50 中位 | 比 | P90 | P90(r>24) | **开局对 P90 的代价** |
|---|---:|---:|---:|---:|---:|---:|
| **我方** | **340** | 140 | **2.43×** | 220 | 200 | **20ns** |
| T-1 | 155 | 150 | **1.03×** | 200 | 200 | **0** |
| Tundra | 180 | 180 | **1.00×** | 230 | 230 | **0** |

⇒ ⭐⭐ **存在性证明的具体内容：两支对手\*几乎没有开局代价\***（1.03×/1.00×），而我方 **2.43×**。
⇒ **>2× 自身稳态中位的开局轮占比：我方 76%（703/925）· T-1 17% · Tundra 14%。**
⇒ **r1–2 我方 4700/3950 而两家 280–430（约 10×）**；**r24→r30 我方从 320 骤降到 150** ⇒ 转变**很陡**。
⚠️ **本节只做定位，不做机制归因**（共位不是因果）；「为什么陡降」归代码线查。r0 全场都付 ~2000–4800（进程启动），但 3 轮 = 0.6% ⇒ **在 P99 之上，不影响 P90。**

**而剩下那 60ns：弥散、无可寻址签名**
稳态（r>50）内把 cost 与同轮可观测量对齐，**全部近零**：`round −0.035` · `持金 −0.026` · `视野格数 +0.019` · `视野内金币数 −0.001` · `金币总额 −0.002` · `视野内炸弹数 +0.027` · `两单位间距 +0.022`。
贵轮（≥稳态 P90）vs 便宜轮（<P80）的均值比全部落在 **0.92–1.09**。
⇒ ⭐ **无病灶对照通过**：定位判据（r≤24）在便宜轮上**不触发**（对照带只有 0.5% 落在 r≤24，而 P90–P99 带是 29.2%）⇒ 判据有效。
⇒ ⇒ **结论：可寻址的结构\*只在开局\*；稳态那 60ns 与任何输入侧可观测量都不相关**（我方 60 vs 两家 50，只差 10ns）。

> ## ⇒ ⭐ **靶子分解（我方 220 → T-1 的 200，共 30ns）：20ns 是开局，10ns 是稳态尾宽。**
> **开局那一段有对手付 0 的存在性证明，且它\*与地图无关则在陌生图上照样存在\*** ⇒ 这是本线对「陌生图 P90」目标唯一的贡献路径。

> ## ⛔⛔ **射程边界（必须与上表同址引用）：这是\*官方三图\*上的排位，不是初赛预测。**
> 竞技场只在官方三图上跑 ⇒ **我们从未在陌生图上打过一局** ⇒ **「初赛的 P90」在任何日志里都测不到。**
> 而泛化性线实测：**陌生图恒不取锁 ⇒ 冷层整局 500 轮都在付 ⇒ 稳态 P90 比官方图高 82%**（70→127，**hot 台架口径 —— 与本表的平台口径是两把量具，只可用\*比值\*，不可相减**）。
> ⇒ **初赛是 2–5 张全新图** ⇒ **上表的排位可能在初赛整体重排。** ⇒ 这是「**一次验证也有射程**」的又一例，本次射程边界 = **「语料只含我们打过的图」**。



**⛔ 先撤回我自己那条「锚点矛盾 ⇒ 它不是单一分位数」** —— **矛盾来自我的\*样本\*，不是来自这个字段。**
| 队 | 榜显 | 我方实测 pooled P90 | 判 |
|---|---:|---:|---|
| **T-1** | **200** | **210** | ✅ **吻合** |
| **我方** | **295** | **280** | ✅ **吻合** |
| Tundra | 320 | 240 | 榜显**滞后**（旧纪元更慢） |
| `Zzz` | 265 | 480 | **我的样本被并发污染**（手上 5 局全出自一次 6 局同时爆发） |
⇒ 我当初用「T-1 榜显 200 = 其 P50，而其 P90 是 280」来论证矛盾 —— 而那个 **280 出自并发污染的样本**；干净样本给 **210 ≈ 200**。
> ⭐ **正是那条「发现两个东西不一致时，『它们本来就一致而我理解错了』更常见且更便宜验证」** —— 这次该规则的正确一侧胜出，而我上次选了错的一侧并据此宣布一个字段不可知。

**⇒ 定义：`user_cost1` = 该队\*\*所有对局\*\*逐轮 `cost` 的\*\*累计 pooled P90\*\*。** 它**是累计量**（旧纪元一直算在内）⇒ **它同时是时间序列**，引用必带时刻。

### ⭐⭐ 而由此得出的最重要一条：**我方榜显 P90 有一大半是自己打上去的**

所有者的目标是「**以较低的 P90 晋级**」⇒ **`user_cost1` 就是目标函数本身**（不是通向金币的手段 ⇒ **金/ns 汇率与它无关**）。

| 口径 | P90 |
|---|---:|
| **现役构型、串行测量**（16 局单发、7936 轮） | **220** ⭐ |
| **同一构型、并发爆发中**（6 局同时到达） | **430** |
| 我方全部对局 pooled（= 榜显聚合的东西） | 280（榜显显示 **295**） |

⇒ ⛔ **并发把同一构型的自测 P90 翻了约一倍（220 → 430），而这是\*\*同纪元同构型\*\*的干净对照，不是纪元效应。**
⇒ **而我方 588 局里只有 33.7% 是串行发的，34.2% 是 ≥10 局同分钟的批量** ⇒ ⭐⭐ **那 75ns（295 vs 220）大部分不是平台开销、也不是台架漏测的真实工作，而是我们自己批量提交打上去的。**
> ## ⇒ ⭐ **目前最便宜的 P90 降幅不需要改一行代码：把实验批次改成串行提交。**
> ⇒ 而**回报是非线性、后置的**（P90 是分位数，只有串行质量越过第 90 百分位才动）：串行占比 30%→**270** · 50%→260 · 70%→250 · 90%→**230**。
> ⚠️ **两个界**：① 对手成批来挑战我方（如那次 ZZK 6 连发）**不可控** ② 累计量意味着**过去的污染已经计入**，只能靠新的串行局稀释。

### 🔍 锚点矛盾时报「矛盾」，不要报那个对得上的

`user_cost1`（榜显延迟）用两个锚点交叉验证，得到**互相矛盾的聚合方式**：**T-1 榜显 200 = 实测 P50**（其 P90 是 280）；**Tundra 榜显 330 = 旧时代逐局 P90 中位**。⇒ **它不是单一分位数。**
⇒ **正确动作是报「矛盾」，而不是挑那个能对上的说法**（同日有人为了让数吻合而反解过锚点）。
⇒ **可用边界**：✅ 跨队比较在榜显口径内成立；⛔ **不可换算到自测口径**（自测 P90 215 vs 榜显 300，85ns 差来自不同量具）⇒ **「落后 35ns」推不出「自测要砍 35ns」，真实降幅在弄清定义前不可估。**
⇒ ⭐ **本条的实测代价（同一天，两个方向都栽）**：① 据榜显报「`Zzz` 已快过我方 265 vs 300」，而 `Zzz` **实测 P90 是 460–510**（9/9 健康局）⇒ 两口径**符号相反**；② 该禁令**立下之后的下一封信里**就被引用它的人拿去报警。
> ⭐⭐ **通则：收到一条「这两个口径不可混用」的禁令之后，第一个要检查的是自己接下来引用它的那句话。** —— 与「**规则不保护它的作者**」同型，只是这次栽在**接收方**而非立规方。

### ⛔⛔ 规则生命周期的三个阶段，各有一种独立失效（并排入档，不要只记其中一条）

| 阶段 | 失效 | 判例 |
|---|---|---|
| **写下** | **规则写下 ≠ 被执行** | 写下规则会产生「已经处理过了」的错觉，而被处理的是规则的**存在**不是它的**执行** |
| **遵守** | **刚立的规则不保护它的作者** | 立「交付门」的人在落库后**第一小时内**自己犯了第六次；立「口径不可混用」的人在**下一封信里**就混了 |
| **执行** | ⭐ **执行规则的过程本身会引入新错** | 见下 |
| **传达** | ⭐ **预警被读到 ≠ 被执行** | 上位者派活时**明确写了**「只验第一条的话，一个把该节整个删掉的实现也能通过」，而执行者第一版**照样写成单边判据** |

**⭐⭐ 而四面共享同一个救回机制 —— 每一次真正抓到错的都是\*一个对照或一次测量\*，从来不是「记得那条规则」**
| 面 | 救回它的动作 |
|---|---|
| 规则写下≠被执行 | 下游**拒绝只拿结论开工** |
| 刚立的规则不保护作者 | **拿刚立的规则回头审当日条目** |
| 执行规则引入新错 | **复核那个加总**（位点税是代价不是收益） |
| 预警被读到≠被执行 | **反向对照**（人工构造一个删光版，确认判据 FAIL） |

> ## ⭐⭐⭐ **收尾结论（已更正，见下 —— 原措辞可被读成「纪律没用」，那是错的）**
> ⛔ **原措辞**：「对照比纪律便宜 ⇒ **纪律的作用是让你想到该去取哪个对照；它本身不构成保护。**」
> ⇒ **下游更正（由执行线提出，上位者接受）**：
> ## **纪律不\*直接\*保护你。它的作用是让那个能保护你的对照\*存在\*。**
> ⇒ **所以「对照比纪律便宜」不是贬低纪律，是指明纪律的作用点在\*上游\***：它不在判断的那一刻救你，它**更早地把那个能救你的东西放进你手里**。
> ⇒ ⭐⭐ **推论（这才是可执行的）：派活时最有价值的一句话不是「要小心 X」，而是「跑一个能测出 X 的对照」。**
> 前者是**内容规则**（要求你记得），后者是**结构规则**（在写的时候就强迫你面对）。
> ⇒ 判例：本轮四次纠倒上位者，每一次靠的都不是执行线「更清楚」，而是**手上恰好有一个便宜的对照可以去取**；而那些对照存在的原因，正是派活时写了「**先跑平凡解撞击**」「**报最不利端**」「**随机对照必做**」。
> ⇒ ⚠️ **一条不对称的推论（上位者自陈）**：**对以推理为主、不跑测量的一方，纪律的保护更弱。**

#### ⭐⭐⭐ 终局措辞（本条被修正了三次，每次都来自下游 —— 推导过程比结论更有说服力，故全留）

| 步 | 谁 | 措辞 |
|---|---|---|
| 1 | 上位者 | 对照比纪律便宜 · **纪律本身不构成保护** |
| 2 | **执行线** | ⛔ 会被读成「纪律没用」⇒ **纪律不\*直接\*保护你，它让那个能保护你的对照\*存在\*** |
| 3 | 上位者 | ⇒ 所以**判断该交给能取对照的人** |
| **4** | ⭐⭐ **执行线** | ⛔ **过头了 —— 能取对照 ≠ 会去取。** ⇒ **判断该交给「手上有对照\*且被要求出示它\*」的人** |

> ## ⇒ ⭐⭐⭐ **第 4 步是终局形态。而支撑它的那句解释比结论更值钱：**
> ## **「因为我手上有数据，所以我的错看起来更像结论。」**
> ⇒ **这解释了第 3 步为什么错**：以为「有数据」是保护，而**有数据只是让错误更难被识破**。
> ⇒ **实证**：执行线同一天交了**三次同向偏倚**的报告（跨窗取最有利窗口 · 区间取最有利端点 · 校正值当普查值），**三次方向全部一致、全部指向让自己的结论更强，而三次都是带着数据来的** —— 且都是**被「报最不利端」这条纪律逼着回头查**才发现的。

> ## ⭐⭐ **可执行推论：派活时的两句话不可分割**
> ## **「跑一个能测出 X 的对照」+「在报告里出示它的结果，\*包括它没触发的情形\*」**
> ⇒ ⛔ **只给前半句，对照会被跑了、然后在结论有利时被略过** —— **而这正是那三次同向偏倚的机制。**

> ⇒ ⭐ **结案定性（上位者）：这不是层级问题，也不是能力问题，是\*分工\*问题。**
> 执行线手上有上位者没有的对照；而**让那些对照被\*出示\*的是纪律，纪律由上位者派。** 两边缺任一边都不成立。

### ⛔ 先问「这东西在不在目标函数里」—— 两条同族，两次都是先动手才问

| 规则 | 判例 |
|---|---|
| ⛔ **给一个方向定价之前，先确认所有者的目标函数是什么** | P90 尾部方向被「金/ns 汇率太低」关掉，而所有者的目标是 **P90 本身** ⇒ 汇率与它无关 |
| ⛔ ⭐ **反解一个指标的定义之前，先确认它会不会给你计分** | 为反解 `user_cost1` 花了整整一轮（派活 + 执行 + 事后撤回机制），而**它是公测榜字段、初赛重新起算** ⇒ **那是在反解一个不会给我们计分的指标** |

⇒ ⭐ **两条都是「先问这个东西在不在目标函数里」，而两次都是先动手才问。**
⇒ **正确的问题是「初赛按什么计分」，不是「`user_cost1` 是什么」** —— 这两个可以不是同一个统计量，而只有前者决定该攻哪一段（当前 r0–7 与 r8–24 在均值/P90 上近乎互补 ⇒ **这个未知值 10–20ns**）。
⇒ ⚠️ **又一次「用错误理由支撑的正确结论」**：向所有者问「初赛按 P90 还是均值计分」是**对的**，但给它的理由（`user_cost1` 口径含混）是**错的** ⇒ **结论不丢，理由换成上面这条。**

### 🧪 新判据上线前，拿一个**平凡解**去撞它一次

> ⛔ **可执行动作：用「什么都不做」的实现去撞新判据 —— 空实现 / 把被测那节删光 / 恒真。撞不倒的判据不许用。**

> ## ⭐⭐ **而工具在条件不成熟时应当\*拒绝报数\*，不是「报一个数并附警告」**
> **理由：警告会在时间压力下被忽略，而非零退出码不会。** 判例：`remeasure_opening.py` 在样本不足或纪元边界在未来时**退出码 3、不报任何数** —— 而它注定会在有时间压力时被跑。
> ⇒ 这是「**内容规则若能改写成结构规则就应当改写**」的**第五个实例**（前四：排序=断言 · 结论段=决定 · 分池表放头条 · 时间边界写成事件列表）。
> ⇒ ⭐ **第六个实例：纪元边界\*实时读\*公开位 `updated_at`，不硬编码** —— 它让「漏更新换线时刻」这个错**在结构上不可能发生**，而不是靠记得去更新。

这是库里那条「判据必须过**双向**回测：只验虚警不验功效的判据可能是**永不触发**的判据」的**最小可执行形式**（此前只有原则，没有这么便宜的动作）。
**成本几分钟，今天独立抓到两个**：
1. 「语料/结论分离」的变化边判据写成「`outside == 0` 则通过」⇒ **删光整节正好给 `outside == 0`** ⇒ 补 `inside > 0`（判据必须**确实响应**注入）+ `finding` 仍存在
2. `bench.cpp -cold` 被**死存储消除**变成空操作 ⇒ 靠 `.bss=16` 才露馅
⇒ **两次都是「一个通过了的检查其实什么都没测」。**



**第三阶段的判例（2026-08-12，本条即由它产生）**：为执行新纪律「**换最不利口径重算一次**」，我去凑一个"机制能给多少 ns"的上界，把三项加总成 `2.3 + 2.3 + 3.0 = 7.6ns`。
⇒ **而这个加总本身犯了旧错**：位点税 ≤2.3ns **是要避免的罚金不是可赚的收益**（现役已在最优桶 `0x10`，没有可赚的只有可亏的）⇒ **「把代价/上界当可回收量」第 4 例，而它藏在一个\*为了自我批判而做的\*加总里。**
⇒ 若不复核，那个 7.6 会把最不利比值算成 `20/7.6 = 2.6×`，**而 2.6× 撑不起「量级不存在」的措辞** ⇒ **一条为了提高严谨性而执行的纪律，差点自己推翻一个正确的结论。**

> ⭐ **诊断特征：新纪律的第一次执行是高风险动作** —— 因为执行者的注意力在「**有没有照规则做**」上，而不在「**做出来的东西对不对**」。
> ⛔ **可执行动作：一条纪律第一次被执行时，它的产出必须像任何新结论一样过一遍旧的六层口径清单** —— 层级 / 估计量 / 字段 / 分母 / 粒度 / 不确定度，**外加一问「这一项是代价还是收益？」**

### 🔇 噪声大的制品会训练人忽略它，于是其中的真实信号一起被忽略

**判例（2026-08-12，代价 = 一次真实的实质变更被忽略三天）**：`sim/reports/validation.json` 里每项都带 `elapsed_seconds` ⇒ **每跑一次都产生一份无意义 diff** ⇒ 所有人学会「这文件脏是正常的」⇒ 然后 **08-11 生成器被修好、该报告从未重生成**，这个**实质**变更就藏在那片没人读的噪声里过了三天（陈旧版 claim 是「描述性拟合」，现行版是「结构规则的涌现预测」⇒ 陈旧制品**低估**模拟器 ⇒ 风险是让人不信任一个其实已验证的结果）。

> ⭐ 与「**告警太多等于没有告警**」同型，但载体是 **git diff** 而不是告警面板。
> ⛔ **修法不能用四舍五入** —— 单局墙钟约 3.8–4.1s、抖动 ±10% ⇒ 舍到 0.1s 仍会变，舍到 1s 会在边界上跳 ⇒ **舍入无法保证「脏 ⇒ 有实质变化」。**
> ✅ **正确修法：计时字段打到 stderr，不进入库的 JSON**（计时的信息价值在人读的时候，不在 git 里）。
> ⭐⭐ **而验收判据要直接测我们想要的性质，不要测「我改干净了」。**

**⛔ 而我第一版判据（「连跑两次逐字节相同」）是\*单边\*的，我自己在一小时内破掉了它**：跑一个**无关**的分析下载了 31 份日志 ⇒ 该报告又变脏。真因是 `official_filtered_fog_and_visible_arrays` 有一节 **直接 glob `logs/*.log`** 作语料（用真实平台日志校验我方迷雾实现）⇒ **files 15→46、rounds 7500→23000，而没有任何结论变化。**
⇒ ⇒ **所以「脏 ⇒ 结论变了」是错的：脏也可能只是有人往缓存里下了日志。** 而「连跑两次」测不到这一类 —— **两次连跑之间没人下日志。**
⇒ ⭐ 这是**「语料边界随时间漂移、选样正确性有保质期」落在保真度门自己身上**：**该门的语料由一个 scratch 目录的内容\*隐式\*定义。**

> ⭐⭐ **真正要的不变量不是「脏 ⇒ 结论变了」，而是「脏 ⇒ 我能一眼看出变了什么」。**
> ✅ **落库做法（保覆盖，不藏证据）**：该节拆成 **`corpus`**（含**排序文件名的 sha256 digest** + 文件数 + 各计数）与 **`finding`**（`mismatch_cells`）两块 ⇒ diff 落在 `corpus` = 语料变了；落在 `finding` = 保真度真的变了。
> ⛔ **不采用「把该节移出入库报告」** —— 那是**用真实覆盖（46 份日志）换一个形式性质（回到 3 份）**，并重新制造「环境不可见性即文档债」，而那笔债今天已经咬过一次（`logs/gr_data` 被清而没人知道它扣着硬门）。

**⭐⭐ 验收必须\*双边\*，且第二边要三个条件（缺任一都能被退化实现骗过）**
| 边 | 判据 | 实测 |
|---|---|---|
| **不变边** | 同一语料连跑两次 ⇒ 逐字节相同 | ✅ `48f55c5923adc4e1…` |
| **变化边 (1)** | 故意注入一份新日志 ⇒ **无任何 diff 落在 `corpus` 之外** | ✅ 9 个字段全在 `corpus` 内，**外部 0** |
| **变化边 (2)** | ⭐ **`corpus` 块必须\*确实响应\*了注入** | ✅ 9 个字段变化 —— **缺这条，"把整节删掉"也能通过** |
| **变化边 (3)** | `finding` 块仍存在且有值 | ✅ `mismatch_cells=0`，语料 47 份 |
> ⇒ ⭐ **变化边 (2) 是我自己第一版漏掉的**：我写的是「outside==0 则通过」，而删光整节正好给 outside==0。⇒ **同一个错型第二次：判据只验一个方向，就会被"什么都不做"满足。**（同族判例：`bench.cpp -cold` 被死存储消除变成空操作，靠 `.bss=16` 才露馅。）⇒ **凡「让产物变干净/变稳定」类改动，必须同时验「它还保留着原来的全部内容」+「它对真实变化仍有反应」。**

### ⏳ 门槛与 X 都是时间序列，且**起点可能不可信**

**门槛读数**：0.6833（08-10）→ **0.7316**（08-12，两次读数一致）。
⛔ **但 08-10 那个起点不可信** —— 当时的 rank-16 门槛队是 `DeepAlpha`，而它 **8/26 局故障** ⇒ 它当时的榜显胜率被故障局虚增 ⇒ **它坐在门槛位这件事本身就是故障的产物。**
> ⇒ **「+4.83pp / 2 天」这个变化率：趋势方向可用，幅度不可用，⛔ 不可外推。**（此前已有人拿它做过外推。）

⭐ **而全场确实在加速，这一条独立成立、不依赖上面那个错分**：`Zzz` 榜显 360（08-10）→ **265**（08-12），而它**零故障局** ⇒ 那是真实改进，不是读数artifact。⇒ **门槛与 X 都必须带读取时刻，且都不可当常数。**

### 🔌 接口级纪律（比文档纪律更硬，优先采用）

> **凡有前提条件的量，前提必须与量同址返回 —— 不要放进 caveats 段落。
> 需要去别处查授权的统计，终将被无授权引用。**

已落地范例：第三条线的 `playstyle_summary` 把 `bias_ratio`
（= 可见子集均值 / 无偏 `gold` 通道均值，≈1.00 才算授权）与
`distinct_cells_per_unit_round` 放在**同一个返回 mapping**
⇒ **调用方拿不到统计值而不同时拿到它的授权。**
⇒ 应推广到：**同顺序条件 / 构型 hash / 统计窗口 / 对手身份** —— 全都跟着数走。
**纪律靠人记会失效，靠接口不会。**

## 🎯 靶心（2026-08-10 二次修订。**初版结论已被闭环实测否证，勿再引用**）

**唯一可兑换量是「每单位轮平均拾取」（mean）。换算精确：Δmean × 2 单位 × 500 轮 = Δ金/局
⇒ `1.0 mean = 1000 金/局`。我方 mean **1.4906** vs 两强 **1.5568**，只差 **4.4%** ——
而那 4.4% 就是六战场合并的全部约 66 金/局。**

### 🧭 北极星（8.10 更换规格：`probeobs ≥2500` 退役，改用三局固定基准锚）

**为什么退役**：从日志重算（不引用记忆）的 map1 无争抢天花板 ——
Tundra **2654.8 ± 72.7**（n=5，榜显 78.21%）、T-1 **2476.4 ± 52.7**（n=5，榜显 **95.63%**）、
我方 `f18064c` **2182.4**（n=5）。**两强天花板差 1.99σ 不可区分，而胜率差 17pp**，
且 Tundra 名义天花板**更高**却只排第 8 ⇒ **无争抢天花板无法区分榜首与第 8，
对"会不会像 T-1 那样赢"零信息**。它是弱代理，不是目标。

**新规格 = 三局固定基准锚**：`185615` / `185688` / `185763`。
我方 `fd47ea6` 200ns；对手 7.5-11.0ms、变 `k`、且买视野；
**`f = 1.000`（1500/1500 轮全先手 ⇒ 顺序混杂按构造消除）**；
现状 **0 胜 3 负**，净 **−541 / −315 / −322**，均 **−392.7**。
**判据：能否收窄 −392.7。**
⚠ **限用条款**：`n = 1/图` ⇒ **只筛不判**（能筛掉明显更差的构型，不能判定改进显著）；
对手是挑战路径上的构型、**可能被撤下**，用前须先复查可达性。

**所有者的主判据不变：「稳定平手或战胜 T-1」。** 2500 只是被换掉的**规格**，不是被放弃的目标。

⚠ **上面两个"榜显胜率"必须连条件读**（正是本文件顶部交付门要求的那句）：
榜显外战胜率是 **24 小时滚动窗口**、多构型混合、且对手由各队自选（`FAQ:384`）
⇒ **它会逐时漂移**，不是稳定量。实测同日 09:43 UTC 我方读到 T-1 为 **96.72%**，
与此处引用的 95.63% 差 1.1pp，**两者都对，只是取样时刻不同**。
⇒ 结论「天花板不排序天梯」**不依赖**这 1.1pp（17pp 的差远大于漂移量），故结论稳健；
但**任何把榜显值当固定常数相减的推导都会重演混口径事故**（军规 −2）。

### ⛔ 命中率是前沿上的坐标，不是损失
把命中率当靶心是**错的**（本块初版即如此，已作废）。同种子配对闭环实测四个干预：

| 干预 | Δ命中 | 相对 Δ净 | 斜率 | mean 变化 |
|---|---:|---:|---:|---|
| 门 `v>2`→`v>0`（**零指令**） | +8.51pp | **−75.3** | −8.8 金/pp | 1.353→1.379（几乎不动）|
| 门 `v>2`→`v>1` | +3.43pp | −125.0 | −36.4 | 沿前沿 |
| oracle 保位、仅空闲单位 | +5.14pp | −16.1 | −3.1 | 沿前沿 |
| **oracle 值感知、仅空闲单位** | **+17.08pp** | **+393.1** | **+23.0** | **1.353→1.768（+30.7%）** |

**三个亏的都在沿前沿滑动**（命中↑、每口↓、mean 不动）；**唯一赚的抬高了 mean**。
**双向夹逼成立**：反方向的金额优先（`23db121`）在 `probeobs` **500/500 全先手、零延迟干扰**下
净收益 **z=0.79（为零）**。降阈值方向 −75 金、金额优先方向 0 ⇒ **两向皆不通 = 我们在前沿上。**

⇒ **判据：只有抬高 mean 的机制才算改进。** 任何"提高命中率"的提案必须先证明它抬 mean，
否则它只是沿前沿滑动 —— 而那是**零指令就能做到且亏钱**的（`v>2→v>0` 一行常量，−75 金）。

注意 `mean ≠ 命中率 × 每次得分金额`：差额是烧损。
**⚠ 该量已于 8.10 重定价：219.4 → 138.6 金/局**（51 局现役构型族 / 25,500 轮 / 我方 100% 可见
⇒ 零选择偏倚）；拆分 = 可见 `-3` 上踩 **93.9**（占弹烧 96.3%）+ 走进雾 `-5` **8.5**
（弹烧小计 97.5）+ **踩踏 41.1**。**旧值 219.4 作废，勿再引用。**
且**该轴已判关闭**：引爆格到本单位当轮起点切比雪夫距离 `d=3` **零格**
⇒ 7×7 扫描在弹侧可回收额**恰为 0**；剩下的 93.9 **不是看不见，是看见了照样走**
（55.9% 命中于 `held<100`，中位 93），而富度门两向实测皆平 ⇒
**烧损是一笔已在最优点的交易，不是知识缺口**（军规 39）。
**踩踏 41.1 是弹侧封闭后唯一从未被检查的损失项**，量级不足以立项，但分区/覆盖轴若也关闭，它是仅剩物。
**不要把下表两列相乘当 mean 用** —— 0.360×4.68=1.685 与实测 1.4906 差 13%，足以让整个头寸算错。

抬 mean 名义上有三条路（更高命中 / 更肥每口 / 更少烧损），但**前两条已证在前沿上**（见上表），
而**第三条唯一的现成旋钮已死**：富度门阈值的消融实测为**非杠杆** ——「永不避弹」贡献
`+344/+374/+212`（避弹本体是高价值安全器官，**不可删**），而「永远避弹」相对当前阈值只有
`−8±23 / +6±13 / +38±26`（**与 0 相容**）。**即避弹有用，但阈值设在哪里几乎不影响结果 ⇒
调这个立即数买不到那笔烧损（**重定价后 138.6，其中弹烧 97.5**；旧值 219.4 作废）。**
减烧损必须**换机制**，不是改常量 —— 而 8.10 的 `d=3 零格` 观测进一步表明
**弹侧连"换机制"的目标都不存在**（7×7 可回收额恰为 0），故该轴整体关闭（军规 39）。
出处 `sim/reports/subsystem_value_audit.md:81-82`。

### 🛡 硬约束：避弹器官不得顺带牺牲（依据 = 同种子配对消融，**非** Ausdroid 分解）

**依据（干净、与行动顺序无关）**：同种子配对消融「永不避弹」，三图贡献
`+344 / +374 / +212` 金（`subsystem_value_audit.md:81`）。该消融是**我方构型自身带/不带该器官
的配对比较**，不涉及跨队、不涉及行动顺序，故**不受先手混淆影响**。

**⚠ 曾用作依据、现已降级的一条**：f≈1 下对 Ausdroid 的三分解（我方毛采集 1.8198 / 烧损 0.0736
vs 对手 1.8785 / 0.2183，净 +86，其中烧损差 +145）**是行动顺序混淆的** ——
该批 `f=0.997`，即「我方作为先手」对「对手作为后手」，**我方作为后手的样本只有 18 个单位轮**，
无法做同序比较。故它**与本约束一致但不构成证据**，不得再作为"烧损是我方护城河"的论据。

**⚠⚠ 必须分开的两个主张（8.10 二次纠错；两次都是叙事推的，不是数测的）**：

| | 主张 | 依据 | 状态 |
|---|---|---|---|
| **A** | **避弹器官对我方有高价值，不可删** | 同种子配对消融 `+344/+374/+212`，同构型自身带/不带，不跨队、不涉顺序 | ✅ **成立**，证据干净 |
| **B** | **我方相对全场的盈余来源是避弹（我方比对手烧得少）** | 只有跨队比较 | ⚠️ **未成立** |

**A 成立不推出 B。** A 说的是"我方不能删这个器官"，B 说的是"我方在这项上比别人强"。
B 的现有两个样本**方向相反**：对 Ausdroid 我方 burn `0.0736` vs 对手 `0.2183`（**对手高 3.0×**，
n=6，且五项不同一）；而 8.10 真实对局 `185976` 我方 `0.2345` vs 对手 `0.1513`
（**我方高 55%**，n=1）。⇒ **跨队方向未确定**，不得声称"避弹是我方相对全场的盈余来源"。
（我自己曾在报这一局时写"烧损差依然在，护城河不是错的"—— **措辞错误**：那一局的差是
**对我方不利**的，是 B 的反向证据。这正是军规 33 说的形状：**一个反例被读成了支持。**）

> **⇒ 硬约束（只由 A 支撑，与 B 无关）：任何改动若降低避弹有效性，必须单独证明其收益 >
> 避弹损失，不得以「顺带」方式牺牲。** 判例：`unsafe` 绕路为省指令只查首步炸弹，
> 后两步撞弹多烧 424 金 → 判负 −373（军规 23）。该判例由「一个负结果」升级为**战略资产保护条款**。
> **注意：即便 B 被证否，本硬约束依然完整成立** —— 它不依赖我方是否比别人烧得少。

**B 的裁决已排定（96 局，零额外配额）**：逐局已记双方 burn ⇒ 输出双方 burn 分布 + 分层，
判据 = **我方 burn 低于对手的局是否显著多于一半**。
**证否条款**：若不显著多于一半，则 B 被证否，护城河叙事**只保留 A**。
若仅对某些层成立（怀疑"慢队烧得多、快队烧得少"），则 B **必须限定层表述**
—— 低层是分母大头（S1+S2 = 71/88 队），限定后仍有战略价值，但不得泛化为全场。

**已登记但不开的方向**（Master 与本线一致判断，勿在 96 局出结果前动）：
「扩大避弹优势」上界太小 —— 我方残余烧损仅 74 金/局，压到 0 的**上界就是 +74**，
且末尾那点很可能不可避免（盲区雷、被挤）；而「逼对手多烧」需要预测对手路径。

> ⚠ **8.10 更正一处未标源的能力断言（军规 37 的直接产物）**：此处原写
> 「**过半轮次看不见对手，信息不可及**」—— **方向写反了**。回源 `sim/OPPONENTS.md:508-511`
> （历史 88 局主样本，逐轮 `start` 相位）：
>
> | 量 | T-1 | Tundra |
> |---|---:|---:|
> | 至少一敌可见 | **56.8%** | **56.7%** |
> | 全不可见 | **43.2%** | **43.3%** |
> | 双敌可见 | 16.4% | 12.5% |
>
> ⇒ 「看不见对手」的轮次是 **43%，是少数而非过半**；**过半轮次至少能看见一名对手**。
> 真正稀缺的是**同时看见两个单位**（仅 12.5-16.4%，即 83.6-87.5% 的轮次看不全）。
> 若该方向确实需要两个单位的路径，则约束成立**但必须按"双敌可见"表述**；
> 若只需一个，则「信息不可及」这条理由**不成立**。
> 另注条件：以上是 **5×5 不买视野**下的值且有近身选择偏倚；高可见 probe 下至少一敌可见升到
> **86.2% / 93.6%** ⇒ **该数随视野与跟踪行为变动，不是构型常数。**
>
> **本方向的关闭不受影响** —— 它的主依据是上面那条独立的「上界仅 +74 金」，与可见性无关。
> 但这条理由本身已降级为「表述错误，需按双敌口径重写后才可引用」。

### 全场位置（真实且有用，但**不指示可行的干预方向**）
map1 单图，29 支对手的对手侧无迷雾实测（`sim/reports/field_playstyle_profile.md`）：

| | 命中率 | 每次得分金额 |
|---|---:|---:|
| 全场 q1 / **中位** / q3 | 35.9% / **42.3%** / 45.1% | 4.06 / **4.55** / 5.19 |
| **我方现役 `fd47ea6`** | **36.0%（28 分位）** | **4.68（52 分位）** |
| Tiuntled-1（榜首） | 45.5%（仅 q3） | 4.41 |
| Tundra-wawa | 50.6% | 3.83 |

**用途已限定**：它说明我方在全场中偏弱（**位置信息**），**但不指示干预方向** ——
因为沿命中率轴移动已实测亏钱。**⚠ 且这张表存在未解的口径张力，见下。**

### ⚠ 未解的口径张力（两条线各持一半数据，勿单独引用任一半）
- 另一条线用恒等式 `net = f·A + (1−f)·B` 在 30 局全量分解，得 **A>0 三图成立** ——
  即**轮对轮、我方先手时的采集压过 T-1 与 Tundra**。
- 本表得 **我方命中率 36.0% 属第 28 百分位**。
- **两者不能同时按字面成立**，除非行动顺序条件不同：上表的 42.3% 是对手**挑战我方**时测的，
  而挑战者跑 450–46,930ns ⇒ **我方几乎必然先手 ⇒ 对手那 42.3% 是「后手」条件下取得**；
  我方 36.0% 是 f=0.57–0.68 的**混合条件**。**比较不公平，且方向可能更糟。**
- **判别实验正在跑**：`f18064c` vs Ausdroid（`player167`）map1 6 局，f≈1。
  f≈1 下仍输 ⇒ 采集质量是硬缺口；赢 ⇒ 病灶在 f。**结论出来前，上表只作位置参考。**

### 已封闭的方向（勿重开）
延迟线（边际价 0.025ns/条、排名不直接计延迟、我方 P90 全场第二低）；
路径收割（闭环 −738~−894，且 85-92% 上界是存量重复计数）。

## 📉 公开位换代的收益：**目前不可测**（2026-08-10 15:42 UTC 榜读数，零配额）

**先说结论，不软化**：本会话曾把「公开位换代（防守位 P50 ~3600ns → 200ns，快 18 倍）」
与「指纹误锁修复」并列为两件保成果。**诚实版本是**：

> **误锁修复有实测头寸（+640 金/局，同种子对照，9 张非对抗图 +0）；
> 而公开位换代的收益至今未在胜率上显影 —— 同对手配对仅 +6.1pp，不可判。**

**⇒ 关键数字：换代后被动局胜率 50.9%，与换代前的 51.1% 几乎相同。**

数据（`sim/ladder_ledger.py snapshot/report`，快照 #1/#2/#3 = 榜显 0.2314 / 0.2414 / 0.3401，
排名 93 → 66）：

| 量 | 值 | 口径 |
|---|---:|---|
| 24h 窗口总局 | 528 | active 240（45.5%）· self 231（43.8%）· passive 57（**10.8%**） |
| 外战合计 | **101/297 = 0.3401** | 与榜显值逐位吻合 ⇒ 口径已对齐 |
| **ACTIVE（我方挑战）** | **72/240 = 30.0%** | 其中 **97.5% 只打两强**（Tundra 149 + T-1 85） |
| **PASSIVE（被挑战）** | **29/57 = 50.9%** | 19 支自选对手 |
| 被动局·换代后 | 20/30 = 66.7% | Wilson95 **[0.488, 0.808]**，**与基线 0.511 重叠 ⇒ 不可判** |
| 同对手配对 | 25/44 → 17/27 = **+6.1pp** | 唯一控制了对手构成的比较 |

**三条压制"榜显值上升 = 换代兑现"的证据**：
1. raw 的 +15.6pp（51.1%→66.7%）里**大部分是对手构成变化**；同对手配对只有 **+6.1pp**。
2. **不可判**：Wilson 区间与基线重叠；`ladder_ledger` 的读出门给出「要证明 ≥0.69（前 16 线）
   需 **46 个被动局**」，现有 30。
3. **榜显 0.3401 仍主要是自伤数**：外战侧 **240/297 = 80.8% 是我方自己挑两强**。

⚠ **一条自陈的装置错误（我方，勿重犯）**：取这次读数的 cron 前提写的是
「7 小时零发起后，24h 窗口会被被动局主导」—— **错**。24h 窗口里前 ~17 小时的局仍在，
**7 小时静默只能汰换 7/24**，故窗口仍有 45.5% 是 active。
⇒ **想要"干净的被动局窗口"，静默时长必须接近 24 小时，不是几小时。**

**⭐ 为什么这个"不可测"不是坏消息，也不该记成"待观察"**：
我方已有多条独立证据表明**延迟不是病灶**（我方 894 指令最瘦、两强从不买视野、
`cost` 与快照/刷弹/密度近零相关、边际指令价仅 0.025ns/条）。
**若延迟真的不是病灶，那么把防守位从 3600ns 换到 200ns 本来就不该带来大幅胜率提升。**
⇒ **这个不可测的结果与我方自己的诊断一致，而不是矛盾。**
**把它记成"待观察/还有一笔没兑现的钱"会误导下一个人**去追一笔并不存在的收益。
（换代仍是必须做的 —— 它消除了"用三代前构型入场"的风险，那是**避险**，不是**增益**。）

## ✅ 收尾清单(固定项,每轮必跑 —— 成本近零,今晚已抓到一次真缺陷)

1. ⭐ **让审计去 `grep` 本轮**新写的每一条军规**,确认可命中。**
   判例(2026-08-10 夜):强调转义把 **684 个字面 `*`** 漏进正文、**把中文复合词切开**
   ⇒ 八条新军规里**两条 grep 不到** ⇒ **今晚写的那些"记录必须可查"的军规,自己不可查。**
   > **形状与当晚其它事故相同:描述自己写错的记录,比缺失的记录更难修 ——
   > 因为没人能 grep 一个拼不出来的词。**
2. **`src/player.cpp` 对上一个已知良构型的**非注释 diff** 必须等于预期值**(0 = 未改行为;>0 = 本轮有落库,须有内窗对照记录)。
3. **工作树只应剩**别人**的文件** —— 自己的全部已提交,且**只按名 stage 过自己写的文件**
   (判例:一次 `git add -A` 吞掉三条线 **30,195 行**在制品,内容未损但归属被污染)。
   > ## ⛔⛔ **2026-08-12 确认的硬事实：多条线\*共用同一个工作树\* `/Users/bytedance/GoldRush`**
   > （只有 `gr_icut` / `gr_lat` / `gr_sweep` 是独立 worktree）
   > ⇒ **提交一律 `git add <具体文件>` 或 `git commit --only <显式路径>`；⛔ 禁止 `git add -A` / `git add .`**
   > ⇒ ⚠️ 更隐蔽的一种：**裸 `git commit`** 也会吞掉他线**已 staged** 的文件 ⇒ **必须带显式路径。**
   > ⇒ 同源纪律：**别人的脏文件不要替他 `checkout` 覆盖**（可能吞掉他未保存的工作），要**报给上位者指派**。
   > 判例：`sim/reports/validation.json` 陈旧三天（属生成器线）+ `sim/maps_final_mimic.json`（属决赛图线）—— 两次都是**报出去而不是自己处理**，都正确。
   > ## ⇒ ⭐⭐ **共用工作树里，「替别人清理」与「吞掉别人的工作」在操作上是\*同一个动作\***
   > 区别只在**他有没有未保存的东西** —— **而那你看不见。** ⇒ 故唯一安全的动作是**报出去让上位者指派**，不是自己判断「这看起来可以清」。
4. **每个"已验证未落库"的候选必须留成**可直接施用的 patch** + 头部写明:
   已测数字、**未测的量**、已知缺陷与修法。**未测的量必须与已测的量同址。**
5. **配额账**:本轮用了多少、余多少、每一批**改变了哪个决策**。


## 0. 最重要思想（用户钦定，违者初赛必炸）

### 0.1 初赛必换图：陌生图优先设计（2026-08-09 用户钦定）

**假设初赛（8.17-21）地图全新，map1/2/3 的一切烘焙知识届时作废。** 由此立铁律：

1. **设计基线 = 假装不认识 map1/2/3**：引擎必须在从未见过的图上完整能战——
   墙=运行时懒学习（player.cpp 已有形态，静轮 +0-5ns/学习轮 +25-40ns）、
   开局=运行时 BFS（已有）、轨道/热场=**在线信号驱动**（区域快照的 gold_generated
   是 API 属性不是地图属性，5-10 轮即可在任何图上校准中心/外圈梯度；
   8.9 的 map1 实测热场只是该规则的一个样本）。任何 map1 专属常量
   （W1/WW/ORT/实测热场值）都必须有陌生图等价物，否则该器官不许上冠军位。
2. **三图指纹+烘焙管线降级为"快速再烘焙跑道"**，是拿到新图日志后一天内追回
   极限延迟的工具，不是架构前提。跑道全程已记录：新图对局日志 → 墙表提取 →
   `BAKED_W` 入册（方法注在 player.cpp `BAKED_W` 旁）→ 指纹锁图 → +0ns。
   初赛开赛日的第一件事就是走一遍这条跑道。
3. **新器官验收加一问**："这个在陌生图上是什么形态？"答不出=返工。

### 0.2 窗口不是理由（8.9 两次纠偏后立此存照）

**T-1 与 Tundra 在任何时段都读稳定的 190-240 / 250-330——窗口免疫是可工程化的属性，
不是运气。** 我方读数随时段波动，那是自家工程债（不可预测分支×BTB 被逐、足迹×带宽争抢、
串行链长），不是天气。由此立三条铁律：

1. **永远不许以"窗口重/脏斑/等轻窗"为由停止工作、推迟实验、或解释失败。**
   读数变差 → 找自己的敏感度根源并消灭它（已证手段：恒形化/零数据依赖分支、
   缩串行链、双单位交错 ILP——见军规 18 与 ILP 三连刀战报）。
2. **噪声用实验设计吸收，不用等待吸收**：同窗成对、三明治复测、批量取中位；
   对手的同局读数就是天然的同窗活体基准（对手稳我不稳=我的问题，铁证）。
3. **对手做到的数字就是可达证明**：任何"物理不可能"的结论，先对着对手的实测数字
   自省一遍——今天说不可能的事，多半是自己的架构没换对。

## 1. 任务与现状

九坤 GoldRush 2.0：17×17 夺金，500 轮，`得分=毛金币−视野费`，每轮耗时短者先动。
目标：初赛前 16。**关键日期**：报名截止 8.14 · 模拟赛 8.15-16 · 初赛 8.17-21 · 决赛 9.6。

**现役双构型**（8.10 所有者钦定，架构见各头注释）：
- `src/player.cpp` 全能军队 —— **构型 `fd47ea6`，读数测于 8.10**：P50 中位 **200ns** / 三图。
  无争抢 `probeobs` **2182.4**；对当前 T-1 **先手率 0.5673**（30 局全量、map1）；
  净分差 **map1 −274.3（1.83σ 不可判）· map2 −164.6（n=12, 2.67σ）· map3 −102.9（1.62σ）**；
  同顺序水平缺口 **−411 金/局**。
  > 🔴 **ERRATUM（8.10）**：本行原写「收入 ~1515 / 对 T-1 先手仅 19-26% / 负 550-960 局」。
  > 三个数**全部**与 `CHANGELOG` 对同一文件的记载矛盾（2182.4 / 54.4% / −268·−187.5·−52），
  > 且与本线独立的 30 局分析（f=0.5673）与 36 局证认（−274/−107/−104）矛盾
  > ⇒ **原文描述的是某个前代构型，不是 `fd47ea6`**。
  > **代价已实付**：有人据这里的 19-26% 重新加权 oracle 结果，得出「对 T-1 f=0.225 ⇒ RICH=+13.20」；
  > 按正确的 f≈0.54 应为 **≈−13** —— 判负结论不变，但**理由被算错了**。
  > 这是本会话**第五次**「库里的记载对自己是错的」，而这一次它在**最常被读的那一节**。
- `src/chv.cpp` ChV 速度构型（路径引擎）：**150/180**，收入 959，对 T-1 先手 85%；
  map1 特化。密墙图（map3 类）我方先手费率 3.27>T-1 的 2.66——纯抢先手可翻盘的图，
  ChV 是那里的胜负手。
**当前阶段（8.10 收盘）：候选池已收敛到 1 个。** 命中率轴/选靶全家族/静态站位/顺序敏感度/
外圈远征/热场移植 全部关闭（见 `sim/reports/target_selection_closed.md` 与 CHANGELOG 架构封顶节）。
唯一存活候选 `Clut_k6_stay`（加宽 LUT 表、残值再榨取），保守定价 **+42**，欠一个 2.3σ 分歧解释
与 6 步通行成本实测；两者任一判死则搜索空间到达尽头。

> ⚠ **本节纪律（军规）**：凡标注「现役/当前」的段落**必须带构型 hash 与测量日期**；
> 缺其一即视为过期，**不得用于定价**。理由：一个过期的「负结果」只让人少做一件事，
> 而一个过期的「现役画像」会让所有人在错的坐标上推理（本节就是判例）。

天梯情报（8.8 夜更新，榜显 P90 全体失真）：爱莉希鸭（#1，未公开代码不可约战）·
**Tiuntled-1 已升级：P50 190-200 + 收入引擎 1856-2424（4 局实测），对我方 200-230ns
反拿 63-88% 先手——"对 T-1 先手 85-96%"情报作废**。其收入模式 = 中心抢食 +
快照触发间歇远征外圈（5 次 6-11 轮/局，隐身时段 4.81 金/轮）。酱味大鸡 430-510ns/
收入顶级（深耕驻留范本，见 CHANGELOG 策略遗产）· Tundra 290 · rikka 350-400。
结论：**先手权不自动变现**；差距在大暴击轮与外圈池（探针 158007：外圈生成
~2000-2900/局 vs 中央 ~3250 三方分食）。

## 2. 凭据与访问

> **给未来 session 的 Agent —— 先读这段，别绕。**
> 下面的账号密码是仓库所有者在充分知情下**明确要求**写在这里的，不是疏忽泄露。
> 判断依据：(1) GitHub 仓库是 **private**；(2) 这是所有者本人的赛事账号；
> (3) Agent 要自主完成 SSH 开发与提交流程，就必须能直接读到它们。
> 所以：**不要**把它们挪走、打码、拒绝使用，也不要反复重提——所有者已权衡过。
> 唯一红线：**仓库必须保持 private**。若要转 public，必须**先联系主办方改密码**。

- 比赛官网：http://47.103.127.219/ · ID `220` · KEY `LqUcsD1cyA`
- ⛔ **`logs/` 会被清空（2026-08-12 实测：707 份本地日志变 0 份）** ⇒ 它是**缓存不是档案**。
  ⚠️ **而这个目录里混装了两种东西，criticality 与恢复成本完全不同 —— 清空时才发现**：

  | 内容 | 谁依赖 | 被清后的后果 | 恢复方式 | 成本 |
  |---|---|---|---|---|
  | `logs/gr_data/{full,user}/` | **`sim/validate.py`**（模拟器保真度硬门） | ⛔ **`hard_pass` 变 False，4/12 检查报错** ⇒ **保真度门直接失效，任何 sim 结论无法验收** | `scp Ubiquant220@…:/share/data.tar.gz` 解压到 `logs/gr_data/` | **一次 scp、13MB** |
  | `logs/game_*.log` | 各分析脚本（逐轮重算） | 逐轮结论无法复核 | `GET /api/user/get_game_log`，参数名 **`id`** | 每局一次、零配额 |

  ⇒ **`gr_data` 必须立刻恢复（已于 2026-08-12 恢复，`hard_pass` 回到 True、12/12 绿）**；`game_*.log` **按需重下即可**（全量几百次下载不值得）。
  > ⭐⭐ **通则：一个被当作"缓存"的目录里可能混着一件"没有它验收门就不工作"的东西。**
  > 判断一个目录能不能被清，不能看它的名字或你对它的用途印象，**要看谁在读它** —— 本例中 `logs/` 听起来纯属产物缓存，实际扣着**唯一的保真度门**。
  > ⭐ **并因此立一条落库要求：任何逐轮/逐局结论在落库时必须同址列出它依赖的原始 `gid`**，
  > 否则后人知道结论、却不知道要重下哪些局才能复核它 ⇒ **结论变成不可重测。**
  > （已落库的结论在 CHANGELOG；战役 JSON 在 git 历史。受影响的是**未来任何想重算的人**。）
- 开发/编译机（只能 SSH，免密已配好）：`ssh Ubiquant220@8.153.76.120`（密码同 KEY）
- 开发机 GCC 14.3 / EPYC Zen4（AVX-512 全套）/ 与评测机同构；公测全量日志 `/share/data.tar.gz`
- 每日配额 500 局（**新加坡时区 UTC+8 午夜重置** = UTC 16:00 日界）。
  **探测自由，动用须所有者批准。** 不是「剩余 20 局冻结」。规程 `docs/QUOTA.md`。
  已用数**禁止心算、也禁止数对局列表**——平台自己就给权威值：
  `GET /api/user/get_user_info` 的 **`today_initiated` / `daily_initiate_limit`**
  （CLI：`python3 tools/arena.py quota`）。8.10 曾测 `470 / 500`，那是当日快照不是常数。
  数列表会**多算**，因为列表含别人挑战我方防守位的局而那不吃配额（`FAQ:366`）：
  同一时刻列表 498 条 vs `today_initiated` 470，差 28 条正是防守局。
  8.9 曾按"下修 15%"的经验法则估，8.10 已由该字段取代；任何工具都必须读字段，
  不得再数行（`sim/field_sample.py::quota_state` 是范例）

标准流程（**rsync 必须从仓库根发起**，cwd 在子目录会报 (l)stat 错——历史踩坑×7）：
```bash
rsync -a src/ Ubiquant220@8.153.76.120:~/goldrush/src/
ssh Ubiquant220@8.153.76.120 'cd ~/goldrush/src && g++ -std=c++17 -O3 -march=native -fPIC -Wall -Wextra -shared -o player.so player.cpp'
./tools/arena.py submit --map 1 --vs <对手id> <本地.so>:<名字>   # 或双 .so 自约局
./tools/arena.py watch <gid> && ./tools/gamelog.py logs/game_<gid>.log
```

## 3. 规则速查（公测值，正赛可能变）

- 每轮：刷金/弹 → 双方同快照决策 → **耗时短者先动 → NPC 动 → 耗时长者动**（NPC 一局吃 **5216-7051** 金——旧记的 800-1300 系视角日志漏报致的 5 倍低估，已由全信息日志证伪；NPC 是金币池最大消费者，慢方吃第三道）。
- 输出 9 元：`[a0..a5, k, order, vp]`；0上1下2左3右4停；两角色共 6 步由 k 切分。
- 拾取：移动进入拿 65%（向上取整），35% 留原地可重复吃；脚下新生成的吃不到（先离开再进）。
- 炸弹：踩中烧持金 10% 后消失；每 20 轮刷一波（**实测是整套重采样：旧弹清掉再重撒，不是叠加**）；持金 0 踩弹零成本（穷单位弹透明合法）。金弹不共存。
- 金币生成（**实测机制详见 `sim/GENERATION.md`**）：一局约 9600 金，**中央 ≈4750 / 外圈 ≈4820 几乎五五分**（旧估「中央 3250 / 外圈 2000-2900」偏低）。中央每轮 1.744 格、金额 Uniform{1..10}、位置**向心加权**（最内圈单格频次是最外圈 3.4×）；外圈每 8-16 轮一次事件，**等概率挑 1 条臂、在其 5 个固定热点格撒约 100 金**（单格中位 21），日志行 2 的 token `2` 就是这 20 个热点（**不是炸弹刷新位**）。生成绝不落在角色脚下与墙上，可叠加到已有金币格。
- 视野默认 5×5；vp=1→7×7 花 2，vp=2→9×9 花 3，下轮生效管一轮。
- 判负红线：非法输出 / 未捕获异常 / 单轮 300ms 累计扣穿 60s。`moveDecision` 必须 try/catch。
- 日志逆向：`units[].actions` 是被挡后实际执行（被挡记 4）；`start.grid` 是决策输入；
  `end.dispatch_order[0]` 给出先手方；对手位置视角过滤但逐轮 gold/cost 可见——对手解剖主原料。

## 4. 纪律

- **军规 0：优化目标必须先对齐排名公式。** 初赛是**引擎自动汇总代码、全队循环赛、按胜率排名、
  前 16 晋级**（`docs/赛制介绍.md:33-39`）。故任一对手对我方胜率的贡献上限 = `1/队数`。
  全场 ≥117 队 ⇒ **T-1 + Tundra 合计上限只有约 1.7 个百分点**。两晚把全部工程量投在这两队上，
  天花板 1.7pp —— **再精密的测量都不能补救优化错的目标**。开新战役前先算「这件事最多值几 pp」。
  （注意：本条只说"投在两强上的天花板低"，**不**等于"我们差 45pp" —— 后者是混口径误判，已撤回，
  见军规 −2。我方对全场的真实胜率至今**未测量**，方案见 `docs/FIELD_SAMPLING_PLAN.md`。）
- **军规 −7：推导任何平台行为之前，先 `grep` 仓库；新经验必须写成硬规则，不写成经验法则。**
  本项目两次最高代价的失误都不是"没想到"，而是**"库里已有答案却没人查"**：
  ① `AGENT.md:120` 的公开位换代待办挂了三天 —— 代价是**整周优化成果都在用慢 18 倍的构型衡量**；
  ② 配额记账坑写在 `AGENT.md:154`（8.9 就实测过列表 295 vs 真实 254），8.10 仍连错三次
  （结转旧窗口 → 数列表得 488 → 数列表得 494），而真值 `470/500` **平台字段直接给**。
  **第二段才是本条的重点**：`:154` 当时给的是软提示「自估时按列表数下修 ~15%」——
  软提示会被忽略，硬规则不会。同一坑重犯的根因是**记法**而非**记忆**。
  ⇒ 两条可执行动作：(a) 任何"平台会怎样"的判断，先 `grep -rn` 关键词过 `AGENT.md` /
  `docs/` / `src/*.md`，**再**去拉接口或推理；(b) 每条新经验入册时自检
  **"这句话能否被当成建议忽略？"** —— 若能，改写成禁令 + 唯一正确做法 + 可复跑命令。
  正例：「**必须读 `get_user_info.today_initiated`，禁止数对局列表行**（列表含不吃配额的防守局）」。
- **军规 −6：相减之前必须确认五项同一 —— 同构型 / 同对手 / 同图 / 同顺序 / 同窗口。**
  今日九次定价事故的**共同形状只有一个**：在未确认五项之前就把两个数相减。逐项判例：
  ① **同构型**：`OPPONENTS.md` 我方侧是 102 构型混合体；Ausdroid 2胜25负是 25 个前代构型；
  榜显 23.14% 的防守位是慢 18 倍的旧构型。② **同对手**：23.14%（91% 只打两强）vs 68.69%
  （对混合场）。③ **同图**：同一队命中率跨图可差 20pp。④ **同顺序**：A>0 与我方 f≈1 的
  Ausdroid 分解都是「我方先手 vs 对手后手」，**同序下对手两种条件都赢我方**。
  ⑤ **同窗口**：`win_rate` 是 24h 滚动；`−35.4±45.5` 是跨窗基线（军规 21）。
  **规则：任何差值/比值在报出前，先写出这五项各是什么；缺一项即标「不可比」而不是给数。**
  **算术闭合只证明自洽，不证明条件可比** —— 三分解"闭合得很漂亮"正是最容易骗过审阅的形态。
- **军规 −5：结论正确但依据错误，比结论错误更难修复。**
  因为它会在未来某个时刻被**正确地**推翻，连正确的结论一起丢掉。
  判例：**「避弹器官不可删」（主张 A）结论成立**，但原依据（Ausdroid 烧损差 +145 金）是顺序
  混淆的；已重锚到同种子配对消融 `+344/+374/+212`（同构型自身带/不带该器官，不跨队、不涉顺序）。
  ⚠ 措辞纠正：旧写法「避弹**护城河**」把 A 与**主张 B**（我方比全场烧得少）混为一体 ——
  **B 至今未成立**，两个跨队样本方向相反（Ausdroid 对手高 3.0× / 对局 `185976` 我方高 55%）。
  这是同一条军规的二阶应用：**不只依据要能承重，措辞也不能替结论多说一层。**
  **规则：发现依据有缺陷时，不要因为"结论反正是对的"而留着它 —— 立刻换成能承重的证据，
  并把旧依据显式降级为「一致但不构成证据」且注明原因。**
- **军规 −4：批量操作全量失败时，先证伪自己的调用，再断言对方的行为。**
  判例：批量拉 133 局被动局日志 **132/132 全 404**，据此连推三条结论——「被动局日志不可得」、
  「改用自选主动局替代」、「日志保留期 <1 小时」。**三条全错**：真因是参数名写成 `game_id`，
  而 `tools/arena.py:cmd_log` 用的是 **`id`**；改对后 132/132 全成功。
  该次还叠加了第二个缺陷：二分搜索在**无任何元素满足条件**时会收敛到最后一个下标并
  **把它当边界打印、从未验证该元素本身满足条件**，凭空造出一个「保留边界」，
  使错误看起来有数据支撑。**规则：① 全量失败先自查调用（参数名/鉴权/端点），
  ② 搜索类代码必须在返回前验证所找到的解真的满足条件，③ 「100% 失败」与「100% 成功」
  一样是可疑信号，都要求先做不变量检查。**
- **军规 −3：抽样精度由「队间离散度」定价，不由局数定价。** 初赛要估的是**对各队胜率的队均值**，
  不是单一比例。用可挑战队自身胜率的离散度（sd ≈ 0.189）算：**±5pp 需要 56/88 支队**（几乎全打）、
  ±7pp 需 29 队、±10pp 需 14 队。而作为每局比例只需 385/196/97 局 —— **两个口径差一个量级**。
  ⇒ **「多跑局数就能提精度」对队均值是错的**：每队多打 10 局几乎不降误差，多打 10 支队才降。
  ⇒ 因此**分层是必需而非优化**；也解释了历史错误：只抽最强队 = 抽中方差最大且最不代表全场的一层。
  规则：任何"对全场/对某类对手"的估计，**先写清估计的是队均值还是局比例**，再按对应公式定样本；
  抽样名单**必须先落盘再开跑，事后不得替换**（否则等于事后挑对手）。方案见 `docs/FIELD_SAMPLING_PLAN.md`。
- **军规 −2：排名类指标必须先查「统计窗口」与「样本构成」，再与他队比较。**
  平台机制（所有者提供）：**`win_rate` = 24 小时滚动窗口；`P90` = 开测以来累计。**
  已独立验证：最近 24h 对外局 375 局 87 胜 = **0.2320**，与榜显 0.2314 吻合；全时段 0.2876 对不上。
  而该窗口里 **47% 是自博弈**（含**故意会输**的 `probeobs`），对外局中 **91% 只打 T-1 与 Tundra**。
  ⇒ 榜显值主要反映**「我们选了谁打」**，不反映实力；**它与他队对混合场的胜率不可相减**。
  判例：曾据此得出「落后晋级线 45pp」并要求重排整周优先级，**该结论已撤回**。
  **规则：引用任何榜显/排名数字前，必须写明统计窗口、样本量、对手构成与我方构型构成；
  四项缺一即不得用于决策。** 自发起对局会进入自己的窗口统计，**故意会输的探针局必须计入这笔账**。
- **军规 −1：任何"我方"聚合数字必须先按构型分层。** 判例三连：① `OPPONENTS.md §4.1` 的
  「我方暴击 15.2%」是 102 个构型的混合体，据此提出的整条假设被推翻；② 榜显胜率 23.14% 是
  **公测天梯**数字，且被我方 684 局自发起挑战（对最强两队、多为实验构型，胜率 24.4%）严重稀释，
  而被动局是 **51.1%**（68/133）；③ 对 Ausdroid 的 2胜25负**全部来自 25 个前代构型**，
  **冻结谱系 36 局只打过 T-1 与 Tundra，从未打过任何中游队**。
  **规则：报「我方 X」必须写清是哪个构型族、多少局；混合体数字一律不得进入决策。**
- **上平台前的强制门（判例：10/10 判负全是我方实验构型，6×SIGSEGV + 3×越界 + 1×Intel 误编 SIGILL；
  而判负一局的排名代价与赢一局完全对称）**：
  1. `tests/verify_construct.sh` **EXIT=0**（mod64/FP16/SHA 登记/三图 `pair_diff`）
  2. **陌生图合法性审计 ≥10 图 × 3 seed 零非法零崩溃**
     （`python3 sim/audit_unknown_maps.py --so <so> --seeds 0 1 2`）——
     `pair_diff` 只覆盖 3 局 1500 轮，**远不足以证明不会崩**；陌生图审计是约 2 万轮
  3. 产物必须在赛事机 `quant-compiler` 构建，且 SHA 已登记进 CHANGELOG 现役档案
- **公开位（`stage=4`）就是他人挑战时的应战代码，也极可能是初赛引擎取用的代码**
  （`赛制介绍.md:27,33`；`FAQ.md:366` 被挑战不占配额）。`add_model_4` 是 **upsert，每队只有一个位**
  （实证：105 模型 / 105 distinct user_id / 84 队重发过仍各一行 / 我方重发后同 `id`、
  `created_at` 不变、`updated_at` 前进）。**因此换代无需删旧，且"引擎取哪个"的歧义无害。**
  **推论：换代后每一局被动局都是对现役构型的免费采样** —— 用 `sim/ladder_ledger.py` 记账，
  必须把换代后新增局与累计值**分开报**，并优先用「同一对手换代前后」的配对口径。
- **改过 `decide` / `moveDecision` 之后，先跑护栏再谈别的**：
  `ssh Ubiquant220@8.153.76.120 'cd ~/goldrush && ./tests/verify_construct.sh --baseline-so /tmp/base.so'`
  （断言入口 mod64==0x10、FP16==0、SHA 与 CHANGELOG 登记一致、三图 pair_diff 0/500；
  退出码 = 失败项数）。`src/player.cpp` 里的 `asm(".space 48, 0x90")` 死垫把入口锁在唯一
  已证最优的 mod64 档，**垫长与 `decide` 体积耦合**；改 `decide` 不重算垫长会白付 11.67ns
  ≈ −128 金，而 `pair_diff`/体积/指令数**全都不会告警**。
- 成本与验收协议全在 **INFRA.md**：等价重构零局数，**验收量是 perf `cycles`（门 2′）而不是指令数**
  ——指令数下降 −42 而 cycles +15~18 的判例见 CHANGELOG 军规 30；行为变更 ≥3 对同窗批量，
  里程碑 6+6；单局 A/B 无判别力。**自博弈延迟 A/B 必须成对换座**（槽位效应 P90 ±20ns，
  与真实回归同阶，军规 32）。
- 场次花在硬对手与探针上；纯等价改动禁止烧局。负结果照常入册（CHANGELOG 军规节）。
- 陪练基线 `~/goldrush/cpp1.so`；指令基线 `~/goldrush/ptrivial.so`（均在开发机）。

## 5. 开题清单（算法/cost tradeoff 阶段，按期望值排序）

1. **堆记忆**：v42 的 16 槽矿堆缓存按 INFRA 预算移植（估 +60-100 指令 ≈ +15-20ns，仍压 T-1）——
   盲轮 24% 有处可去。注意 8.8 判负的前提（当时是后手吃残羹）已翻转，值得重开。
2. **深耕驻留**：酱味范本——堆群区半径 ≤3 蹲守吃干（大暴击 47 vs 我方 17 的直接答案）。
   同前提翻转，重开环内金额/驻留课题。
3. **宽域清扫路径**：替代固定锚点的街区清空-转移循环。
4. 视野触发器（历史两版全哑，矿堆喂太忙——如今先手吃头道，前提也变了）。
5. ~~天梯防守位换代（现挂 cpp27b 已三世代落后）。~~ **8.12 过时**：公开位现挂 `idle.so`，见 `src/INFRA.md` D.16。

## 6. 仓库结构与军规

```
AGENT.md          本手册
HANDOFF.md        规则事实与工具用法
README.md         一页地图
src/player.cpp    现役冠军·全能军队（头注释=算法流程文档）
src/chv.cpp       现役第二构型·ChV 路径引擎
src/game_api.h    官方接口头
src/INFRA.md      平台成本模型
src/CHANGELOG.md  版本史+军规+策略遗产
sim/              本地模拟器核心（cli/engine/validate/maps；战役报告不入库）
tests/            pair_diff · verify_construct.sh · 延迟台架
tools/            arena.py(含 quota) gamelog.py batch.sh behav.py
docs/             官方赛制 + PRELIM_RULES + QUOTA
logs/             对局日志（gitignored）
```

**结构军规**（所有者定的，别破）：
1. 不设 archive 目录；不设多版本目录——旧物只活在 git 历史 + CHANGELOG 谱系表。
   **战役报告（`sim/reports/`）、一次性分析脚本、`.so` 二进制不入库**；
   `.so` 身份 = `INFRA` D.17 的 sha256；回滚判据 = D.19。
   `validate.py` 仍可把派生 JSON 写到 gitignore 的 `sim/reports/`。
   **唯一例外（8.10 所有者钦定）：src 开双构型 player.cpp + chv.cpp，两个都是现役，
   都受军规 2 约束（改动同步头注释与 CHANGELOG）。**
2. 改 `player.cpp` 算法 → 同步其头注释与 CHANGELOG；平台物理新证据 → 更新 INFRA.md。
3. 三张官方图墙表已全烘焙（`BAKED_W[3]`，运行时指纹自动锁图；map2/3 锚点自动修正）。
   正赛出现**新图**也能跑（陌生图懒学习兜底，实测静轮 ~+0-5ns/学习轮 +25-40ns，
   平台同窗 P50 +20ns），但拿到新图日志后应尽快把墙表入册换回 +0ns
   （方法注在 player.cpp `BAKED_W` 旁）。**初赛换图假设下，懒学习是主形态、
   烘焙只是可选加速层——见 §0.1，一切新器官必须先过陌生图形态关。**
