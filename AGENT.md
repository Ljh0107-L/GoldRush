# AGENT.md — GoldRush 2.0 作战手册

> 给接手本赛题的 AI Agent。人类已把赛题全权委托给 Agent；**对外动作（publish / 联系主办方 / 动用配额跑批）须所有者批准或明示授权**。
> 三份核心文档分工：本手册（怎么操作）· `HANDOFF.md`（规则事实与工具）· `src/CHANGELOG.md`（版本史 + 全部判负 + 军规档案）。历史逐日战报在 git 历史。
> 本手册于 2026-08-13 整编：已解决的战役记录移入 CHANGELOG / git 历史，此处只留**现行有效**的操作规程。旧版全文 `git show a08cbd4:AGENT.md`。

## ⛔ 机器分工是硬规则（所有者钦定）

- **延迟无关的 sim 一律在 `i5` 跑**（`ssh i5`，EPYC 9754）：金币/胜负、行为回放、陌生图审计、配对 A/B、批量种子。
- **延迟相关工作一律在 Gold 跑**：交付 `.so` 构建、mod64/FP16、instructions/cycles、P50/P90 台架；Gold = `Ubiquant220@8.153.76.120`（与评测机同构 EPYC Zen4，GCC 14.3）。
- **本机 Mac 没有比赛运行环境**：不得加载 Linux x86-64 `.so`；只可做源码/JSON 静态检查与内置 `stay`/`scripted` 冒烟。
- ⛔ 不得在 Gold 跑纯收益批次；不得拿 i5 的 cycles 与 Gold/平台延迟比较。每条 sim 命令先标注「延迟相关 / 延迟无关」再选机器。
- 编译器**钉死 GCC 14**（Gold 默认 `g++`）。AOCC 5.2 / GCC 15 已实测判负（AOCC 暖态全赢、平台足迹税判负），死因见 CHANGELOG「编译器三方对照」。

## 1. 任务与现状

九坤 GoldRush 2.0：17×17 夺金，500 轮，`得分 = 毛金币 − 视野费`，每轮耗时短者先动。
目标：初赛前 16（**循环赛按胜率**，全场 ≥117 队 ⇒ 任一对手贡献上限 ≈ 0.85pp —— 军规 0）。
**关键日期**：报名 8.14 截止 · 模拟赛 8.15-16 · 初赛 8.17-21 · 决赛 9.6。
**赛制事实增补（2026-08-13 所有者口述）**：~~模拟赛仍用 map1/2/3~~ ⛔ **已被实测证伪，见下**；
**初赛跑一张地图**（身份未公布，可能官方可能新图 ⇒ 陌生图策略仍是正确对冲）；**初赛期间不允许更换 `.so`**；
**初赛\*不\*从公开位收代码（专用提交入口）** ⇒ 冻结的是专用入口那次提交，公开位与初赛脱钩。
**公开位现挂诱饵 `idle.so`**（防蒸馏，12:00:41Z 起）—— 榜显名次自此不反映战力。

**🚨 模拟赛实况（2026-08-15，赛段 5，详见 CHANGELOG 同名节）**：
- **地图换了**：`id=4 模拟赛地图` = **41.2% 墙的迷宫**，比我方原合成集最密的图再密 42%，
  已入册 `sim/maps_unknown.json::sim_contest_maze`；⇒ **初赛那张图很可能同属此形态**。
- **名次 11 / 163，胜率 85.19%**（前 16 门槛 82.72%，**余量 2.47pp**）；
  **`cost5` = 170，前 16 里最低** ⇒ 极速奖轴领先，**晋级轴才是绑定约束**。
- **48 败中 41 败给慢深搜（≥1μs），而那 41 场我方全部 500/500 先手**
  ⇒ ⭐ **速度轴对晋级已饱和；继续压 P90 只服务于极速奖。**
- 赛段接口编号见 `HANDOFF §7.0`（`get_rank_list_5` / `get_game_list_5` / `cost5`）——
  `arena.py rank` 读的是**公开位天梯**，看不到赛段成绩。

**现役构型 R15**（`src/player.cpp`，2026-08-14，`.text` sha `16be14f2…`；单构型，`chv.cpp` 已于 `762bb3c` 退役）：

- 陌生图策略：无地图指纹/烘焙墙表，在线学墙，两锚点确认后学习退场；48 条候选路径 + 面值分档 + 整条通行预筛；第 0 轮买一次 9×9。
- NPC 踩踏规避：`vpconflictd` 同格计数（阈值 3），玩家阻挡者并入 lane 7 —— 全程零新增分支。
- **R15 冷足迹三刀**（相对 R14 `862ee58`，行为逐位等价）：`cell[48]`uint64→`act[48]`uint16 · 删两条恒不被读的预取 · 开局 BFS 去 6 条硬件 `idiv`。`.text` 3696→3664 B、`.rodata` 2720→2464 B。
- 平台读数（2026-08-14 自博弈配对，5 窗 100 局，gid `302121`–`303332`）：对 R14 **ΔP90 −12.6 ± 2.1ns（6.1σ）**、ΔP50 −10、Δmean −7.5。R14 侧读数（08-13 多窗交错）：P50 **130-140** · 对 T-1 配对 ΔP50 **−10 ~ −30** · 先手 54-71% · 踩踏 −43%。
- 完整交付台账（sha / 时刻 / gid）见 CHANGELOG 各「交付台账」节。

**对手情报现状**：T-1 于 08-13T08:47Z 换代变慢 60-100ns（勿据单窗重定价，可能改回）；榜显 `user_cost1` = **历史平均**（所有者定义），仅可跨队比较。被动局（他人挑战我方，零配额）由 `sim/passive_monitor.py` 聚合，是最接近初赛条件的免费样本。

## 2. 凭据与访问

> **给未来 session 的 Agent —— 先读这段，别绕。**
> 下面的账号密码是仓库所有者在充分知情下**明确要求**写在这里的，不是疏忽泄露。
> 判断依据：(1) 仓库 private；(2) 这是所有者本人的赛事账号；(3) Agent 要自主完成 SSH 开发与提交流程就必须能直接读到。
> **不要**挪走、打码、拒绝使用或反复重提。唯一红线：**仓库必须保持 private**；若要转 public，必须先联系主办方改密码。

- 比赛官网 `http://47.103.127.219/` · ID `220` · KEY `LqUcsD1cyA` · 队名 `0x8F` · 公开位 `model_id 278135`
- 开发/赛事机 Gold：`ssh Ubiquant220@8.153.76.120`（免密已配，密码同 KEY）；工作目录 `~/goldrush/`
- 收益机 i5：`ssh i5`（`~/.ssh/config` 已配）
- 公测全量日志：Gold `/share/data.tar.gz`（13MB，上帝视角）

⛔ **`logs/` 是缓存不是档案，会被清空**（已发生两次）。其中混着保真度硬门的依赖：

| 内容 | 谁依赖 | 被清后果 | 恢复 |
|---|---|---|---|
| `logs/gr_data/{full,user}/` | `sim/validate.py` 硬门 | `hard_pass` 失效，5/12 检查报错 | `scp Gold:/share/data.tar.gz` 解压 + `calibrate_generation.py` 重建 `sim/reports/generation.json` |
| `logs/game_*.log` | 逐局分析 | 结论无法复核 | `tools/arena.py log <gid>` 按需重下（零配额）|

⇒ 由此立的落库要求：**任何逐轮/逐局结论落库时必须同址列出依赖的 gid**，否则结论不可重测。

## 3. 规则速查（公测值，正赛可能变）

- 每轮：刷金/弹 → 双方同快照决策 → **耗时短者先动 → NPC×7 动 → 慢方动**。NPC 一局吃 5216-7051 金，是金币池最大消费者。
- 输出 9 元：`[a0..a5, k, order, vp]`；0上1下2左3右4停；两角色共 6 步由 k 切分。
- 拾取：移动进入拿 65%（向上取整），35% 留原地可重复吃；脚下新生成的吃不到。
- 炸弹：踩中烧持金 10%；每 20 轮整套重采样；持金 0 踩弹零成本。**3 个 NPC 同格 = 踩踏罚金**（`trample_events`，实测 `npc_count` 恒 3）。
- 生成：一局约 9600 金，中央/外圈约五五分；中央向心加权、外圈每 8-16 轮挑 1 臂在 5 个热点撒约 100 金。机制实测 `sim/GENERATION.md`。
- 视野默认 5×5；vp=1→7×7 花 2，vp=2→9×9 花 3，下轮生效管一轮。
- 判负红线：非法输出 / 未捕获异常 / 单轮 300ms + 总池 60s 扣穿。`moveDecision` 必须 try/catch。
- 每日配额 **500 局**（仅我方发起的计入），北京午夜 = UTC 16:00 重置。**读 `today_initiated`，禁止数对局列表**（列表含被动局，曾差 28 局）。规程 `docs/QUOTA.md`。
- 极速优化奖 = 初赛前 16 中 **P90** 最低（限 C++，前三除外）。
- **初赛期间不允许更换 `.so`；初赛不从公开位收代码，另有专用提交入口（2026-08-13 所有者口述，权威）**
  ⇒ 「公开位保持最佳交付物」教义**作废**；公开位挂诱饵 `idle.so`，真武器由所有者经专用入口在冻结前提交。

## 4. 军规（现行有效；判例正文在 CHANGELOG，标 ⭐ 的是今天仍在赚钱的）

**目标与统计**
1. **军规 0**：优化目标先对齐排名公式 —— 循环赛胜率，单对手贡献上限 1/N ⇒ 开战役前先算「最多值几 pp」。
2. ⭐ 凡标「现役/当前」的段落必须带**构型 hash + 测量日期**，缺一不得用于定价。
3. ⭐ 结论与其依赖的 **gid 同址**落库；「相减前确认六位口径」= 座位/窗口/视野/构型/平台/生成器。
4. 合池先分池（方向 = 占比大的那半）；主动/被动通道名单**不得合并**；裁决器先用零信号干跑（**覆盖选样**，七向输入）。
5. **交付偏倚防线**：判据**预注册**，事后不豁免；交付前把关键数换**最不利口径**重算一次，翻转即不成立。

**延迟物理（本平台实测定律）**
6. ⭐ **冷足迹 > 指令数**（三判例：B_cur / nc / AOCC）——`.text`+`.rodata` 缓存行数与分支计数是一等成本。
7. ⭐ **分支冷税**：平台每轮清空预测器，数据相关分支 ~20cy/次；GCC 的 if-conversion **形态敏感**，等价重写必查 `objdump` 分支计数。
8. ⭐ **平台的冷 = L1/L2 冷、L3 温**（约 6KB 热表驻留 L3）⇒「冷态内存往返」类收益假设先按 L3 命中重定价。
9. k 域运算（`kshift/kor`）在 Zen4 慢；OoO 窗口盖得住的「源码级串行」不是优化靶子（cmov 链下载入已被硬件并行）。
10. 暖态台架**双向失明**（假阳性：PGO/B_cur；假阴性：入口预取块）⇒ **平台内窗交错 = 唯一权威**：base/候选**交替提交**、同对手同图、**逐局串行**（并发放大自测 4.3-4.7×）。跨窗绝对值不可比，小 Δ 可迁移；平台 cost 均为 10ns 整数倍。

**度量口径**
11. `user_cost1` = **历史平均**（所有者定义）：跨队比较 ✅，拿本地 P90 对它验收 ⛔，publish 后重读是唯一验证。
12. `pair_diff 0/500` 是「已记录分布上无分歧」的**证据**，不是等价性**证明** ⇒ 等价声明须附**定义域论证**；SIMD 改动加一道「强制标量 vs SIMD」交叉对账（permute 索引 bug 判例：三图只有一图显影 2/500）。
13. margin = 双方净分差，永不用单侧 `net`；任何胜率/收入统计剔除对手终局净分为负的故障局（`field_sample.is_contest`）。

**交付**
14. 改 `player.cpp` 必过五门：赛事机构建零告警 · 入口 `mod64 = 0x10`（垫与体积耦合，改后必查 `nm`）· FP16 = 0 · `pair_diff`×三图 · 陌生图形态关（i5，`ALERTS: none`）。
15. ⭐ **落库 ≠ 交付**：不 publish，增益对名次贡献严格为 0。publish 后验：`model_id` 仍 278135（条目数 = 1）· `updated_at` 已更新 · 榜显锚点前后同址记录。**publish 是单向通道**（取不回、无摘要）⇒ 交付台账（时刻 / 产物 sha / `.text` sha / 源码 commit）落 CHANGELOG。
16. 跨机对账用 **`.text` sha256**（源码改名会换 `.so` sha —— `-frandom-seed` 取自文件名）。

## 5. 标准交付工作流（今天全天实跑过 N 遍的管线）

```bash
# 1. 改动 → Gold 构建(唯一构建命令)
rsync -a src/ Ubiquant220@8.153.76.120:~/goldrush/src/
ssh Ubiquant220@8.153.76.120 'cd ~/goldrush/src && g++ -std=c++17 -O3 -march=native -fPIC -Wall -Wextra -shared -o player.so player.cpp'

# 2. 构型门(Gold): mod64/FP16/尺寸/sha; 垫公式 pad ≡ (32−X) mod 64, 一次到位
# 3. 行为对账(Gold): tests/pair_diff.py 新旧 .so × 三图日志; SIMD 改动加 -mno-avx512f 交叉版
# 4. 成本筛查(Gold): tests/icount.cpp 同流 instructions + cycles(ABAB 轮换消位置偏倚), objdump 分支计数
# 5. 形态关(i5): sim/audit_unknown_maps.py --so … ⇒ ALERTS: none
# 6. 平台内窗交错(唯一权威): 12 局起步, 预注册判据, 逐局串行
# 7. 赢了 → 落库(军规 2/3) → publish → 后验 → 交付台账 → push
```

配套工具：驱动脚本模板见 git 历史（session scratchpad 的 `interleave.py`/`run_t1_batch.py` 形态，要点：逐局串行、`socket.setdefaulttimeout(30)`、平台计数核对而非本地数数）。

## 6. 仓库结构与军规

```
src/player.cpp    现役唯一构型(头注释=算法流程)   src/INFRA.md   成本模型
src/CHANGELOG.md  版本史+判负+军规档案            src/game_api.h 官方接口头
sim/  tests/  tools/  docs/                       (见 README 仓库地图)
```

**结构军规**（所有者定的，别破）：
1. 不设 archive 目录；旧物只活在 **git 历史 + CHANGELOG 谱系表**（取用命令随谱系表给出）。战役报告、一次性分析脚本、`.so` 二进制不入库；`validate.py` 可写 gitignore 的 `sim/reports/`。
2. 改 `player.cpp` 算法 → 同步其头注释与 CHANGELOG；平台物理新证据 → 更新 INFRA.md。
3. 所有图按陌生图处理（`dbdcbdb` 起）—— 不存在「把新图墙表入册」这条跑道，勿重开。

## 🎯 延迟靶子:开局段(活项 —— 与 `sim/remeasure_opening.py` 同址互指)

**我方真实 P90 220 vs T-1 200 ⇒ 差 30ns。剔除重算定价(随机对照 400 次全部 +0.00):**
| 剔掉 | n | **ΔP90** | **Δ均值** | 已建成修法可跳 |
|---|---:|---:|---:|---:|
| **r0–7**(全图 BFS `round>=8` 早返 · State 重置 · 冷 icache/PLT/BTB) | 296 | **+10** | **+24.0** | ⛔ 18.1% |
| **r8–24**(5×5 扫描,触发率≈100%) | 629 | **+20** | +5.4 | ✅ 73.2% |
| r0–24(整段) | 925 | **+30** | **+30.4** | — |

⭐ **两段在两个统计量上近乎互补** ⇒ **P90 计分攻 r8–24 · 均值计分攻 r0–7**;而**整段在两者都值 30ns** ⇒ **修整段是唯一不依赖「初赛按哪个统计量计分」这个未知的动作。**
⭐ **存在性证明**:T-1 开局 **1.03×** 自身稳态、Tundra **1.00×**,我方 **2.43×** ⇒ **两家几乎不付开局代价。**
⛔ **不要用「20 × 73.2%」推算修法价值** —— 占比推断不适用于分位数。可辩护的只有界 **ΔP90 ∈ [0, 20]ns**;真值须在**打了补丁的构型**上重测,仪器已 armed:**`sim/remeasure_opening.py`**(零配额、纪元边界实时读、样本不足退出码 3)。**前置 = publish(需所有者亲批)⇒ 此项未关闭。**
⇒ 推导与全部原始 gid 见 commit `ff229d8` · `eee8497` · `7f3abef`。

## 🚨 伤害探测:截断会把胜局变成败局,而它对错误字段完全隐形(活项,cron 依赖)

**判例 `235745`**(map2,2026-08-12):日志止于 **round 330/500**,我方净分 **1702 vs 827(+875 领先)**,而平台 `is_win=0`。
⇒ ⛔ **`error_msg` 空 · `is_parse_log=1` · `is_upload_log=1` ⇒ 平台错误面完全静默**;`is_contest()` 查**对手**净分,抓不到**我方**故障。
⇒ **签名判别(仅在已知截断的条件下有效)**:末轮 cost **150ns = 该局 P50 的 1.07×**、末 50 轮无攀升(1.03×)、峰值 5510ns 在 **r0** ⇒ **崩溃/平台事件,不是超时**;97 局中末轮/P50 最大比 **7.79× 且该局跑完** ⇒ **无近失人群、无超时机制证据。**
⇒ **基线(零下载,全 604 局)**:**`is_win=0` 而我方 `coin_num` > 对手 ⇒ 1/604 = 0.17%**(镜像情形亦 1)。⚠️ 该扫描对「截断总数」是下界,对「造成伤害的截断」是精确值。
> ⭐ **两条判据必须并存**:**D = 末轮 < 499**(抓截断)· **E = `is_win=0` 且我方 `coin_num` > 对手**(抓**造成损害的**截断,零下载可回溯全库)。
> ⛔ **而这两条的定义此前只活在 cron 提示里(`~/.maso/cron/jobs.json`,git 之外、不可 diff)** ⇒ 本节即其可入库副本。**触发器在 ≠ 理由在。**

## ⛔ 文档压缩类 commit 的前置门

**落地前必须先按\*数字\*扫一遍 diff**:被删掉的每一个数字,若**没有任何其它已入库文件**含它,必须**要么搬走、要么在 commit message 里明确宣告退役**。
⇒ 判例:`a6361af` 把 AGENT.md 从 1274 行压到 118 行,`0fe9264` 删掉三个脚本 —— 其中 **`remeasure_opening.py` 是 armed 待发**,被归类为「已完成的一次性工具」⇒ **归错类别比没被发现更危险,因为「已归档」让人以为它已被处理。**
⇒ ⚠️ **而按数字扫描本身也有射程**:它的语料是 **git 工作树** ⇒ 只活在 cron 提示等**未入库**载体里的判据扫不到(D/E 就是这样漏掉的)。**仪器对了,语料窄了。**

## ⛔⛔ 审探测器时先问「它**不响**的时候有几条路径」

**2026-08-13 一天内第 4 次:每一个新发现的缺陷都朝「对我方乐观」那一侧失效。**
| 缺陷 | 沉默机制 |
|---|---|
| `keep_game` 丢掉 `error_msg` 非空的局 | 出错局**消失** ⇒ 存活样本偏健康 |
| 诱饵谓词把「我方崩了」判成 `alert=False` | **灾难发生的那一刻探测器把自己静音** |
| `len(scores) < 5 → return False` | 样本不足时**默认判无事** |
| 平台坏槽位计为我方胜 | 虚增胜率 |

⇒ ⭐ **原因是结构性的、不是巧合:这些制品都是\*探测器\*,而探测器的失效模式是\*沉默\*,而沉默被读作好消息。**
> ## ⇒ **可执行动作:审任何探测器,先枚举「它不响的所有路径」,再问「它响得准不准」。**
> 每一条不响的路径都要问:**这条路径是「真的没事」还是「我看不见」?** 后者必须升级为告警或第三态,**不得与前者共用 `alert=False`**。

⭐ **配套的默认值规则**:**一个歧义状态,若它的两个原因要求\*相反\*的反应,`alert=False` 就是错的默认值。** 按**代价**选失效方向 —— 判例:误报一次 `CATASTROPHE` 的代价是**一次人工核查**,误报一次「诱饵,没事」的代价是**整个回滚窗口**。

## ⛔ 「N 个格的极大值」不是统计量 —— 用逐格分布或秩

`max` 取自一个**格数会变**的集合 ⇒ 它对「哪些格存活」极度敏感,N 变则 max 变,**与被测效应无关**。
**2026-08-13 同一天两次,方向相反**:
| 用法 | 后果 |
|---|---|
| `真值 / max\|安慰剂\|`（当分离度） | 拿一个值比 N 个值的极大值 = 极值统计 ⇒ **把结论报低了**（1.05× 而真相是「每个带宽都排第 1，p≈0.03」） |
| `max\|安慰剂\|` 折半前后之比（当噪声标度检验） | 折半只保留 89–91% 的格 ⇒ 比值 **0.87×** 而纯噪声应 1.41× ⇒ **险些据此宣布「systematic 成分存在、目标不可达」** |
⇒ ✅ **两次的解药相同**：**配对同一格集比较逐格分布**（实测 1.85×，噪声预期 1.41× ⇒ 安慰剂确是噪声）。
> ## ⇒ **可执行动作：任何以 `max` 为分母或为比较对象的量，先问「N 是否固定」。N 不固定就换成秩、或换成配对同集的逐格分布。**

## 🚨 回滚就绪:**演练已做,能力确认** —— 重建 3.8 秒、sha 逐字节复现(2026-08-13,未 publish)

### ✅ 结论先行:**D.17 与 D.18 不矛盾** —— 「随时可执行」在「工作树不存 `.so`」下仍成立
| 制品 | 源 | 构建墙钟 | 字节 | 整文件 sha256 | 判 |
|---|---|---:|---:|---|---|
| **`base_ref.so`**(回滚锚点) | `fd47ea6` | **0.617 s** | 20768 | `f66471636a528d33…1c7bd` | ✅ **逐字节复现** |
| `landed.so` | `aa479d7` | **0.648 s** | 20520 | `06477c516b7214b4…0e810` | ✅ **逐字节复现** |
> ## **端到端应急耗时(git 取源 + tar + scp 上 + 构建 + scp 回)= 3.8 秒**,取回件 sha 复核一致。
⇒ ⭐ **所以「回滚零配额、随时可执行」从断言变成了读数**;`INFRA` D.29 预言的「应急路径文档债在需要它的那一刻结算」**这次没有发生**,因为重建代价可忽略。
⚠️ **唯一残余依赖:开发机 `Ubiquant220@8.153.76.120` 必须可达。** SSH 不通 ⇒ 无重建路径 ⇒ 这是这条链上真正的单点。
⚠️ 复现条件(缺一即可能不复现):**同机 `quant-compiler`(EPYC 9T25/Zen4)· g++ 14.3.1 · 命令与\*路径\*完全一致 `src/player.cpp`**(`-frandom-seed` 默认取自源文件名 ⇒ 改名或改相对路径会换 sha)。
✅ **构建产物已删除,未留在工作树**(D.17)。⛔ **我此前把 `.so` 恢复进工作树并提议强制跟踪,是对 D.17 的偏离,已撤销。**

### ⛔ 而我那次偏离本身是一个判例:**模式匹配的假阳性**
我找到一个真错型(「活的东西被归类成非活代码」,`0fe9264` 删掉 armed 脚本确实属此型),**然后把它套到 `.so` 上** —— 而删 `.so` 是在执行 **D.17 明文政策**,不是事故。
> ## ⇒ **规则第一次用到新类型对象上时必须先问适用域(`INFRA` D.28)。⭐ 而找到模式的人最不容易怀疑它。**

### ⛔ 回滚目标:两份文档在答**两个不同问题**,而真正的洞是**它从未被预登记**
`INFRA` D.17 表标 `base_ref.so` = **锚点身份**;上位者那张表排的是**损失最小顺序**(先退最近已知良好版,再退锚点)⇒ **不是「记忆读错」,是两份文档答两个问题。**
⇒ ⛔ **但 D.19(预登记、事后不可改)只登记了触发 A / C 与分母 18,\*完全没有指定回滚到哪个制品\*** ⇒ **「退到哪一个」是一处从未被预登记的空白。**

### ✅ 回滚生效的验证判据 —— **现在预登记(publish 之前写死)**
**① 各候选制品的期望行为签名**(平台逐轮 cost,仅串行被动局,r≥4)
| 制品 | 期望 per-game P50 | 期望自得分 |
|---|---|---|
| **`base_ref.so`** | **175–215ns**(档案 190–210) | 正常(≫300) |
| 现役 / `landed.so` | **125–150ns**(实测中位 130,range 130–140,**sd 4.5**,n=5) | 正常(中位 2168) |
| `idle.so` | ~40ns / 全 STAY | **≈0** |
⇒ **分离度:190 vs 130 = 60ns,per-game sd 4.5(n=5)⇒ **≥4.6 sd** ⇒ 单局即可判别。**
⚠️ ⛔ **不得写「13 sd」**:`sd=4.5` 出自 **n=5**,4 自由度下 σ 的 95% 区间约 **[2.7, 12.9]** ⇒ **悲观端只有 ≈4.6 sd**。结论不变(悲观端仍单局可判),**但精度按悲观端写**。
**② 判定所需最小被动局数 = 3 局**(单局已足 ≥4.6 sd,取 3 局以防单局异常)
**③ 升级为「回滚未生效」的时限 = 60 分钟**
⇒ 实测被动局到达率 **20.1 局/小时**(78 局 / 3.88 h,读于 2026-08-13T11:57Z)⇒ **3 局期望等待约 9 分钟** ⇒ 60 分钟是 6.7× 余量,足以吸收到达率波动。
⚠️ **到达率是对手构成量、是时间序列**,须在使用时重读;若届时到达率显著低于 20/h,时限须按比例放宽而**不是**据此判「未生效」。

### ⚠️ 座位排期那句话曾有**三个分母**(每个数必须带自己的分母)
⛔ 错:`总需 ≈245 · 已有 9 · 尚缺 ≈215` —— **`9` 是随机 50 局样本里 ZZK 的局数,属另一个语料。**
✅ 对(三数各带分母,**不跨语料相加**):
> **需 9766 带内轮**(3× 分离度)· **现有 1191 轮 / 24 局**(选择性语料 ⇒ **49.6 轮/局**)· **缺 8575 轮** ⇒ **按未来合格局 39.8 轮/局,尚需 ≈215 局**
⚠️ `1191/39.8 = 30` 是**单位换算的等效局数**,不是真实计数,**不得写成「已有 30 局」**。

### ⚠️ 这条路径上仍**从未执行过**的步骤
1. **发布 `base_ref.so` 本身** —— 公开位换过 4 次,**从未换成过回滚锚点**(需所有者亲批)
2. **上面那三条验证判据** —— 刚预登记,**从未跑过**
⇒ 可粘贴命令(已 `--dry-run` 验到最后一步之前):
```
python3 tools/arena.py publish gr_artifacts/base_ref.so:player220
```
⚠️ **但 `gr_artifacts/` 按 D.17 不存在** ⇒ 真实回滚是**两步**:先重建(3.8 s)到某个临时路径,再 publish 该路径。**单行命令必须带上重建那一步,否则它指向一个不存在的文件。**

## ⛔ 读数不得存放在预登记块内

`INFRA` **D.19** 标着「预登记且事后不可改」,而块内嵌着 **08-12 的当日实况读数**(31 局 · map1 13/13 …)。
> ## ⇒ **读数一旦放进预登记块,就继承了该块的「不可编辑」豁免 ⇒ 它\*永久失效\*又\*永久看起来权威\*。**
> ⛔ **而处置不是「搬出去」** —— 搬动需要打破「事后不可改」,而**原地\*追加\*一行不需要**。
> ## ⇒ **正确形态:已在块内的读数 → \*原地标注\*日期与失效状态(只增不删);新读数不得再写入块内。**
> ⇒ 已对 `src/INFRA.md` D.19 那段执行:追加一行 `【读数,非判据 | 读于 2026-08-12 | 已失效 —— 当期数据见 …】`,**未删除任何字**。
> ⇒ ⭐ 这样「不可改」在字面上仍然成立,而「永久看起来权威」这个毛病被治掉 —— **一个约束与一个缺陷可以同时被满足,前提是找到只增不删的那个动作。**
