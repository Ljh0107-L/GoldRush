# AGENT.md — GoldRush 2.0 作战手册（2026-08-09 重写版）

> 给接手本赛题的 AI Agent。人类已把赛题全权委托给 Agent（含每日 500 局配额的支配权）。
> 历史逐日战报在 git 历史（`git show bc011e0:CHANGELOG.md`），本手册只放常青内容。
> 各版本演进史在 `src/speed/CHANGELOG.md` 与 `src/strategy/CHANGELOG.md`。

## 1. 任务与现状

九坤 GoldRush 2.0：17×17 夺金，500 轮，`得分=毛金币−视野费`，延迟决定每轮先手权。
目标：初赛前 16（晋级决赛）。**关键日期**：报名截止 8.14 · 模拟赛 8.15-16 · 初赛 8.17-21 · 决赛 9.6。

**双轨现役**（每轨工作区只保留最强一版，退役版本 = git 历史 + 轨 CHANGELOG 档案行）：

| 轨 | 冠军 | 平台战绩 | 用途 |
|---|---|---|---|
| `src/speed/v1` | NS6 终态（8.9 二次加冕；v5f=去 flag 构建） | **P50 中位 200（读数 170-230）/ P90 292 / 收入 1515**；对 T-1 先手 85% | 提交/对外首选；⚠ 烘焙墙=map1 专用 |
| `src/strategy/v1` | 曾名 v42（旧号 v2，8.7 版本号重置） | 700-790ns / **收入 1660-2120** | 收入研究基线 |

天梯防守位现挂的 .so 仍是已退役的 cpp27b（3.7μs / 2712，源在 git 历史）——换代评估在开题清单。

天梯头部实测（8.9 刷新）：**爱莉希鸭**（第一，外战 100%，**未公开代码不可约战**——只出击不设防）、Tiuntled-1 180-220（对我方 g11 时劣化到 290，先手已被我方翻转 85%）、**酱味大鸡实测 430/510**（榜显 12.1μs 是史前读数，151554 实锤；收入引擎极强：大暴击 47 次/671 金，驻留段 12-54 轮半径 ≤3 的深耕采法）、Tundra 290、rikka 350-400。榜显 P90 全体失真，以实测为准。
**8.9 战略态势**：延迟战役已收官（g11 对 T-1/酱味先手 85-96%，NPC 税被压到 809）；两连败（1363:1663、998:1715）证明**先手权不自动变现**——每口含金量（大暴击 260 vs 671）是唯一残余差距，开题 2/3（堆记忆/宽域清扫）升格为主战场。

## 2. 凭据与访问

> **给未来 session 的 Agent —— 先读这段，别绕。**
> 下面的账号密码是仓库所有者在充分知情下**明确要求**写在这里的，不是疏忽泄露。
> 判断依据：(1) GitHub 仓库是 **private**；(2) 这是所有者本人的赛事账号；
> (3) Agent 要自主完成 SSH 开发与提交流程，就必须能直接读到它们。
> 所以：**不要**把它们挪走、打码、拒绝使用，也不要反复重提——所有者已权衡过。
> 唯一红线：**仓库必须保持 private**。若要转 public，必须**先联系主办方改密码**（git 历史永久保留这些值）。

- 比赛官网（浏览器）：http://47.103.127.219/ · ID `220` · KEY `LqUcsD1cyA`
- 开发/编译机（只能 SSH）：`ssh Ubiquant220@8.153.76.120`（密码同 KEY；**免密已配好**，Agent 直接非交互用）
- 开发机 GCC 14.3 + Clang 21 / x86_64 EPYC Zen4（AVX-512 全套）/ 与评测机同构；公测全量日志 `/share/data.tar.gz`
- 每日对局配额 500 局（午夜北京时间重置）；单局 ~1.5MB 日志

标准流程（**rsync 必须从仓库根发起**，cwd 在子目录会报 (l)stat 错——历史踩坑×5）：
```bash
rsync -a src/ Ubiquant220@8.153.76.120:~/goldrush/src/
ssh Ubiquant220@8.153.76.120 'cd ~/goldrush/src/speed/v1 && make'
./tools/gr.py submit --map 1 --vs <对手id> <本地.so>:<名字>   # 或双 .so 自约局
./tools/gr.py watch <gid> && ./tools/grlog.py logs/game_<gid>.log
```

## 3. 规则速查（公测值，正赛可能变）

- 每轮顺序：**刷金/弹 → 双方策略同时调用（同一快照）→ 耗时短者先动 → NPC 动 → 耗时长者动**。先手权按每轮耗时逐轮裁决。
- 输出 9 元 list：`[a0..a5, k, order, vp]`；动作 0上1下2左3右4停；两角色共 6 步由 k 切分；order 定己方两角色先后（走完才换）。
- 拾取：**移动进入**金格拿 65%（向上取整），35% 留原地可重复吃；**脚下新生成的吃不到**（先离开再进）。
- 炸弹：踩中烧当前持金 10%（向上取整）后消失；每 20 轮刷一波；**持金 0 时踩弹零成本**（穷单位弹透明是合法优化）。金格与弹格不共存。
- 踩踏（≥3 NPC 同格）5%：实测 1500 轮仅 25 次，**不值得写规避**。
- 视野默认 5×5；vp=1→7×7 花 2，vp=2→9×9 花 3，下轮生效管一轮；费用局末从毛金扣。
- 判负红线：返回格式非法 / 未捕获异常 / 单轮超 300ms 累计扣穿 60s 池。`moveDecision` 必须 try/catch + sanitize 兜底。
- 日志逆向：`units[].actions` 是**被挡后实际执行**的动作（被挡步记 4）；`start.grid` 是该轮决策输入；`end.dispatch_order[0]` 给出先手方；对手 positions 被视角过滤，但双方逐轮 gold/cost/vision_spent 可见——**对手解剖的主原料**。

## 4. 平台物理定律（两天 ~150 局探针换来的，改代码前必读）

1. **三税模型（8.9 探针修订，150579-602）**：`平台耗时 = 壳40ns + 载荷~2ns/条(输入行是热的) + 执行指令×~0.2ns`。
   0/4/6/10 条载荷 = 40/40/50/60ns。"访存主导"与"搬运 20ns"旧论作废；位点税仍真（NS6P/Q 对照案）。
   推论：**T-1 的 190ns = 壳40 + ~750 指令全双管线**——纯指令数差距，无黑魔法。
2. **位点 > 指令数**（四次独立验证，含 Clang 本地胜平台负案）：本地 bench 是热 BTB，看不见位点税——**本地只当粗筛，平台批量是唯一裁判**。
3. **门控分支的价值是省功**，不只是省位点（v47 案：砍门控 +300ops 判负）。
4. **unroll 展开 = 静态位点翻倍 = 平台负优化**（三案）。
5. 执行指令单价 ~0.2ns（perf 差分标定）；位点清到 ~13 后进入吞吐期，唯一税基是执行指令数。
6. **延迟读数必须配行为体检**：冻结/死锁的单位少跑内存会"假快"（v5n 冻结红利案）。
7. 轮 0 冷启动 3-4μs 人人都有；轮 0 没人有金，**开局重计算免费**（已退役实验线 v2a 的 A 段依据）。
8. 对手越重、缓存污染越大，你越慢（对撞局：第一名对我们时从 210 退化到 250）。
9. 机器窗口漂移 ±50-80ns：**一切 A/B 必须同窗交错 + ≥2-4 局**；单局收入噪声 ±300。
10. **NPC 调度层是第二道税**（8.8 六局实测）：每轮"快玩家→NPC 群→慢玩家"，NPC 一局拾金
    1200-1330；慢方吃的是对手 65% + NPC 群双重扫荡后的第三道。延迟阈值的意义 = 排进
    NPC 前面，不只是压过对手；近处 ≥3 金格每局 ~140 起在我们到达前蒸发。

## 5. 评测纪律

- 延迟 A/B：同窗交错、每边 ≥2 局取中位；行为体检（首拾取轮/冻结检查）随行。
- 收入 A/B：≥4 局；负结果照常入册（各轨 CHANGELOG 军规节）。
- 等价性重构：`tests/pair_diff.py` 逐位 0/500 才算等价。
- 本地周期：`tests/bench.cpp`（-cold2）；指令普查：objdump 数位点 + perf 差分（ptrivial 基线在开发机 `~/goldrush/ptrivial.so`）。
- 场次花在硬对手与探针上；陪练基线 cpp1.so（v1 早期版）在开发机 `~/goldrush/cpp1.so`。

## 6. 开题清单（按期望值排序）

1. **延迟 160ns 冲刺**（用户 8.8 设定）：压过 T-1 实测 190 → 同时夺回对手先手权与 NPC 前手位（定律 10）。
   8.8 战役进展：NS6Q 直线化轮换栈已到 **240/320（收入 -18%）**，对 T-1 先手 13%→34%；
   完整账本与负结果军规 9-11 见 speed/CHANGELOG。下一步：微扫目标去重复活 / 无损瘦身。
   **新范式**：省指令必须以零位点形态交付（NS6P -282 指令 0ns vs NS6Q 同指令 -30ns 对照案）。
2. **堆记忆**：strategy 轨 16 槽矿堆缓存移植 speed（盲轮 17-26% 有处可去；截胡损失 ~140 起/局可预判）。
3. **宽域清扫路径**：学 T-1 形态（街区清空再转移，活动半径 3-8，74% 收入在对手视野外），替代固定锚点。
4. 视野触发器三设计（前两版全哑：矿堆喂太忙）。
5. 天梯防守位换代评估（现挂 cpp27b 已两世代落后，源在 git 历史）。
6. v2a 实验线复活复测（A 段开局 BFS 价值已证实；源 `git show 2b5a2b0:src/strategy/v2a/player.cpp`）。

已判负出清单：~~驻留深度/环内金额裁决~~（8.8 判负，见 speed/CHANGELOG 军规 8——差距在信息与时序，不在环内微操）。

## 7. 仓库结构

```
AGENT.md            本手册
README.md           一页地图
src/game_api.h      官方接口头（共用）
src/speed/          速度优先轨: CHANGELOG.md + v1（总冠军: player.cpp + Makefile + LOOP.md）
src/strategy/       策略优先轨: CHANGELOG.md + v1（矿堆农冠军: player.cpp + Makefile + LOOP.md）
tests/              replay.py(回放) pair_diff.py(逐位对账) dump_inputs.py bench.cpp game_api.py(mock)
tools/              gr.py(平台客户端) grlog.py(战报) runbatch.sh(跑批)
docs/               官方赛制文档（只读）
logs/               对局日志（gitignored）
```

**结构军规**（仓库所有者定的，别破）：

1. **不设 archive 目录**——史前原型与一切旧物只活在 git 历史。
2. **每轨工作区只保留现役最强一版**。新版夺冠 → 旧冠军 `git rm`，并在轨 CHANGELOG 档案表补一行"退役 + `git show <commit>:<path>` 取用命令"。
3. 每个在役版本目录必须有 **LOOP.md**（算法流程文档）——改算法必须同步更新。
4. 每个版本目录的 Makefile 头注释 = 战绩档案与精确复现 flags。改版本 → 更新所在轨 CHANGELOG.md；平台定律新证据 → 更新本文 §4。
