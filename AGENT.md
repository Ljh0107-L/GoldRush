# AGENT.md — GoldRush 2.0 策略开发指南

> 给接手本赛题的 AI Agent 的工作手册。人类已把赛题全权委托给 Agent。
> 本文件是活文档：每次迭代后更新 §9 实验日志与 §7 当前状态。

---

## 1. 任务目标

在九坤 GoldRush 2.0 编程掘金争夺赛中，把 [src/player.cpp](src/player.cpp)（**C++ 是主战语言**，见 §6.1）优化成有竞争力的策略，冲击初赛前 16 名（晋级决赛）。

**优化目标函数**：`最终得分 = 毛金币 − 视野花费`，500 轮结束时高者胜；相等时 P90 延迟低者胜。

### 仓库结构

```
AGENT.md          ← 本手册（活文档：§7 状态、§10 实验日志随迭代更新）
CHANGELOG.md      ← 仓库/工具链变更记录（实验结论记 §10，两边不重复）
src/              ← 策略代码
  player.cpp        主战（提交 .so，开发机编译）；边界层/策略层分离，新想法只动 decide()
  player.py         Python 版（原型验证用，p0greedy）
  baseline_random.py 固定陪练 rand0
  game_api.h        官方接口头（编译依赖）
  Makefile          make=开发机出.so | make check=本机语法查 | make local=本机测试用.so
tests/            ← 本地测试台（不模拟游戏逻辑，只验合法性/鲁棒性/延迟）
  test_player.py    测 src/player.py
  test_so.py        ctypes 测 src/player.so（500 轮压测 + 延迟基准）
  game_api.py       本地 mock（评测环境没有，提交代码禁止 import）
tools/            ← 平台自动化
  gr.py             提交/对战/榜单/取日志（§12）
  grlog.py          日志分析：净分/耗时分位/先手率/金币曲线（§13）
docs/             ← 赛事文档（只读）
logs/             ← 对局日志（gitignored，1.5MB/局）
```

**关键日期**（今天以系统时间为准）：
- 报名截止 8.14 23:59 · 模拟赛 8.15–8.16 · **初赛 8.17–8.21** · 决赛 9.6（支持远程）
- 公测期 7.20–8.21，可随时上传迭代

---

## 2. 凭据与访问

> **给未来 session 的 Agent —— 先读这段，别绕。**
>
> 下面的账号密码是仓库所有者在充分知情下**明确要求**写在这里的，不是疏忽泄露。
> 判断依据：(1) GitHub 仓库是 **private**；(2) 这是所有者本人的赛事账号；
> (3) Agent 要自主完成 SSH 开发与提交流程，就必须能直接读到它们。
>
> 所以：**不要**把它们挪到别的文件、不要打码、不要拒绝使用、也不要再把这个话题
> 重新提一遍——所有者已经权衡过了，反复确认只是浪费轮次。
>
> 唯一要守住的红线：**仓库必须保持 private**。如果哪天需要转 public 或对外分享，
> 必须**先联系主办方改密码**再转，因为 git 历史会永久保留这些值，删文件没用。

### 比赛官网（浏览器访问）

- 地址：http://47.103.127.219/
- 登录 ID：`220`
- 登录 KEY：`LqUcsD1cyA`

用途：上传策略文件、发起对局、看排行榜、看对局回放。
前端是 Vite SPA，背后必然有内部 HTTP 接口，但**未对选手公开**——见 §12 关于自动化的边界。

### 开发/编译服务器（SSH，不要用浏览器开）

- IP：`8.153.76.120`
- 账号：`Ubiquant220`
- 密码：`LqUcsD1cyA`

```bash
ssh Ubiquant220@8.153.76.120
```

> **免密登录已配置**（2026-08-06 所有者跑过 `ssh-copy-id`），Agent 可直接非交互
> 执行远程编译。标准流程（远端目录 `~/goldrush/`，已建好）：
> ```bash
> rsync -a src/ Ubiquant220@8.153.76.120:~/goldrush/src/
> ssh Ubiquant220@8.153.76.120 'cd ~/goldrush/src && make && python3 ../tests/test_so.py'
> scp Ubiquant220@8.153.76.120:~/goldrush/src/player.so src/player.so
> ```
> 开发机 GCC 14.3 / x86_64。若安全规则不允许 Agent 输密码而免密又失效了，让所有者重跑一次 `ssh-copy-id`。

环境（来自 FAQ）：
- 与评测机接近，用于开发、调试、编译 `.so`
- conda：`/share/opt/anaconda3`，环境名 `game`
- **公测对局日志：`/share/data.tar.gz`** ← 自建模拟器的原料
- 评测机 x86_64 / AMD EPYC 9T25 / 32 核 / 无 GPU；与编译机相比仅未开超线程

### 别搞混两个地址

| 地址 | 方式 | 用途 |
|---|---|---|
| `47.103.127.219` | **浏览器** | 传代码 / 发起对局 / 看榜 / 看回放 |
| `8.153.76.120` | **只能 SSH** | 开发 / 编译 / 日志分析 |

开发机用浏览器打不开（FAQ 里有人踩过这个坑）。官网 KEY 与服务器密码目前是同一串。

---

## 3. 绝对红线（违反 = 整局判负，无单轮容错）

1. **返回格式非法** — 必须是长度 9 的 `list`，`actions[i]∈[0,4]`、`k∈[0,6]`、`order∈{0,1}`、`vp∈{0,1,2}`
2. **运行时异常** — 任何未捕获的 exception
3. **时间池耗尽** — 单轮标准限时 300ms，**超出部分**累计扣 60s 总池，扣完判负

因此 `MoveDecision` 必须长成这样：

```python
SAFE = [4, 4, 4, 4, 4, 4, 3, 0, 0]   # 全不动, k=3, order=0, 不买视野

def MoveDecision(self, game_input):
    try:
        out = self._decide(game_input)      # 真正的逻辑
        return self._sanitize(out)          # 越界一律 clamp
    except Exception:
        return list(SAFE)                   # 绝不让异常逃出去
```

`_sanitize` 要做的事：长度补齐到 9、每个元素强制 `int()`、逐字段 clamp 到合法区间。**这层保险的成本是几微秒，收益是避免整局判负——永远不要为了省事去掉它。**

---

## 4. 规则速查（数值为公测期公布值，正赛可能变，不要硬编码依赖）

### 战场
- 17×17 网格；双方各 2 个角色；固定 **500 轮**
- 出生点：P1 主对角线两端，P2 副对角线两端
- 障碍：单张地图内固定，不同地图可能不同，**正赛不保证沿用公测的图**

### 每回合执行顺序（重要）
```
生成金币/炸弹 → 同时调用双方策略 → 决策耗时【短】的玩家行动
             → NPC 行动 → 决策耗时【长】的玩家行动
```
两名玩家拿到的是**行动前的同一份**地图快照。同格金币归先到者。

### 移动与拾取
- 每回合两角色**共分配 6 步**，用 `k` 切分：角色0 走 `a[0:k]`，角色1 走 `a[k:6]`
- `order` 决定自己两角色谁先走——**一个角色走完全部步数，另一个才开始**，不是交替
- 拾取：从别的格**移动进入**含金币格 → 拿 `65%`（向上取整），剩 35% 留在原地
- **可重复拾取**：走开再回来能再吃一次
- **生成在脚下的金币吃不到**，必须先离开再进入
- 碰撞（墙 / 任意其他角色）：**仅该步不执行，后续步照常执行**。4 个玩家角色两两不可重叠；可以进入别人刚腾空的格子

### 惩罚
- **炸弹**：损失当前持有金币 `10%`（向上取整），炸弹消失，可继续行动；每 20 轮刷新一批
- **踩踏**：行动后进入 NPC 数 ≥3 的格子，损失 `5%`（向上取整）
- 同格同时有金币和踩踏：**先吃金币，再按新的持币量扣踩踏**
- 同一格不会同时有炸弹和金币，也不会同时有炸弹和 ≥3 NPC

> 惩罚都是**当前持币的百分比** → 越富的角色越怕炸弹。两角色金币独立计（`my_units_gold[2]`）且不可转移。

### NPC
- 7 个，开局全在地图正中；每轮最多 3 步；策略**不公开**
- NPC 之间、NPC 与玩家角色**可以重叠**
- 参与抢金币，但持币不计入胜负

### 金币生成
- 中心 9×9 每回合随机生成；9×9 外每隔若干轮生成；**分布不公开**

### 视野
- 默认 5×5（以角色自身为中心）；视野外 `grid` 全是 `-5`
- `vp=1` → 7×7，2 金币；`vp=2` → 9×9，3 金币
- **下回合生效，只管 1 回合**，可连续购买
- 费用**对局结束后**从毛金币里扣

### 快照（唯一的全局信息）
- 每 5 轮一次，地图分 5 个**固定**区域
- 每区域给：`enter` / `leave` / `gold_generated` / `gold_collected` / `gold_remaining` / `occupants`
- **进出与人数统计包含 NPC**；快照反映的是**过去几轮**的窗口，不是当前瞬时

---

## 5. 接口契约

### grid 编码
| 值 | 含义 |
|---|---|
| `-5` | 迷雾（视野外） |
| `-3` | 炸弹 |
| `-1` | 障碍 |
| `0` | 空地 |
| `>=1` | 金币，值即数量 |

NPC **不在 grid 上标记**，走 `visible_npcs`；己方位置看 `my_units`，敌方看 `visible_enemies`。

### 输入字段
`round` / `grid[17][17]` / `my_units[2]` / `my_units_gold[2]` / `gold_opp` / `visible_enemies[2]` / `visible_npcs` / `snapshot_valid` / `snapshot`

字段定义见 [src/game_api.h](src/game_api.h)（C++，编译依赖）与 [tests/game_api.py](tests/game_api.py)（本地 mock，提交代码禁止 import）。

### 输出
长度 9 的 list：`[a0,a1,a2,a3,a4,a5, k, order, vp]`，动作 `0=上 1=下 2=左 3=右 4=不动`

### 生命周期
```
加载模块 → Player() 实例化一次        ← 10s 预算，不计入每轮 300ms
   ↓
500 × p.MoveDecision(game_input)      ← 每轮 300ms 软限
   ↓
对局结束才释放
```

**对象跨 500 轮常驻内存**——`self` 上挂的状态全都留着。这是自建累积地图的基础。

---

## 6. 核心洞察（设计决策的依据）

### 6.1 延迟是游戏机制，不只是工程指标 ★最重要

耗时短的玩家先行动，同格金币归先到者。**300ms 的限时是幌子**——看真实榜单：

| 排名 | 队伍 | 外战胜率 | P90 |
|---|---|---|---|
| 1 | Tiuntled-1 | 98.85% | **230ns** |
| 2 | Tundra-wawa | 97.53% | 1.53μs |
| 3 | 酱味大鸡 | 92.49% | 14.65μs |
| 4 | Ausdroid | 90.91% | 750ns |
| 7 | 若叶睦的狗2.0 | 87.07% | 530ns |

**竞争区间是纳秒到十几微秒**，没人接近 300ms。实测数据（对局 137528）：

| | 金币 | 耗时 | 先手回合 |
|---|---|---|---|
| p0greedy（纯 Python BFS） | 370 | 932μs | 19 / 500 |
| rand0（随机） | 166 | 289μs | **481 / 500** |

慢的一方 **96% 的回合在后手**。`end.dispatch_order[0]` 直接给出每回合谁先手，这条因果链可以实证，不用猜。

实践含义：
- **C++ 是入场券，不是优化项——已转正为主战语言**（2026-08-06 决定）。纯 Python 一次函数调用就 ~50-100ns，跑个 BFS 就是几百微秒，结构性出局。榜上第 3 名 14.65μs 大概是 Python 的天花板。实测同一套贪心 BFS：Python 332μs vs C++ ≤1.5μs（本机、含 ctypes 开销）。Python 版只用于快速原型验证想法，验证过的逻辑落到 [src/player.cpp](src/player.cpp)。
- 目标量级是**亚微秒**；重计算全部前移到 `__init__`（10s 预算）
- 用 anytime 算法：设软时限，到点返回当前最优解
- 平局也是 P90 低者胜——延迟是双重收益
- 极速优化奖只限 C++

> 注意权衡：P0 实测里慢 3 倍的一方仍然赢了 2.2 倍，说明**策略差距足够大时能盖过后手劣势**。但对手都在 ns 级时，这个缓冲就没有了。先把策略做对，再把它做快，但别让"做快"排到日程末尾。

### 6.2 引擎不给累积地图，但你的对象常驻 → 必须自建世界模型
FAQ 明确：`grid` 只含**本轮决策时最终位置**的视野，不提供移动途中的增量，也不提供已探索地图。

所以自己维护：
- `known_grid[17][17]` + `last_seen[17][17]`
- **障碍是静态的** → 看到一次就永久记住（同一张图内）
- **金币是动态的** → 置信度随时间衰减
- **炸弹每 20 轮刷新** → 记忆有效期短
- 已知自己位置和当前视野半径 → 能区分"确认为空"和"从未见过"，后者才值得探索

### 6.2b 环境统计实证（来自 3 局无迷雾全量日志，2026-08-06 挖掘）

开发机 `/share/data.tar.gz` 里 `data/full/` 有 3 局**无迷雾全量日志**（已拷至
`logs/public/`，gitignored；含全部 NPC 轨迹与双方位置）。挖掘结论：

- **金币生成**：中心 9×9 每回合生成 0-8 格（均值 ~1.7 格），单格量 1-10 均匀分布；
  外圈不定期生成（间隔 1-13 轮），量呈**双峰**——小堆 1-10 与**大堆 16-28**。
  外圈大堆是无人争抢的高价值远征目标。金币不消失，会越积越多（见过 37）。
- **炸弹**：每 20 轮准时一波 ~15-18 个，位置**不限于**地图第 2 行标记的 `"2"` 格
  （3 局出现过 246 个不同格子）；不被踩就一直留场，稳态场上 ~14 个。
- **NPC = 主要竞争者**：3 局共吃掉 19183 金币（场均 6394，远超玩家）。行为是
  **噪声贪心**：距最近金币 2-5 格时 ~75% 概率靠近，邻格时仅 49%（近处近似随机）；
  每轮几乎满 3 步（停步率 5.7%）；**主动避炸弹**（踩雷率 0.4%）；24.5% 的回合
  终点落在金币格上。含义：与 NPC 抢它更近的金币基本抢不过（它 3 步/轮且比你的
  后手角色先动），目标估值要按 NPC 距离折价。
- **踩踏罕见**：≥3 NPC 同格仅 1.5% 的回合出现，风险低但仍要避。
- 全量日志 grid 里 `-2`=玩家、`-4`=NPC（仅日志编码，实战 grid 无此值）。
- **官方 bot 分两档**（`data/user/` 197 局循环赛分析）：RaptorLake/SkyLake ~95% 胜率、
  均净分 681、P90 30μs、**选择性**买视野（77 轮花 187 金，净赚）；其余 5 个弱陪练
  （胜率 ~30%、无脑每 5 轮买视野净分为负、38% 罚站）。启示：①视野买对了是正收益，
  P4 的成本模型值得做；②官方 bot 不快（30μs），榜首 230ns 是选手做出来的。

### 6.2c 顶级选手解剖（对局 139296/139297/139364 实测）

- **#1 Tiuntled-1**：全程 P50 **190ns** / max 6μs，**无尖峰**——不是"低频重规划+缓存"，
  是每轮全量决策只花几百 ns（无 BFS、无全图扫描，纯增量+反应式）。#4 酱味大鸡同构
  （460ns 恒定；榜上 14μs 是历史中位数，人家早升级了——**对手在持续进化，旧战绩会过期**）。
- **顶级选手不买视野**（对局全程 5×5）。我们的视野购买 A/B（139340-44）显示 ±100 噪声内，
  保留但别指望它。
- **收入连续性是本质差距**：Tiuntled 75% 回合有进账、113 回合单轮 ≥8 金、
  **300-500 轮进账 1068 越打越富**（存量堆轮巡收割）；我们同局大量 1-2 金残渣回合、
  后期乏力。差距不在单步决策，在于"永远待在金币密度高的邻域"的路线组织。
- 对阵他们我们 0-1/500 先手，纯策略对话。当前差距 ~1530 : 2210。
- 挖掘脚本在 scratchpad 一次性使用后未入库；要复挖看本节结论即可，原始数据在
  `logs/public/` 与开发机 `~/data/`。

### 6.3 65% + 可重复拾取 → 富矿格值得反复进出
金币格永远吃不干净。在富矿格旁边"横跳"每 2 步吃一次 65%，可能优于长途奔袭。价值评估要算**多轮累计收益**而不是单次。

### 6.4 比分是有噪声的
`gold_opp` 是**毛金币**，不含对手视野花费，而胜负按净额算。你看到的领先可能是假的；反之亦然。做局势判断时要给对手的花费留一个估计区间（对手若激进买视野，500 轮可达数百金币量级）。

### 6.5 碰撞规则会把你带偏
撞了只跳过那一步，**后续步照走**。规划 6 步路径时必须回答："如果第 i 步被挡，剩下的步会把我带到哪？" 官方例子里，玩家想走"右右上"，第二步被挡，第三步照走，人直接走到炸弹上。

稳健做法：为每条路径评估"被阻断后的最坏落点"，避开落到炸弹/踩踏格的路径。

---

## 7. 优化路线图

**总目标（用户 2026-08-07 设定）：战胜榜单前三名**
| # | 队伍 | 对战ID | P90 | 外战胜率 |
|---|------|--------|-----|---------|
| 1 | Tiuntled-1 | 87478 (player163) | 220ns | 97.37% |
| 2 | Tundra-wawa | 43116 (player57) | 1.47μs | 96.62% |
| 3 | Ausdroid | 162580 (player167) | 750ns | 94.17% |

一切版本迭代以"能否从前三手里拿分"为标尺；原硬对手池（凑企鹅/rikka/ZZK 等）
降级为回归参照。实测对局 P50: Tiuntled 190-210ns / Tundra 280-350ns（榜显
1.47μs 是历史虚值）/ Ausdroid 730-840ns。

**为什么必须打延迟（2026-08-07 三方向实验定论）**：调度顺序 = 快玩家→NPC→慢玩家。
对前三我们永远最后行动，中心新刷金被对手+NPC 各吃一轮 65%，我们只配 ~12% 残渣
（实测拾取中位数 2 金）。后手方增收的三个策略方向已全部实验判死（外圈转移/
强制让中心/关目击盖章，见 §10）。**突破口 = 把实测 P50 压到 Ausdroid 的 750ns
以下**（组件账本：更新+开销 270ns 已达标，DFS 1.35μs 和目标层 1.7μs 是两座山）；
届时我们变成"快玩家"，先于 NPC 进食，收入结构整体翻转。

**当前状态（2026-08-07）**：主战 = `cpp20`（世界模型+计划缓存+3步穷举+争抢热度图
+刷新热度图 SPAWNMAP+避让, 争抢章 30 轮），基线 2814，已发布。战绩画像：
**赢慢于 3.5μs 的一切对手**（含 #20/#24/#25 与大部分中游），**输 ≤1.5μs 快手**
（rikka/数学分析/counterfactual/凑企鹅——全程后手只配 35% 残渣）。ZZK 已是硬币局。
开阔图强势，稠密图(78障碍)相对弱。

**两条出路（下阶段主题, 二选一或并行）**：
1. **真 ns 级重写**：nsv1/2 证明纯反射会塌方（900分），#1 的 190ns 里装着顶级策略。
   需要从头设计"增量维护的策略结构"（收入连续性/轮巡组织）而不是砍功能。
   已有素材：探针数据(update 250ns/build+DFS 1.65μs)、nsv2 骨架(src/ns.cpp)。
2. **策略再+800 分**：无视速度，把基线从 2800 推到 3600（P^GPT 路线），
   靠路线组织(密度驻留/轮巡节奏)。参考 §6.2c Tiuntled 收入画像。

### P0 · 防御性基建 ✅（2026-08-06 完成）
- [x] `try/except` + `_sanitize` 兜底（见 §3）；C++ 版为 try/catch + clamp 边界层
- [x] 无 print / 无文件 IO
- [x] 提交验证：对局 137528 / 137958 跑完 500 轮，0 判负
- [ ] 每轮软时限 + anytime 返回（进入复杂搜索时再做，贪心 BFS 用不上）

### P0.5 · C++ 链路首跑 ✅（2026-08-06 完成）
- [x] 开发机 `make` 出 player.so，`tests/test_so.py` 开发机复验通过
- [x] 提交 .so 自博弈 vs rand0（对局 138842），0 判负
- [x] `grlog.py` 对比：先手率 4% → **100%**，P90 1.15ms → 6.92μs

### P1 · 持久世界模型
- [ ] `known_grid` / `last_seen` / 障碍永久化 / 金币置信度衰减
- [ ] 从 `my_units` + 上回合 `vp` 反推本轮视野范围，标记"确认为空"
- [ ] 快照数据落到 `self`，跨轮可用

**验收**：日志中已探索格子占比随回合稳定上升。

### P2 · 贪心寻路 + 步数分配
- [ ] **探索 fallback**：视野内无金币时朝最近的"从未见过"区域移动（修复 §10 观测到的卡死：贪心无目标就永久原地不动）
- [ ] BFS 最短路（17×17，成本可忽略；`__init__` 里预计算全图距离表）
- [ ] 目标价值 = `金币量 × 0.65 × 可达性 − 风险成本(炸弹 10%持币 + 踩踏 5%持币)`
- [ ] 两角色**目标去重**，避免抢同一块
- [ ] `k` 按两角色的边际收益动态分配，不要固定 4/2
- [ ] `order` 避免自撞
- [ ] 路径鲁棒性：评估被阻断后的落点（§6.5）

**验收**：对随机基线胜率 > 90%。

### P3 · 快照驱动的宏观调度
- [ ] `gold_remaining` 高 + `occupants` 低的区域 = 优先目标
- [ ] `gold_generated − gold_collected` 估计各区净积累速度
- [ ] `occupants` 含 NPC，用 `visible_npcs` 去噪
- [ ] 分层：快照选区域（宏观） → BFS 走位（微观）

### P4 · 视野购买策略
- [ ] 成本模型：2/3 金币 vs 多看到的期望收益
- [ ] 触发条件：进入陌生区域、快照提示富矿区、追击对手
- [ ] 记账自己的累计花费（引擎不告诉你净分，自己算）

### P5 · 延迟优化（**贯穿全程，不是最后才做**）
- [x] ~~策略定型后移植 C++~~ → 已改为 C++ 直接开发（P0.5），Python 只做原型
- [ ] 每轮耗时用 `grlog.py` 的分位数跟踪，别自己在代码里做 IO 打点（见 §8）
- [ ] 热路径零堆分配（现有 Bfs 静态缓冲已是此模式，保持）
- [ ] 目标量级亚微秒：定期用 `tests/test_so.py` 基准回归，防止改策略把延迟改炸

### P6 · 对手与 NPC 建模
- [ ] 从对局日志学 NPC 行为模式
- [ ] 由"某格金币意外消失"反推对手先手与位置
- [ ] 争抢格的博弈：预测对手目标，抢或让

### ~~本地模拟器~~ —— 已否决，不要做

**平台自带真实引擎的自博弈**：一次提交两份自己的代码即可对战，每天 500 场额度，且**不计入公开的外战胜率**。自建模拟器要重猜 NPC 策略、金币生成分布、炸弹刷新规则，得到一个永远存疑的近似物——收益远不及直接用真引擎。

评估循环改成：

```bash
# 新版本 vs 固定基线，跑一局
./tools/gr.py submit --map 1 src/player.so:v2 src/baseline_random.py:rand0
./tools/gr.py games                     # 取 game_id
./tools/gr.py watch <game_id>           # 等完成并自动存日志
```

要统计显著性就多跑几局、换地图（1/2/3）跑，额度足够。

---

## 8. 已知陷阱

| 陷阱 | 后果 | 对策 |
|---|---|---|
| `MoveDecision` 里 `print`/写日志 | **计入 P90 延迟**，影响先后手与平局判定 | 提交版关掉所有 IO；要打点就存内存，别落盘 |
| `import game_api` | 评测环境没这个文件 → ImportError → **判负** | 提交版只 import 标准库；直接属性访问 `game_input` |
| 返回 numpy 类型 | `np.int64` 不是 `int`，格式校验可能挂 | `_sanitize` 里强制 `int()` |
| 硬编码地图/障碍 | 正赛换图直接失效 | 障碍靠运行时探索，别写死 |
| 以为金币生成在脚下能吃 | 白等 | 必须先离开再进入 |
| 以为 `visible_enemies` 顺序固定 | 认错角色 | 官方明确**不保证**顺序 |
| 把快照当成当前状态 | 决策滞后 | 快照是**过去窗口**的统计 |
| 以为 `order` 是交替执行 | 路径规划全错 | 一个角色走完全部步，另一个才开始 |
| 以为 `gold_opp` 是净分 | 误判局势 | 是**毛金币**，不含对手视野花费 |
| 用满 300ms | 每回合后手，丢金币 | 见 §6.1 |

---

## 9. 开发工作流

```bash
# 本地自检（只验证输出合法性/鲁棒性/延迟，不模拟游戏逻辑）
python3 tests/test_player.py            # Python 版
cd src && make local && cd ..           # 本机架构 .so（仅供本地测试）
python3 tests/test_so.py                # C++ 版：合法性 + 500 轮压测 + 延迟基准

# 提交用 .so：必须在开发机上编（本机 arm64 产物评测机不认）
ssh Ubiquant220@8.153.76.120 'cd ~/goldrush/src && make'     # 见 §2 免密待办
```

**迭代闭环（全命令行，见 §12/§13）**：

```bash
python3 tests/test_so.py                                     # 1. 本地自检
./tools/gr.py submit --map 1 src/player.so:v2 src/baseline_random.py:rand0
./tools/gr.py games                                          # 2. 取 game_id
./tools/gr.py watch <game_id> -o logs/game_<id>.log          # 3. 等完成并存日志
./tools/grlog.py logs/game_<id>.log --rounds                 # 4. 量化分析
#                                                              5. 结论记入 §10，代码/工具变更记 CHANGELOG.md
```

日志里能拿到对局中拿不到的信息：双方真实耗时、视野花费、每回合先后手、完整无雾地图。
`grlog.py` 一次给出净分、每轮耗时 P50/P90/P99、先手率、事件统计与金币曲线；
金币曲线出现长平台 = 策略卡死（无目标不动），优先排查。

**看榜的正确姿势**：
- "外战胜率" = 最近 24h 内，新旧代码 + 主动/被动对局**混合**的结果 → 换代码后不会立刻反映，别过度解读短期波动
- "P90 us" = 从公测以来**所有**相关对局的每局 P90 的中位数，实时更新

---

## 10. 实验日志

| 日期 | 对局 | 版本 | 改动 | 结果 | 耗时 | 结论 |
|---|---|---|---|---|---|---|
| 2026-08-07 | — | v0 `rand0` | 官方随机基线，加防御层 | — | 289μs | 固定陪练 |
| 2026-08-07 | 137528 | v1 `p0greedy` | P0：try/except + sanitize + 关 debug + BFS 贪心最近金币 | **370 : 166 胜** | P90 1.07ms | 贪心 = 随机的 2.2×；但 96% 回合后手，Python 必须换 C++ |
| 2026-08-07 | 137958 | v1 `p0greedy` | 同 v1（链路复验局） | **778 : 224 胜** | P90 1.15ms | ①先手率仍仅 4%；②137528 金币从 200 轮起零增长、本局 300 轮后近乎停滞——**贪心无探索，视野内没金币就永久卡死**，P2 首项修它；③单局方差大（370 vs 778），版本对比至少跑 3-5 局 |
| 2026-08-07 | 138842 | v2 `cpp1` | C++ 移植（逻辑同 v1），开发机编译 .so 首次提交 | **683 : 143 胜** | **P90 6.92μs** | 先手率 **500/500**；同逻辑快 160 倍验证 §6.1；6.92μs≈榜第 3 名水平，离头部 ns 级还有 P5 空间；卡死缺陷依旧（100-300 轮曲线平缓） |
| 2026-08-07 | 138897-99 | v3 `cpp2` | P1 世界模型 + P2 探索 + 3 步局部穷举（拾取/炸弹/踩踏精确模拟）+ 全局目标按 NPC 距离折价 | **3:0 胜 cpp1**，2457/2791/2557 : 991/303/985 | P90 14-18μs | 均分 3.4×cpp1；罚站消失；炸弹权重×2 后富角色绕行、穷角色直穿（行为验证通过）；**P90 涨到 14-18μs 需 P5 压缩**；对照：官方最强 bot RaptorLake 均净分才 681 |
| 2026-08-07 | 138954-55 | `cpp2` 外战 | 挑战 #25 eureka、#18 量衡量化 | **胜 eureka** 2267:1880；**负量衡量化** 1479:2035 | P90 22-24μs | cpp2 真实水位 ≈ 20-25 名。败因：对手 740ns 全程先手，**新刷金币 65% 归先手，后手只配 35% 残渣**（我方 0.9 金/次 vs 对手 2.67）——延迟直接决定拾取质量 |
| 2026-08-07 | 139006-08 | v4 `cpp3` **负结果** | +拾取记账 +视野购买 +增量世界模型 | **1:2 负于 cpp2** | P50 反而慢 1.2μs | ①拾取记账是负优化：吃过的格子下轮自动重观测，记账只引入误差，已回滚；②镜像对局先手率随 P50 直接翻转（161/500），速度劣势淹没策略改动的信号——**评估方法论：镜像局测速度差，公共对手局（vs cpp1）才测策略差** |
| 2026-08-07 | 139012-16 | v5 `cpp4` | =cpp2 核心 + 视野购买(环陈旧判据+250 硬预算) + 7×7 补丁局部搜索 | 镜像 **2:1 胜 cpp2**(总分 6140:5668)；vs cpp1 均 2601(无退化) | 平台 P50 9.5μs | 视野费 ~58/局能赚回；**本机 0.88μs vs 平台 9.5μs → 平台延迟大头是每轮冷缓存(~40 次 miss)，不是指令数**。榜首 230ns 的本质是工作集压进几条 cache line |
| 2026-08-07 | 139019-20 | `cpp4` 外战 | 挑战 #22 你是凑企鹅(4μs)、#23 ZZK(3.5μs) | **两负** 1192:2278、1869:2351 | 对手先手率 75-90% | 结论定型：**中游梯队 = 延迟竞赛**。cpp4 赢慢于自己的(#25 17μs)、输快于自己的。策略已够 2600 分档，爬榜主杠杆是 P50 9.5μs→<3μs(缓存足迹) |
| 2026-08-07 | 139108-10 | v6 `cpp5` | 数据结构全面压位(世界 4B/格、BFS 压位、小列表标记) | 镜像 2:1 胜 cpp4 但总分 5831:6125 | P50 **反涨** 11.4μs, P99 31→23 | 压位对尾部有效、中位数无效 → 冷缓存假设可疑 |
| 2026-08-07 | 139118 | 探针局 | ptrivial(纯返回) + pmemory(模型更新+补丁读) | — | **30ns / 110ns** (P50) | **推翻冷缓存论**：平台无固定开销、内存触碰不贵；延迟大头 = 赶路回合每轮 2 次全图 BFS+扫描。榜首 230ns 完全真实且形态=重计算只占少数回合 |
| 2026-08-07 | 139135-38 | v7 `cpp6` | **多轮计划缓存**(BFS 路径存起来逐轮弹 3 步+廉价校验) + 探索目标限边疆(≥4) + BFS 半径≤9 | 镜像 **2:1 胜 cpp4**, 总分 5926:5146(+15%) | **P50 4.0-4.6μs, P90 8μs** | P50/P90 双砍半, 先手率 65-74%。架构成立：P50 由"每轮必做"的部分决定(模型更新+补丁+局部DFS) |
| 2026-08-07 | 139150-52 | v8 `cpp7` | +NPC竞速放宽 +目标簇加成 +无金补丁跳DFS | 镜像 2:1 胜 cpp6 (+3.5%) | 同 cpp6 | 已发布为公开防守版本 |
| 2026-08-07 | 139155-58 | `cpp7` 外战 **4连败** | 挑战 #23/#22/#18/#25 | 全负, 我方绝对分仅 ~1500 | 对 eureka **500/500 先手仍输** 1451:2240 | **两大教训**：①镜像局赢的是相对抢夺, 绝对采集效率一路镜像胜利中悄悄退化(cpp2 外战绝对分 1873 → cpp7 1500)——**评估必须以固定基线(vs cpp1)+外战为准**；②BFS 半径≤9 砍掉外圈大堆远征；③NPC 竞速放宽是双刃剑(NPC 住中心本来就近, 放宽=送人头) |
| 2026-08-07 | 139181-82 | v9 `cpp9` | +尾步填充(16%步在罚站) +两段式BFS(9步→全图) +行军目标衰减校验 | **vs cpp1 基线 2719/2886, 均 2802**(历史锚点 2600) | P50 4.2μs, P90 10-13 | 绝对效率超过 cpp2/cpp4 时代且保住速度 |
| 2026-08-07 | 139183-87 | `cpp9` 外战 | — | 胜 eureka 2328:1768; 负 ZZK/凑企鹅(~1400 分) | — | 被快手先手压制时绝对分崩到 1400——"后手只配残渣"问题结构化存在 |
| 2026-08-07 | 139191-215 | v10-11 `cpp10/11` | 补丁盖章化(-500 比较/轮); NPC 竞速回滚保守 | **首胜 ZZK 2122:2004**(先手率仅 48%!); 负凑企鹅 1747:2081(大幅缩差) | P50 3.5μs | NPC 回滚外战验证有效(绝对分 1400→1700-2100); cpp11 已发布为防守版 |
| 2026-08-07 | 139227-29 | `cpp11` 高位外战 | 挑战 #20/#24/#25 | **胜杨东升(#20) 1905:1628, 胜#25 2136:1616**; 负 P^GPT(#24) 2142:2332 | — | 站上 20 名边界。**P^GPT 43μs 全程后手仍 2332 分** = "避开吸尘器收割存量"策略的活证明, cpp13 学它 |
| 2026-08-07 | 139240-43 | v12 `cpp12` | 金币列表 + 曼哈顿导向走位, BFS 只兜底 | 基线均 2652; 负凑企鹅/ZZK(ZZK 翻盘, 对手也在进化) | P50 3.6μs **没降** | 中位成本在"每轮必做"部分(更新+补丁+DFS), 不在 BFS; P90 改善到 9.5-12。速度已到收益递减区, 转攻策略 |
| 2026-08-07 | 139252-56 | v13 `cpp13` | **争抢热度图**(目标被吃空/目击敌人→盖章折价) + 探索去中心化 | 基线新高 2724; **复仇 P^GPT 2127:1960** | — | 避让对策略型对手有效; 已发布 |
| 2026-08-07 | 139266-70 | v14 | 导向目标缓存 + 列表 swap-remove | 对凑企鹅/ZZK 差距继续收窄 | P50 3.7 | 探针二分: update 250ns / +build+DFS 1.65μs / 其余在目标层 |
| 2026-08-07 | 139340-44 | 视野 A/B | cpp15(买) vs cpp16(不买) | ±100 噪声内 | — | 顶级选手不买视野但对我们中性; 保留 |
| 2026-08-07 | 139394-402 | cpp15 vs cpp17 | 轮作衰减+软分区 A/B(各4局基线) | **cpp15 2597 > cpp17 2309, 否决** | — | ROTATION/TERRITORY 开关默认关 |
| 2026-08-07 | 139418-24 | v18 `cpp18` | **刷新热度图 SPAWNMAP**(观测 spawn 事件导向探索) vs OPENING | **SPAWNMAP 2734 且方差骤降**(2680-2793); OPENING 均值平方差大, 否决 | — | 已发布; "待在金币会出现的地方"被验证 |
| 2026-08-07 | 139441-44 | 多地图验证 | cpp18 于地图2(24障碍)/地图3(78障碍) | **4:0**, 3144/3005 与 1369/1690 | — | 换图鲁棒; 密图视野费升到 88-110 可接受 |
| 2026-08-07 | 139459-69 | cpp18 广谱外战 | 挑战 8 个中游 | **7胜1负**(唯负 P^GPT 1706:1953) | — | 胜 #20/#25/#26/566/啊对对对对队/肯特镇/有鹅选鹅 |
| 2026-08-07 | 139624-27 | v19 EXPRESS **弃用** | 快车道(有计划+窗口无金→跳过补丁DFS) | P50 未动(3.85μs) | 场上金币密度~6%, 25格窗口仅~20%回合为空, 触发率过低 | 速度攻坚正式收手 |
| 2026-08-07 | 139634-72 | 参数A/B×3 | yield 40→80: **2395<2734 否决**; 争抢章 60→30: **2814>2734 采纳(cpp20)**; NPC折价÷2: 2553 否决; 衰减T45: 2380 否决 | — | — | 当前配置=局部最优; cpp20 已发布 |
| 2026-08-07 | 139679-81 | cpp20 外战 | — | **再胜 P^GPT** 2363:2028; ZZK 1721:1728 仅差7分; 负凑企鹅 | — | ZZK 已是硬币局 |
| 2026-08-07 | 139734-54 | 天梯上段扫荡 | 挑战 12 个 15-30 名区间 | **4胜8负**(胜ZZK/counterfactual/嘟嘟/Q-Knight; 负者全是≤1.5μs快手) | — | 水位 22-28; 瓶颈=对≤1.5μs档全程后手 |
| 2026-08-07 | 139771-82 | 多图天梯 | 地图2/3 各挑 5 人 | 图2 **4:1**; 图3 2:3(两局6分/60分惜败) | — | 开阔图强势; **稠密图是相对短板**(直线导向吃亏), 后续方向 |
| 2026-08-07 | 139890-98 | v21 `cpp21` | 局部穷举改直读输入(免补丁构建) + 砍视野购买 | 基线 2705 无回退; **硬对手池 3W3L: 胜 rikka(#18,350ns) 1898:1854(0先手!), 胜 ZZK/力行**; 负数学分析(近)/凑企鹅/Convergens | P50 仍 3.8μs | ①策略已追上部分亚微秒选手; ②P50 成本在 DFS 逐边计算不在补丁构建(下步配探针拆); ③Convergens 重视野流(198费/2736毛)值得研究; 已发布 |
| 2026-08-07 | 139932-40 | v22 `cpp22` | 金币计数分派(0金→计划/1金→专用直取+d1振荡双吃/≥2金→DFS) | 基线 2754; 硬池 1W5L(胜ZZK; rikka/力行翻回去) | P50 3.8→3.3μs | ①硬池对局=±300硬币局, cpp21/22 合计: 与 rikka/力行/数学分析/ZZK 互有胜负, **稳定落后仅剩凑企鹅/Convergens**; ②本机 P50 650ns 但平台 3.3——剩余中位成本需上平台按回合类型剖析(探针法); 已发布 |
| 2026-08-07 | 139543-88 | `nsv1/nsv2` **负结果** | 从 220ns 预算倒推的反射架构(无BFS/DFS→加回微型穷举) | 延迟达标(P50 1.1-2μs, 先手 80-100%)但**分数仅 900-1100** | 首负 cpp1 一次 | **先手优势(~+300)买不回策略机器价值(~1500)**。#1 的护城河是 190ns 里塞着顶级策略, 不是单纯砍算法。主线维持 cpp18 |
| 2026-08-07 | 140135-40 | cpp22 vs **前三**摸底 | 用户设定新目标=击败前三 | 0W6L: Tiuntled 1582/1577:1989/2268; Tundra 1544/1489:2418/2219; Ausdroid 1240/1309:2668/2578 | 对方实测 P50: 190ns/300ns/750ns(Tundra 榜显1.47μs是虚的) | ①前三全程先手, 我方跌到 1300-1600(全程后手处境); ②我方被炸 163 金/局 vs 对面≈0——炸弹是纯策略漏洞; ③拾取中位数仅 2 金(残渣), 80% 时间在中心 |
| 2026-08-07 | — | **日志逆向**(不花场次) | tests/replay.py 回放器 + 6 局全量重建 | 动作编码/炸弹波 mod20==0/漂移伤害机制 全实锤(见 §13) | 回放吻合 100% | 25 次被炸主因 = **步进阻挡漂移**(提交路径干净, 某步被 NPC/敌方挡→后续步偏移一格踩弹) |
| 2026-08-07 | 140487-516 | cpp23/b/c 防炸三连 | ①防漂移截断 ②DFS炸弹罚下限 ③陈旧雾格风险分 ④导向绕陈旧 | 基线 2130→2415→2448(均低于 cpp22 的 2754) | 截断无门控 93 次/局(实际事件仅~4) | **两个负结果**: 导向绕陈旧(-600, 探索方向必然全陈旧→蛇形)已撤; 陈旧风险分(-300, 富时冻结窗口扩张)已撤。教训: 风险惩罚必须给出"替代路径", 否则等于自废行动力 |
| 2026-08-07 | 140519-23 | **v23d `cpp23d` 定型** | = cpp22 + 防漂移截断(持币≥300+可信阻挡门控) + 炸弹罚下限 | **基线 2537/2976 均 2757(无损)**; 对前三炸弹损失 163→52/局; vs Tiuntled 1742:1986 | 截断 37 次/局全在高持币段 | 已发布。vs #1 差距缩到 -244(140521), 140514 曾到 -103 |
| 2026-08-07 | 140557-58 | 平台探针 P1/P3 | 自博弈测组件耗时 | — | **update+开销 270ns; +双DFS 1.6-1.75μs; 全量 3.3μs** | 目标/计划层 ≈1.7μs 是最大块; DFS 1.35μs 次之。即便全砍到 700ns 前三仍更快——**对前三先手权不可得, 分数路径=后手收入** |
| 2026-08-07 | 140531-35 | v24 `cpp24` **负结果** | 被压制(对手收入≥1.5x)门控的外圈转移 | 前三局 1313/1658/1724/1038, 无增益; 被炸回升(231/195) | 外圈驻留几乎没变(23-37%) | 门控 vs Tiuntled 从不触发(1.14x < 1.5x); 已回退 |
| 2026-08-07 | 140571-72 | v25f 强制外圈 vs #1 **负结果** | CAMPTH=0 强制外圈转移, 直击 Tiuntled | **1349/1773 无增益, Tiuntled 反涨到 2254/2427**(原 ~2000) | — | **让出中心=送分**: 我们在中心的存在本身压制蹲守者进食(这就是 #1 对谁都只拿 ~2000 的原因)。外圈轮巡方向判死; 正确方向=中心对抗效率 |
| 2026-08-07 | 140578-79 | v26 关目击盖章 vs #1 **负结果** | -DNOSIGHTSTAMP: 不再因看见敌人折半周边 | 1352/1673, 判平/微负 | — | 目击盖章不是瓶颈。**结构性结论**: 调度顺序=快玩家→NPC→慢玩家, 对前三我们永远排在 NPC 之后, 中心新刷被吃两轮 65% 只剩 ~12% 残渣(拾取中位数 2 的根因)。**后手方增收天花板已被三个方向的实验证实**——胜前三的唯一路径是把实测 P50 压到对手之下(Ausdroid 线 750ns 最近) |
| 2026-08-07 | — | profiling 基建(零场次) | -DPROFILE + profile_replay + bench.cpp(交错取最小, 噪声免疫) | 真实对局矩阵: **DFS 跑 70-82% 回合(中位成本 73%), 缓存命中仅 11-15%, pick 47-53%, gBFS 29-36%, explore 单轮 14-22μs** | 共享机单次测量噪声 ±15% | 结论: wg 分派没省到(wg2 占 64-78%); "粘性导向"无的放矢(目标在到达时清除, 非采集时) |
| 2026-08-07 | 141096-98 | v27 `cpp27` 中位手术 | ①MiniLocal 扁平化(逐位等价) ②candEval 接近型候选评估(mind>=2 免盲DFS) ③全图BFS 3轮冷却 | 基线 2567/2461/2624(均 2551, 待更多样本判质量); **平台 P50 3.4-3.7 没降**(热态 -20% 未兑现) | 冷态基准: bp1=270ns 与平台吻合; **全量-探针3 的 1.7μs 差距本地冷态复现不出** | **平台成本被"每轮冷启动"支配**(引擎轮间冲掉代码+数据), 指令数优化在平台上被冷成本淹没。数据冷读模型也解释不了→疑 I-cache/BTB; P5/P6 平台探针二分中 |
| 2026-08-07 | 141123-24 | 平台探针 P5/P6 **定案** | P5=update+分派+采集链(轮转走位); P6=P5+尾步+护栏 | P5 **1.04-1.43μs**; P6 1.11-1.29μs | 尾步+护栏≈免费 | **目标层执行 = 平台 ~2.2μs, 频率 ~50%, 正骑在 P50 边界**——把目标层频率压到 <40%, P50 应整段塌到 ~1.4μs |
| 2026-08-07 | 141135-37 | v27b `cpp27b` 定版 | +wbuf 窗口缓冲(逐位等价) +粘性到达/备胎目标 +自吃不盖章 | **基线 2767/2642/2726 均 2712(质量门过)**; P90 9-13(小降) | pick 频率仍 47%——备胎救错了对象 | 已发布。**盲探索轮(gBFS 29-33% + explore)才是高频大头**: 采集/尾步填充都作废计划, 每次 2-3 轮采集插曲后目标层从零重建(6-27μs)。下一步 = **单位目标跨采集持久化重构**(非缓存补丁) |
| 2026-08-08 | 141381-83 | **ns300 v0.1 首测** | 300ns 契约重写: 状态 250B/无 BFS·DFS/预算账本制 | 基线 1687/1798(100% 先手+先于 NPC); 自博弈合计 2946 | **P50 1.40-1.48μs, P90 1.8μs 无尖刺** | 起点即超 nsv 崩盘线(900-1100); 本地 600ns vs 平台 1.4μs 待解 |
| 2026-08-08 | 141393/141403 | ns 探针 np1/np3 | np1=扫描+对账(站桩); np3=+轮转走位(窗口移动) | np1 **380-390ns**; np3 **470ns** | 移动窗口只贵 90ns | ①冷读理论出局; ②**决策核平台成本 ~0.9μs 而本地免费 → 疑冷 BTB(分支预测器被引擎轮间冲掉)**——下一步分支削减; ③除法清除(64位idiv→倒数表)只值 100ns |
| 2026-08-08 | 141422/24 | ns302 收入第一轮 | 窗口值局部缓存+簇加成+尾步填充+矿堆门槛5 | **基线 1933/2076 均 2005 (+260)** | P50 1.58-1.65μs(+200ns, 换收入) | 中程盲区是下一收入杠杆(v1 glist 记全部金, v2 只记 ≥5 堆): 矿堆门槛→3 + 槽位 16→24 待试; 延迟侧待试: 分支削减(BTB) |
| 2026-08-08 | 141554-142235 | ns303-314 延迟攻坚 | 位图/无分支/冷函数/SIMD扫描/SoA 全套手术 + np5-11 判决探针 | P50 1.35→1.17μs; 全算全选+粘性目标**负结果**(收入957) | np10: 固定冷启动~900ns; np11: 混沌溢价~300ns | 成本三分定案; 单局判定不可靠(±300分/±80ns), 改 ≥4 局批量协议 |
| 2026-08-08 | 142310-ish | ns315-319 | 32槽矿堆+AVX2对账 / PGO+对齐 / 炸弹门控; **负结果**: 门槛3(1407), 3行隔轮二审(1150ns/1450) | **P50 1080-1170ns, 收入 1897(1755-1992 紧)** | PGO 布局运气 ±90ns | v2 现状: 距 800ns 差 ~300; B2+C(~660ns池)是仅存可压区; 收入距 v1(2712) 还差 ~800, 待端到端天梯验证先手收益 |
| 2026-08-08 | 142450-ish | **ns320-321** | 目标选择SIMD+对齐(平); PGO彩票(无大奖); **两阶段重构: 双单位背靠背扫描(输入线 miss MLP 并行)** | 交错 A/B 8 局: **ns321 P50 1070ns(1040-1100) vs ns319 1175**; 收入 1776 vs 1904(重叠) | 延迟优先定格 ns321 | v2 = **1070ns/~1800**; 距 800 剩 ~270: 阶段2 决策层复制 MLP / B2C 残余直线化 / A 段瘦身; 端到端天梯先手收益待验 |
| 2026-08-08 | 141554-56 | ns 探针 np5/np6 + ns303 | 决策核二分(只采集/只目标) | 各自 +550-650ns, 无单一元凶 | ns303(炸弹位图+填充门控) 1.34-1.38μs | 成本与执行的分支代码量成正比(冷前端理论) |
| 2026-08-08 | 141576-93 | ns304 掩码扫描+统一决策; -Os 实验 | 无分支掩码扫描/双支合并; 代码 17.4K→5.6K | 掩码化不降延迟(1.4μs); **-Os 也不降(1.52μs)** | 基线 1548-1908 均 1717(疑目标层小回归, 待批量A/B) | 代码体积理论出局 |
| 2026-08-08 | 141601-48 | **平台内部遥测建成**(rdtsc+vp信道) | 分段计时, vp字段按位回传, 日志 vision_r 解码(注意视野生效延迟1轮, 偏移251) | **总账: A对账542 / B1掩码循环520x2 / B2打分349x2 / C决策359x2 / D尾步137x2 周期, 总和=实测P50** | 行数5→3 B几乎不动(931→891); 预取无效 | **定案: 成本∝每轮执行的带分支循环结构数(冷BTB/循环机制每轮重启), 非数据量/触线数/代码体积**。300ns 路径=全展开无分支定形扫描+对账入循环(wv7)+直线化决策 |

> 每次上传都追加一行。记录**改了什么**和**观测到什么**，即使是负结果。
> 日志存 `logs/game_<id>.log`（已 gitignore，1.5MB/局）。

---

## 11. 纪律

赛事诚信守则：独立参赛；严禁共享账号密码与策略代码；赛事文档与数据不得传播转载。

对本仓库的含义（**只有这三条，执行即可，不需要再讨论**）：
- GitHub 仓库保持 **private**，不要转 public
- 不要把 `docs/`、`AGENT.md` 或策略代码分享到仓库外
- 若将来必须转 public：**先找主办方改密码**，再转（git 历史删不掉旧值）

---

## 12. 平台 API 与 CLI

**所有者已决定走纯 API 自动化**（浏览器截图循环太慢，不作为常态工作流）。接口契约是从公开的前端 bundle 读出来的，不是探测出来的。

> 仍未做的一件事：去赛事群/坤坤小助手（微信 `Ubihr2012`）确认脚本化提交是否被允许。拿到明确绿灯前，别把频率开到很高——服务端 500 场/天的额度就是天然节流，别绕它。

### CLI：[tools/gr.py](tools/gr.py)

纯标准库，无依赖，开发机上直接能跑。Token 自动获取并缓存到 `tools/.gr_token`（已 gitignore），失效自动重登。

```bash
./tools/gr.py rank                                  # 排行榜(含 P90，纳秒)
./tools/gr.py maps                                  # 地图 id
./tools/gr.py opponents                             # 可挑战的他人代码 id
./tools/gr.py games -n 20                           # 我的对局记录
./tools/gr.py info <game_id>                        # 单局状态
./tools/gr.py log <game_id> -o logs/x.log           # 下载日志
./tools/gr.py watch <game_id>                       # 轮询至完成并自动存日志

# 自博弈（不计入公开外战胜率，用于版本对比）
./tools/gr.py submit --map 1 src/player.so:v2 src/baseline_random.py:rand0
# 挑战他人（计入 24h 外战胜率，弱版本别打）
./tools/gr.py submit --map 1 --vs <model_id> src/player.so:v2
# 只组装不发送
./tools/gr.py submit --dry-run ...

# 日志分析（gr.py 的下游，见 §13）
./tools/grlog.py logs/game_<id>.log --rounds

# 发布公开代码(供他人挑战, 被动对局计入外战胜率)
./tools/gr.py publish src/player.so:cpp4
```

注意：`add_model_4`(publish) 用**单数**字段名 `model_file/model_lang/model_name`，
与 `add_model_1`(submit) 的复数形式不同——2026-08-07 实测探出，别改回去。
我们的公开模型在他人列表里显示为 `player220`（队名 `0x8F`）。

### 接口契约

```
BASE  http://47.103.127.219
鉴权  Authorization: <token>        原始值，不是 Bearer
封装  {code, data}；code 非 0 即错误；1001-1004 = token 失效
日志  get_game_log 直接返回纯文本，不走封装
```

| 端点 | 方法 | 参数 |
|---|---|---|
| `/api/user/login` | POST | `{key}` → `{token, user:{role}}` |
| `/api/user/get_user_info` | GET | — |
| `/api/user/get_stage` | GET | — |
| `/api/user/get_map_list` | GET | → `{list:[{id,name}]}` |
| `/api/user/get_model_list_4` | GET | 他人代码 → `{list:[{id,name,user_name_cn}]}` |
| `/api/user/add_model_1` | POST | multipart，见下 |
| `/api/user/add_model_4` | POST | 上传供他人挑战的代码 |
| `/api/user/get_game_list_1` | GET | `page`,`page_size`（公测） |
| `/api/user/get_game_list_2` | GET | 初赛 |
| `/api/user/get_game_info` | GET | `id` → `{is_parse_log,is_upload_log,error_msg,viewer_side}` |
| `/api/user/get_game_log` | GET | `id` → 纯文本日志 |
| `/api/user/get_rank_list_1/2/3` | GET | 排行榜 |

`add_model_1` 的 multipart 结构：
```
map_id
自博弈  model_files ×2, model_langs ×2, model_names ×2
挑战    model_id(对手) + model_files ×1, model_langs ×1, model_names ×1
语言    Python=1(.py)   C++=2(.so)
Model名 限字母数字、字母开头
```

---

## 13. 对局日志格式

`get_game_log` 返回按行分隔的 JSON，一局约 **1.5MB / 502 行**。
**先用 [tools/grlog.py](tools/grlog.py) 看摘要**，只有它答不了的问题才手写解析。

| 行 | 内容 |
|---|---|
| 1 | `{"player1":"名字","player2":"名字"}` |
| 2 | **17×17 真实地图**（字符串值，无迷雾）：`"0"`空地 `"1"`障碍 `"2"`炸弹刷新位 |
| 3+ | 每行一个回合 |

回合对象：
```
{round, start:{grid, players, npcs, overlap_events, npc, vision_r},
        end:  {grid, players, npcs, overlap_events, npc,
               dispatch_order, trample_events, burned}}
```

关键字段：
- **`end.dispatch_order`** — 本回合完整执行顺序，如 `[2,-1,-7,...,1]`：先玩家2，再 7 个 NPC（负数 id），最后玩家1。**`[0]` 就是谁先手**，用它实证延迟优势。
- `players[].cost` — 该局耗时，**单位纳秒**
- `players[].gold` / `vision_spent` — 毛金币与视野花费
- `players[].units[].actions` / `pickup` / `position` / `gold`
- `start.vision_r` — 各玩家视野半径（默认 2，即 5×5）
- `trample_events` / `burned` — 踩踏与炸弹事件；`burned` 是**当轮全场损失量合计（含 NPC），不区分归属**，双方各自的炸弹损失要靠金币曲线突降反推

> ⚠️ **日志按视角过滤**：对手 `units[].position` 全是 `null`，只能看到对手的金币总数。无法从自己的日志完整反推对手走位——对手建模只能靠间接推断。

**逆向实证结论（2026-08-07, 基于 140135-40 六局全量重建）**：
- **动作编码**：`0`上 `1`下 `2`左 `3`右 `4`停（与 game_api.h 一致）。
- **`units[].actions` 是"有效动作"**：被阻挡的步被记为 `4`，不是我们提交的原值。
  用它 + 上轮终点位置 + 真实地图做步进模拟，终点吻合率 100%（含"两者皆合"）。
- **`start.grid` 就是当轮决策输入**：雾窗与上轮终点位置的 5×5 并集完全对齐。
- **炸弹波在 round%20==0 刷新**（首见 -3 的 mod20 分布峰值 332 vs 均值 ~40）；
  命中位置**不限于**地图第 2 行的 `"2"` 标记格。
- **步进阻挡漂移是真实伤害源**（FAQ§294）：提交的干净路径中某步被动态实体
  （NPC/敌方，决策后才移动）阻挡 → 该步跳过、后续步从偏移位置照走 → 踩上
  本来避开了的炸弹。6 局 25 次被炸中至少 5 次实锤此机制（r377 逐步复盘）。
- **回放器 `tests/replay.py`**：把日志逐轮喂给本机 .so 复现线上决策（喂真实位置，
  不累积分叉），是行为验尸和新版本历史回测的标准工具。
